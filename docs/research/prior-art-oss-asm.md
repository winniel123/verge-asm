# Prior art: open-source ASM, recon, and asset-inventory platforms

Research ticket #2 (wayfinder). Survey conducted **2026-07-30/31**. This survey read all
dates for "last commit" / "last release" from the GitHub REST API on those dates
(`GET /repos/{owner}/{repo}`, `/releases/latest`, `/commits?per_page=1` against the
default branch). They are point-in-time facts and will drift.

**Method.** Every claim below has a first-party source: the project's own README, its own
documentation site, its own source code, or the vendor's own pricing page. It uses no listicles
and no "top 10 ASM tools" posts. Where a README claim and the source code disagreed, the source
code wins. This document notes each discrepancy.

**The question this survey exists to answer:** verge-asm's proposed differentiator is
*exposure drift* — "what changed since last time". So for each tool the load-bearing
column is **"models change over time"**. Code or docs answer that column, not marketing
copy.

---

## 1. Comparison table

| Tool | Discovers | Deployment | Audience | Models change over time? | Licence | Maintained (as of 2026-07-30) |
|---|---|---|---|---|---|---|
| **OWASP Amass** | Subdomains/FQDNs, IPs, ASNs, netblocks, certs, WHOIS, via ~50 passive APIs + DNS brute force/zone walking/reverse sweep; optional active cert pulls | Single Go binary, Homebrew, or Docker; persistent graph DB (asset-db) on a mounted volume | Both — README testimonials cite red team and adversary simulation; project positions itself as EASM | **Partially.** Ships `oam_track`: *"Analyze OAM data to identify newly discovered assets"*, with a `-since` flag. Additive only — reports FQDNs whose `CreatedAt` and `LastSeen` are both ≥ `since`. No removals, no changed ports/certs/services, FQDNs only. No scheduler, no alerting, no UI. | Apache-2.0 (LICENSE file header: `SPDX-License-Identifier: Apache-2.0`; GitHub's classifier reports "Other"/NOASSERTION because of the prepended copyright line) | **Active.** v5.1.1 released 2026-04-07; last default-branch commit 2026-04-07; repo pushed 2026-07-19 |
| **reNgine** | Subdomains, IPs/ports, endpoints, directory fuzzing, screenshots, WAF detection, WHOIS, OSINT (employees/emails), **plus vulnerability scanning** (Nuclei, Dalfox XSS, S3 misconfig) | Docker Compose + PostgreSQL + Celery workers; install script targets Ubuntu/VPS; docs tune worker concurrency for 4–16 GB RAM | **Offensive.** README: *"security pros, pentesters, and bug bounty hunters"* | **Yes — the strongest OSS match.** `get_new_added_subdomain()` and `get_removed_subdomain()` in `web/reNgine/common_func.py` diff the current `ScanHistory` against the previous one for the same domain and return added/removed `Subdomain` objects. Docs call it *"Recon data changes!"*: *"If any new vulnerabilities has been identified, or any subdomain no longer found, reNgine can detect the changes"*, with Discord/Slack/Telegram notifications, plus **Clocked Scans** (*"Run reconnaissance exactly at X Hours and Y minutes"*) and **Periodic Scans** (*"Runs reconnaissance every X minutes/hours/days/week"*). Scope of the diff is **subdomains + vulns only** — not ports, certs, or DNS records. | GPL-3.0 | **Mixed.** Last release v2.2.0 on **2024-09-07** (~22 months old). Default branch `master` last commit **2025-11-16**. Real activity is on an unreleased `release/3.0.0` branch (commits through 2026-06-26, e.g. *"feat: add scan insights and correlation utilities"*). A community fork, `Security-Tools-Alliance/rengine-ng`, last released v2.2.1 on 2025-08-31 and last committed 2025-08-31. |
| **ScopeSentry** | Subdomains, ports, web fingerprints, directories, crawler/URL extraction, sensitive-info leakage, subdomain takeover, POC-based vuln scanning, ICP filing lookup, WeChat mini-programs | Docker Compose (MongoDB + Redis + server + scan node); multi-node by deploying extra scan containers | Security teams / red team; Chinese-first with an English UI toggle | **Only page content, not asset inventory.** The "Page Monitoring" feature stores per-URL `Content`/`Hash` arrays and exposes `PageMonitoringDiffRequest`, `PageMonitoringBody{MD5, Content []string \`bson:"content" json:"diff"\`}`, and a `Similarity float64` score (`internal/models/page_monitoring.go`). That is HTTP-response-body diffing for a watchlist of URLs. There is no new-host / removed-host / new-port drift object. | **Problem.** No `LICENSE` file exists in the repo root. The README states: *"All branches of this project follow AGPL-3.0, and additional terms need to be followed: 1. The commercial use of this software requires a separate commercial license. 2. Companies, organizations, and for-profit entities must obtain a commercial license before using, distributing, or modifying this software."* | **Very active.** v1.9.3 released 2026-07-26; last commit 2026-07-26 |
| **Reconmap** | **Nothing autonomously.** It wraps and schedules other people's CLI tools, parses their output, and stores findings for reporting | Docker Compose, or a hosted SaaS via netfoe.com | **Offensive / consultancy.** *"collaboration-first security operations platform for infosec teams and MSSPs, enabling end-to-end engagement management"* | **No.** It is an engagement/project lifecycle tool (recon → execution → reporting). No continuous monitoring or scan-to-scan diff documented. | Apache-2.0 | **Active.** 3.2.2 released 2026-06-26; last commit 2026-07-27 |
| **Faraday** | **Nothing.** Ingest-only — normalises output from *"more than 80+ supported tools"* via plugins | Docker Compose, Docker + Postgres, PyPI, .deb/.rpm, or source | Both; explicitly aimed at *"managers and analysts alike"*, with CI/CD ingestion guides | **No inventory drift.** It is vulnerability management, not asset discovery, so there is no "assets since last run" concept. It does have cron-driven **agent schedules** in the open-source edition (see `CHANGELOG/5.23.0/community.md`: *"Reject out-of-range cron expressions on agent schedules…"*), but that schedules re-runs, it does not diff them. The repo's changelogs are filed under `community.md`, confirming the public repo is the community edition of a commercial product. | GPL-3.0 | **Very active.** v5.23.0 released 2026-07-23 |
| **Nemo (`nemo_go`)** | IP/port (Masscan, Nmap, gogo), subdomains (Subfinder, Massdns), HTTP fingerprints (httpx, FingerprintHub), non-HTTP fingerprints (fingerprintx), CDN/honeypot detection, IPv6, Fofa/Quake/Hunter/ICP-filing APIs, Nuclei POC validation, LLM-generated task reports | Server + Worker over RPC/MQ, MongoDB; new "Standalone mode" single-file worker for internal-network use | Internal red team / pentest efficiency — README: *"提高隐患排查和渗透测试的工作效率"* ("improve the efficiency of hidden-danger investigation and penetration testing") | **Yes, but shallow.** `v3/pkg/db/asset.go` `AssetDocument` carries `IsNewAsset bool \`bson:"new"\`` ("是否是新资产"), `IsUpdated bool \`bson:"update"\`` ("是否有更新"), `TaskId`, `CreateTime`, `UpdateTime`. README: *"支持保留和查询每次任务执行的历史数据"* (retains and allows querying historical data of each task run). So: boolean new/changed flags on current state + per-task history, not a first-class drift record of *what* changed. | Apache-2.0 | **Active-ish.** v3.2.1 released 2026-02-09; last commit 2026-02-09. UI and all docs are Simplified Chinese. |
| **ProjectDiscovery: subfinder** | Passive subdomains from curated sources | Single Go binary / Docker / library | Both; bug bounty leaning | **No.** Stateless CLI. Docs describe no database, no monitoring, no diffing. | MIT | **Very active.** v2.14.0 2026-04-27; last commit 2026-07-30 |
| **ProjectDiscovery: httpx** | HTTP probing — status, title, tech stack, TLS, screenshots, response storage | Single Go binary / Docker / library | Both | **No.** Stateless CLI. | MIT | **Very active.** v1.10.0 2026-07-09 |
| **ProjectDiscovery: naabu** | TCP/UDP port scanning | Single Go binary / Docker / library | **Offensive by self-description:** *"Designed to be used in combination with other tools for attack surface discovery in bug bounties and pentests"* | **No.** Stateless CLI. | MIT | **Very active.** v2.6.1 2026-05-05 |
| **ProjectDiscovery: cloudlist** | Assets enumerated from cloud provider APIs (credentialed) | Single Go binary / Docker | Both | **No.** Stateless CLI; docs describe no state or change tracking. (Also: cloud-provider connectors are explicitly out of scope for verge-asm.) | MIT | **Active.** v1.4.0 2026-02-09; last commit 2026-06-24 |
| **ProjectDiscovery Cloud Platform** | Cloud asset discovery via *"persistent API connections"* to AWS/Azure/GCP, plus Nuclei scanning | **SaaS only** — no self-host option documented | Both | **Yes** — *"Real-Time Cloud Asset Discovery"* and *"Enable Continuous Scanning"*. This is the commercial answer; the OSS CLIs deliberately do not carry it. | Proprietary | Active |
| **BBOT** | Subdomains (*"consistently finds 20–50% more subdomains than other tools"*), emails, web vulns, cloud buckets, via APIs + DNS brute force + NLP mutations | `pipx install bbot`, Python library (sync + async), or Docker. **No web UI.** | *"built to automate your Recon, Bug Bounties, and ASM"* — offensive framing, ASM named as a use case | **No.** Event-driven: scans emit events consumed by output modules (Neo4j, webhooks, databases). No persistent inventory that a second scan is compared against. | AGPL-3.0 | **Very active.** v3.0.1 released 2026-07-21 |
| **SpiderFoot** | 200+ OSINT modules — hosts, IPs, emails, breaches, dark web, certs | Python + built-in web UI, self-hosted | OSINT / threat intel / *"mapping your attack surface"* | No scan-to-scan drift model documented. | MIT | **Effectively dormant.** Last release **v4.0, 2022-04-07**; last default-branch commit **2023-11-05**. (Repo `pushed_at` 2026-04-13 reflects non-default-branch activity only.) The commercial successor is SpiderFoot HX. |
| **natlas** | nmap-driven network exposure — open ports, services, screenshots, weak SSL, exposed SMB/NFS/rsync, password-auth SSH. *"Attack Surface Management since before Attack Surface Management was a thing"* | Server + N agents + Elasticsearch, Docker only | Defensive — organisations scanning their own ranges | Stores scan history in Elasticsearch, but no documented rescan-diff or new-asset alerting. | Apache-2.0 | **Weak.** Last release **v0.6.12, 2020-08-25** — nearly six years old. Commits continue (last 2026-03-03) but are bug fixes and CSS. |
| **XingRin (星环)** | Subdomains (Subfinder/Amass/PureDNS), ports (Naabu), sites (HTTPX), fingerprints (XingFinger, 27k rules), URLs (Waymore/Katana), dirs (FFUF), Nuclei/Dalfox checks, Playwright screenshots | `install.sh` one-command install on Ubuntu 20.04+/Debian 11+; distributed workers | **Defensive, explicitly.** *"开源攻击面管理平台 (ASM) | 面向授权安全测试与防御研究"* — open-source ASM platform for authorised testing and defensive research; *"聚焦自有资产管理"* (focused on managing your own assets) | **Yes.** Feature table lists **资产快照** (asset snapshot) — *"扫描结果对比，追踪资产变化"* (compare scan results, track asset changes) — plus **定时任务** (cron-expression scheduled scans) and **通知推送** (WeCom / Telegram / Discord). This is the closest thing to verge-asm's stated concept in OSS. | MIT | **Paused.** v1.5.8 released 2026-01-11; last commit 2026-07-22. But the README states in bold: *"当前版本暂停维护"* — the current version's maintenance is suspended pending a ground-up rewrite with a Go backend, earliest August. UI and all docs are Simplified Chinese. |
| **Mantis (PhonePe)** | Discovery → recon → vulnerability scanning workflow orchestration | CLI / Python | Enterprise security teams | Not documented as a drift model. | Apache-2.0 | Last release `cli-1.6.0` 2025-03-21; last commit 2026-07-07 |
| **Sn1per** | Recon, scan, exploit, report; markets itself as an *"attack surface management platform"* | Shell + Docker; Community vs Professional split | **Offensive** (penetration testing) | Drift/monitoring features are in the paid Professional edition. | "Other" — non-OSI custom licence with a commercial edition | Last release **v9.2, 2023-07-29**; commits through 2026-07-04 |
| **DefectDojo** | Nothing — ingest-only vulnerability management / ASPM | Docker/K8s, self-hosted | Defensive | Tracks findings over time (dedup, reimport, mitigation) but has no asset discovery, so no exposure drift. | BSD-3-Clause | **Extremely active.** 3.1.303 released 2026-07-30 |

### Reference points: commercial free tiers ("what good coverage looks like")

| Service | Free tier | Change monitoring on free tier? |
|---|---|---|
| **Netlas** | "Community", free forever: **50 requests/day**, **2.5K search results/month**, **up to 200 results/download**. Access to internet scan data + DNS & IP WHOIS lookups. Domain WHOIS, SSL certificate search, Private Scanner, CVE data and threat intel are **excluded**. Attack Surface Discovery capped at **20 nodes per group / 100 nodes per surface** (vs 100/1K at $49/mo Freelancer, 1K/10K at $249/mo Business). Plan is **"for personal use only"** — non-commercial, which disqualifies it for the small-org operator verge-asm targets. | **No.** The Attack Surface Discovery page describes a one-shot seed-to-graph expansion (horizontal/vertical expansion over WHOIS, DNS, shared certs, IP ranges) with no re-run, monitoring, or new-asset alerting documented. |
| **Censys** | *(Caveat: `docs.censys.com` returns HTTP 403 to automated fetches. The credit figures below are Censys's own documentation text, read via a search-engine excerpt of `docs.censys.com/docs/platform-credits-free-starter` and `/docs/censys-credits`; the tier table figures are from `censys.com/resources/pricing/` fetched directly. Verify the credit numbers by hand before relying on them.)* Free accounts get **100 credits/month**, no rollover, giving *"basic internet visibility, access to basic protocols and certificates, and limited web property data such as root endpoints."* A standard query costs 5 credits (~20 searches/month); a single-asset lookup costs 1; paging results costs another 5. The published pricing table starts at **Starter, from $100**, which itself caps at **500 viewable results per search**, **2 collections**, and **7 days of host data history**; downloads, screenshots, CVE data, and live re-scan are excluded even there. | **No.** Collections (the tracking primitive) start at Starter, and even Starter has only 7 days of host history. Censys ASM is an enterprise product. |
| **Shodan Monitor** | Not a free tier, but the closest commercial analogue to verge-asm's core promise. Shodan's own guide describes network alerts as *"a private firehose where the only banners you see are from your monitored IPs"*, auto-alerting on **new services**, high-risk exposures (ICS, unauthenticated databases, Telnet) and **changes to publicly accessible resources**. Pricing is not stated on the help page. | **Yes** — this is exactly the drift-alerting product, but SaaS and paid, and it only sees what Shodan's own crawler sees (IP-range-centric, weak on subdomain/CT-driven discovery). |

---

## 2. What the landscape actually looks like

Three clusters, and only one of them is a real competitor.

**Cluster A — stateless CLI primitives** (subfinder, httpx, naabu, cloudlist, BBOT, Amass-as-enumerator). Excellent, MIT/AGPL, extremely well maintained, and *deliberately* stateless. They are the building blocks verge-asm would consume, not competitors. Amass is the partial exception. `oam_track` genuinely answers "what FQDNs are new since `-since`" from a persistent graph DB. But the answer is additive-only, DNS-names-only, CLI-only, and has no scheduler or notifier attached.

**Cluster B — case-management shells** (Reconmap, Faraday, DefectDojo). These have UIs, users, and persistence, but perform no discovery of their own. They solve "where do findings live", not "what is exposed and what changed".

**Cluster C — full self-hosted recon platforms with a database and a UI** (reNgine, ScopeSentry, Nemo, XingRin, natlas, Sn1per, Mantis). This is the only cluster that overlaps verge-asm. Within it:

- **reNgine already does subdomain drift properly.** Not "sort of" — it computes set differences between consecutive scan histories, separates added from removed, and pushes both to Slack/Discord/Telegram on a cron. Anyone claiming "no OSS tool does exposure drift" has not read `common_func.py`.
- **XingRin already advertises exactly verge-asm's pitch** — asset snapshots, scan-result comparison, cron scheduling, notification push, MIT-licensed, self-hosted, explicitly framed for defending your own assets. It is 623 stars and eleven months old.
- **Nemo flags new and updated assets** at the record level with per-task history.
- **ScopeSentry's "monitoring" is not asset monitoring** — it is HTTP body hashing for a URL watchlist. Marketing and code disagree here and the code is unambiguous.

The risk signals in verge-asm's brief — expiring certs, dangling DNS / subdomain takeover, plaintext HTTP, exposed admin panels, unexpected open ports — are **already commodity**. Subdomain-takeover detection is a named feature in ScopeSentry and reNgine. The rest come from httpx + a handful of Nuclei templates, which every tool in Cluster C already bundles. There is no differentiation available in the signal set.

Where Cluster C is genuinely weak, and where the weaknesses are consistent:

1. **Drift is a notification, not an object.** In every one of these tools the primary surface is "here is the current asset list" or "here is scan #47". Change is a Slack message that scrolls past, or a boolean flag. None of them has a first-class, queryable, historical drift record you can open on Monday and read as "here is the diff of your exposure across the last 30 days". reNgine computes its diff on demand between two adjacent scans. That diff is not itself an entity with a lifecycle.
2. **Drift is subdomain-shaped.** Amass tracks new FQDNs. reNgine diffs subdomains (and vulns). Nobody diffs *ports opening and closing*, *TLS certificates rotating or approaching expiry*, *DNS records repointing*, *HTTP titles/status changing*, or *services appearing on an existing host* as a unified change stream. ScopeSentry diffs page bodies, which is the wrong granularity in the other direction.
3. **Scope creep to a vuln scanner is universal.** reNgine ships Dalfox and Nuclei. ScopeSentry ships POC scanning. Nemo ships Nuclei. XingRin ships Nuclei and FFUF. Mantis and Sn1per go further. This is what makes them heavy (reNgine's docs tune Celery concurrency for 4–16 GB RAM), noisy, and legally fraught for an operator who only wants to know what's exposed. It is also why they are all framed for pentesters.
4. **The defensive, English, maintained, permissively-licensed quadrant is empty.** The two tools closest to verge-asm's concept are Chinese-only (Nemo, XingRin) and one of them is on an announced maintenance pause. The English-language leader (reNgine) is offensively framed, has not cut a release in 22 months, and its default branch has been quiet since November 2025. ScopeSentry is disqualified outright for the target user: its README forbids companies and for-profit entities from *using* it without a commercial licence, and it ships no LICENSE file at all.

---

## 3. What does verge-asm do that these don't?

**Honest answer: nothing that is technically novel. The gap is real but it is a gap in packaging, scope discipline, and maintenance — not in capability.**

Everything on verge-asm's v1 list exists somewhere in open source today:

- Passive discovery from CT and DNS: subfinder, Amass, BBOT — all better at it than a v1 will be.
- Active probing: httpx and naabu, which every platform in Cluster C already wraps.
- Scan-to-scan diffing with added/removed sets: **reNgine has this and it works.**
- Asset snapshots + change tracking + cron + notifications, self-hosted, MIT, defensively framed: **XingRin advertises precisely this.**
- New/updated asset flags with per-task history: **Nemo has this.**
- Expiring certs, dangling DNS, plaintext HTTP, exposed admin panels, unexpected ports: commodity Nuclei/httpx output, present in all of Cluster C.

So the case for building verge-asm cannot be "no tool does exposure drift". It has to be one of the following, and each is a product claim rather than a technical one:

1. **Drift as the primary domain object**, with its own identity, history, and lifecycle — a `Change` you can list, filter, acknowledge, and query over 30/90 days — rather than a diff computed ad hoc between two scan rows and fired at a webhook. No surveyed tool models change this way. This is defensible but it is a data-model and UX claim. A determined contributor could add it to reNgine.
2. **Drift across all asset facets, not just DNS names.** Ports, certificates, DNS records, service banners, and HTTP identity in one unified change stream. Amass does FQDNs. reNgine does subdomains. ScopeSentry does page bodies. Nobody does the union. This is the most substantive genuine gap found.
3. **Scope discipline as a feature.** No vulnerability scanner, no cloud connectors, no exploitation. Every Cluster C tool went the other way, and their weight and framing are the direct consequence. A tool a small-org owner can stand up on a 2 GB VPS and hand to an auditor is a different product from reNgine, even if the discovery pipeline is identical.
4. **Being an actively maintained, English-language, AGPL, defensively-framed option.** Today that quadrant is empty by accident, not by design — reNgine stalled, XingRin paused, SpiderFoot dormant, natlas last released in 2020, ScopeSentry commercially encumbered. This is a real opening and a fragile one: reNgine 3.0 landing, or XingRin's Go rewrite shipping with English docs, would substantially close it.

If the project states its differentiation as "we do exposure drift and others don't", that claim is false, and the project should drop it. The defensible statement is narrower: **"a small, unencumbered, defensively-framed tool where the change record is the product, and where change is tracked across ports, certificates, and DNS — not just subdomains."** Points 1 and 2 are worth building. Points 3 and 4 are worth being honest that they are positioning, and that they can evaporate if reNgine ships 3.0.

One further recommendation the survey supports: **before committing, install reNgine and XingRin and use them for a week.** Reading `common_func.py` says reNgine's drift works. It does not say whether it is *usable* as a Monday-morning report, and that is the entire wager.

---

## Sources

Repository metadata (licence, stars, last release, last commit) retrieved 2026-07-30/31 from the GitHub REST API.

- OWASP Amass — https://github.com/owasp-amass/amass · README https://raw.githubusercontent.com/owasp-amass/amass/master/README.md · user guide https://raw.githubusercontent.com/owasp-amass/amass/master/doc/user_guide.md · `oam_track` source https://github.com/owasp-amass/amass/blob/master/cmd/oam_track/main.go and https://github.com/owasp-amass/amass/blob/master/internal/track/cli.go · LICENSE https://github.com/owasp-amass/amass/blob/master/LICENSE
- reNgine — https://github.com/yogeshojha/rengine · README https://raw.githubusercontent.com/yogeshojha/rengine/master/README.md · docs https://rengine.wiki/ · diff source https://github.com/yogeshojha/rengine/blob/master/web/reNgine/common_func.py · releases https://github.com/yogeshojha/rengine/releases · `release/3.0.0` branch https://github.com/yogeshojha/rengine/commits/release/3.0.0
- reNgine-ng fork — https://github.com/Security-Tools-Alliance/rengine-ng
- ScopeSentry — https://github.com/Autumn-27/ScopeSentry · README (features + licence terms) https://github.com/Autumn-27/ScopeSentry/blob/main/README.md · docs https://www.scope-sentry.top/en/ · page-monitoring model https://github.com/Autumn-27/ScopeSentry/blob/main/internal/models/page_monitoring.go
- Reconmap — https://github.com/reconmap/reconmap · README https://raw.githubusercontent.com/reconmap/reconmap/master/README.md · https://reconmap.com/
- Faraday — https://github.com/infobyte/faraday · README https://raw.githubusercontent.com/infobyte/faraday/master/README.md · community changelog https://github.com/infobyte/faraday/blob/master/CHANGELOG/5.23.0/community.md · https://faradaysec.com/pricing
- Nemo — https://github.com/hanc00l/nemo_go · README https://github.com/hanc00l/nemo_go/blob/main/README.md · asset model https://github.com/hanc00l/nemo_go/blob/main/v3/pkg/db/asset.go
- ProjectDiscovery — subfinder https://github.com/projectdiscovery/subfinder and https://docs.projectdiscovery.io/tools/subfinder/overview · httpx https://github.com/projectdiscovery/httpx · naabu https://github.com/projectdiscovery/naabu · cloudlist https://github.com/projectdiscovery/cloudlist and https://docs.projectdiscovery.io/tools/cloudlist/overview · cloud platform assets https://docs.projectdiscovery.io/cloud/assets/overview
- BBOT — https://github.com/blacklanternsecurity/bbot · README https://raw.githubusercontent.com/blacklanternsecurity/bbot/dev/README.md · https://www.blacklanternsecurity.com/bbot/
- SpiderFoot — https://github.com/smicallef/spiderfoot · releases https://github.com/smicallef/spiderfoot/releases
- natlas — https://github.com/natlas/natlas · README https://raw.githubusercontent.com/natlas/natlas/main/README.md
- XingRin — https://github.com/yyhuni/xingrin · README https://github.com/yyhuni/xingrin/blob/main/README.md
- Mantis — https://github.com/PhonePe/mantis · https://phonepe.github.io/mantis/introduction/introduction.html
- Sn1per — https://github.com/1N3/Sn1per · https://sn1persecurity.com
- DefectDojo — https://github.com/DefectDojo/django-DefectDojo
- Netlas — pricing https://netlas.io/pricing/ · Attack Surface Discovery https://netlas.io/features/discovery/
- Censys — pricing https://censys.com/product/pricing and https://censys.com/resources/pricing/ · free/starter credits https://docs.censys.com/docs/platform-credits-free-starter · credits https://docs.censys.com/docs/censys-credits · access tiers https://docs.censys.com/docs/data-access-tiers-entitlements
- Shodan Monitor — https://help.shodan.io/guides/how-to-monitor-network
