## Destination

A written v1 spec for verge-asm — an AGPL-3.0, self-hosted, single-tenant web application that takes seed domains and IP ranges, discovers internet-exposed assets via passive sources then active probing, and makes **exposure drift a first-class, queryable object with its own lifecycle — tracked across ports, certificates, DNS records and HTTP identity, not subdomains alone** — complete enough to hand to implementation sessions.

That emphasis is the differentiation, and it is deliberately narrow: the prior-art survey established that discovery, probing, scan-to-scan diffing and the risk signals all exist in open source today. What does not exist is change modelled as a durable object across every asset facet. The spec must earn that claim or the effort has no reason to exist.

## Notes

- **Plan, don't do.** This map produces the spec, not the implementation. Every ticket resolves a decision.
- **Recommendations are pre-accepted — run every ticket AFK.** Standing instruction from the map's dev, 2026-08-13: *"I'm taking all your recommendations on the grilling tickets. Assume I'm going with your recommendations unless I come in and say otherwise. I want to make this as AFK-able as possible."* This **overrides the ticket-type HITL rule** for this effort — `grilling` and `prototype` tickets run without waiting on a live human, which is the override the skill routes through this section. What it does **not** license is skipping the interrogation: `/grilling` and `/domain-modeling` still run in full, the session just argues both sides itself and rules, rather than leaving the question hanging. Three riders bind. **The resolution comment is where review happens, so it must be reviewable** — it names the option that lost and why it lost, because a recommendation taken silently is one the dev cannot disagree with later. **Do not manufacture consensus**: where the evidence genuinely underdetermines the answer, say so in the comment and rule anyway, flagging it as a decision taken on thin ground rather than dressing it as settled. And **stop for a scope change, never for a preference** — a resolution that would redraw the Destination, or that costs money, sends mail to a third party, or is otherwise hard to walk back, is put to the dev; everything short of that is the session's call.
- **Licence: AGPL-3.0.** The network copyleft is deliberate. Dependencies and design choices must be compatible; flag anything that would force a licence change.
- **Defensive posture.** The operator owns the assets being scanned, so no scope-consent machinery is needed — but probing safety against the operator's own production is a live concern. **Qualified by [ADR-0002](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0002-ownership-gates-probing.md):** discovery follows records outward, so "the operator owns it" is not automatically true of what discovery reaches. Ownership is derived per address and *gates* probing, which is how the premise is kept true rather than assumed.
- **Single-tenant, self-hosted** via `docker compose`. No multi-tenancy, billing, or hosted infrastructure in scope.
- **The user** is a small-org security owner. The product answers: *what of ours is exposed to the internet, and what changed?*
- **The instance is a high-value target.** Its database is a complete, current map of the operator's attack surface. Security decisions are weighed against that, not against the app's size.
- **Differentiation is narrow, and must not be overstated.** "No OSS tool does exposure drift" is **false** — see the prior-art ticket. reNgine ships working subdomain drift with scheduling and alerting. The defensible claim is narrower: drift as a *first-class, queryable object with a lifecycle*, tracked across **ports, certificates, DNS records and HTTP identity** rather than subdomains only. Any session that finds itself writing "nothing else does this" should re-read that ticket. **The margin has since narrowed**: reNgine's unreleased `release/3.0.0` rewrite already models ports, IPs, DNS and full certificate data, and still diffs subdomains only — so the gap is now *temporal modelling alone*, not data coverage. If 3.0 ships drift across the facets it already stores, this map's differentiation is gone. Re-check `api/app/services/scan.py` and `worker/app/orchestrator/finalize.py` on that branch for any diff over a model other than `Subdomain`.
- **The operator-supplied zone file is the product's spine.** A defensive tool can ask for ground truth that an offensive recon script must guess at. Every guess-based technique is gap-filling for what the zone doesn't cover. This is a structural advantage over the offensive-framed tools in the prior-art survey, and it should be visible in the domain model, the onboarding flow, and how the project describes itself.
- **A source that errors must produce no observation, never an observation of absence.** Measured: crt.sh returns spurious 404s. In a drift product, "non-200 means empty" manufactures false "asset removed" alerts.
- **Use the glossary's words.** [`CONTEXT.md`](https://github.com/winniel123/verge-asm/blob/main/CONTEXT.md) is opinionated about vocabulary and explicit about what it refuses: **`Asset`**, **`Host`**, **`Finding`**, **`ScanRun`**, and `Discovery`/`Probe` as nouns are all rejected, each for a stated reason. A session reaching for one of them is usually about to re-introduce the failure that rejected it.
- **The design system exists and is not optional.** [`design-system/`](https://github.com/winniel123/verge-asm/tree/main/design-system) — "engineered paper": warm paper `#f7f7f4`, ink `#16160f`, one working blue `#2d4fd4`, 0px radius, hairline rules, hard offset shadows, Helvetica for UI and IBM Plex Mono for every technical value. Any session producing a visual surface — production UI, prototype, mock, slide — invokes the `verge-asm-design` skill first. Rules in [`docs/agents/design-system.md`](https://github.com/winniel123/verge-asm/blob/main/docs/agents/design-system.md). **Its visual layer is canonical; its vocabulary and IA are not** — the kit predates [#7](https://github.com/winniel123/verge-asm/issues/7) and ships a `Findings` section, a fingerprinting claim, and a Dashboard-first layout with *no drift screen at all*. Where the kit and the glossary collide, the glossary wins and the visuals get re-skinned around it.
- **Skills:** `/grilling` + `/domain-modeling` for HITL tickets; `/research` for AFK research tickets; `/prototype` for UI tickets — and `verge-asm-design` alongside `/prototype`, always.
- **⚠ THE BODY CEILING IS THE BINDING ONE NOW, NOT THE SUB-ISSUE CAP.** This body is **~243 KB against GitHub's 262,144-byte limit — 93% full, roughly 19 KB left**, which is about **two more ticket entries** at the length the recent ones run. The sub-issue list has room for six. **A session that adds an entry without first compacting is likely to be the one that cannot save the map.** Compact before the next fan-out: prior passes are archived at `docs/wayfinder/map-archive-2026-08-14{,b,c}.md`, and the cheapest wins are the **Not yet specified** patches that have re-grown by accretion and the Decisions-so-far entries whose median has drifted back above 1,400 characters.
- **The map's sub-issue list is capped at 100 by GitHub, and it stands at 95** — [#116](https://github.com/winniel123/verge-asm/issues/116) added by [#115](https://github.com/winniel123/verge-asm/issues/115), and before it [#115](https://github.com/winniel123/verge-asm/issues/115) added by [#113](https://github.com/winniel123/verge-asm/issues/113), after [#113](https://github.com/winniel123/verge-asm/issues/113) and [#114](https://github.com/winniel123/verge-asm/issues/114) were added by the #111/#112 batch. **Closing a ticket does not free a slot** — a closed child still occupies one until it is detached — so the ceiling is a function of tickets *created*, not tickets *open*, and it leaves room for roughly two more fan-outs of this size. First hit on 2026-08-14 while wiring [#102](https://github.com/winniel123/verge-asm/issues/102); the #108/#109/#110 batch added [#111](https://github.com/winniel123/verge-asm/issues/111) and [#112](https://github.com/winniel123/verge-asm/issues/112) for **91 of 100**, leaving room for roughly three more fan-outs of this size. **The next detach was prepared and not applied**: #23–#42 were verified to be closed *and* still indexed in Decisions so far with their links, so detaching them is inert — but the `DELETE .../sub_issue` call was refused by the session's permission classifier, so the batch is **queued for the dev rather than done**. The fix applied: **the 20 oldest closed children (#2–#22) were detached from the map.** This is functionally inert — the frontier query in [`docs/agents/issue-tracker.md`](https://github.com/winniel123/verge-asm/blob/main/docs/agents/issue-tracker.md) reads only **open** children, every detached ticket is still indexed in **Decisions so far** with its link, and each still carries `Part of #1` in its body — and it is reversible with a POST to the sub-issues endpoint. **A session that finds the ceiling again detaches the next oldest closed batch; it does not drop the child relationship for an open ticket, and it does not close a ticket to make room.** This is the sibling of the body-size compaction the section comments below record: the map is an index, and both its body *and its child list* accumulate.

### Rules established by closed tickets

<!-- One line per rule: the rule itself, then the ticket and ADR that hold its reasoning, its
     forcing measurement and what binds from it. Compacted 2026-08-14 — the prose these lines
     replace is preserved verbatim at docs/wayfinder/map-archive-2026-08-14.md. The map was
     compacted three times on 2026-08-14; the second pass left this section untouched, and the
     third re-cut the fourteen lines that had re-grown [measured] riders and amendment chains.
     The body before each pass is at docs/wayfinder/map-archive-2026-08-14{,b,c}.md. Zoom the
     links rather than re-deriving; a rule is never restated here, only named. -->

- **Evidential coverage and operational attempt are two different records.** — [#9](https://github.com/winniel123/verge-asm/issues/9) · [ADR-0005](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0005-scan-execution-model.md)
- **Every seam is a place drift can be manufactured.** — [#6](https://github.com/winniel123/verge-asm/issues/6)
- **Three layers, and only one of them drifts.** Declared · Observed (*the only drift-bearing layer*) · Derived — [#7](https://github.com/winniel123/verge-asm/issues/7) · [`CONTEXT.md`](https://github.com/winniel123/verge-asm/blob/main/CONTEXT.md)
- **Cite the source that owns the claim, not the loudest authority.** A row is admitted on a named claim, an owner's attestation and a determinacy check; governments and cloud vendors corroborate and never carry a row alone — [#21](https://github.com/winniel123/verge-asm/issues/21) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §2, §10, §11. **Repaired by [#37](https://github.com/winniel123/verge-asm/issues/37)** — the claim set is closed by construction, a shipped default attests only where it **restricts**, and owner keys on the **artefact**, not the party. **[#66](https://github.com/winniel123/verge-asm/issues/66) then moved the first row**: `161/udp` could name no claim once the SNMP family was retrieved — **37 pairs, not 38** — and **a negative retrieval is a verdict**. **[#69](https://github.com/winniel123/verge-asm/issues/69) moved a footing, not a row**
- **A source ships on only if the modal operator clears its terms — and changing that later is an aperture change.** — [#15](https://github.com/winniel123/verge-asm/issues/15) · [ADR-0003](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0003-third-party-source-consent-bar.md)
- **A rule may ship as a `Signal` only if its reference data changes at release cadence.** — [#16](https://github.com/winniel123/verge-asm/issues/16) · [ADR-0004](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0004-signals-are-release-coupled-rules.md)
- **Nothing leaves because time passed, and nothing leaves by a state we invented.** — [#17](https://github.com/winniel123/verge-asm/issues/17) · [ADR-0006](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0006-subjects-leave-by-measurement.md)
- **Drift is a timeline of `Span`s, and comparison is legal only within one derivation.** — [#8](https://github.com/winniel123/verge-asm/issues/8) · [ADR-0007](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0007-drift-is-a-timeline-of-spans.md)
- **A version moves on content, and a `Break` clamps rather than blanks.** — [#18](https://github.com/winniel123/verge-asm/issues/18) · [ADR-0008](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0008-derivation-versions-move-on-content.md)
- **A table deciding *where to look* is aperture; a table deciding *what an answer means* is a signature database.** — [#31](https://github.com/winniel123/verge-asm/issues/31) · [ADR-0008](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0008-derivation-versions-move-on-content.md)
- **Redistribution is a separate permission from use, and the party who needs it is the project.** — [#27](https://github.com/winniel123/verge-asm/issues/27) · [ADR-0003](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0003-third-party-source-consent-bar.md)
- **The probing gate reads Declared input only, and no third party's file can open it.** — [#27](https://github.com/winniel123/verge-asm/issues/27) · [ADR-0002](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0002-ownership-gates-probing.md)
- **Completeness of the numerator is measurable; completeness of the estate is not — so it never gets a number.** — [#28](https://github.com/winniel123/verge-asm/issues/28) · [ADR-0008](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0008-derivation-versions-move-on-content.md)
- **A list that must contain another list is not two lists, it is a union.** — [#29](https://github.com/winniel123/verge-asm/issues/29) · [ADR-0009](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0009-verge-core-is-a-union.md)
- **A rule reads a leg, never a state — a state enum is a projection, not the thing.** — [#32](https://github.com/winniel123/verge-asm/issues/32) · [ADR-0010](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0010-exposure-composes-two-reaches.md)
- **A signal reads a value, and where the value is absent the answer is already `not-evaluable` — so *may a signal fire on absence?* is never the question.** — [#35](https://github.com/winniel123/verge-asm/issues/35) · [ADR-0004](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0004-signals-are-release-coupled-rules.md)
- **A facet is six parts, and every canonical value is a closed union.** — [#36](https://github.com/winniel123/verge-asm/issues/36) · [ADR-0011](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0011-a-facet-is-six-parts.md)
- **A thing that produces no observations is not a `Source` — it yields `Proposal`s, and only `consent` applies to it.** — [#39](https://github.com/winniel123/verge-asm/issues/39) · [ADR-0012](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0012-a-proposer-is-not-a-source.md)
- **The gate reads control, not title — and it is the first Derived value that moves because the world moved.** — [#40](https://github.com/winniel123/verge-asm/issues/40) · [ADR-0013](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0013-custody-is-control-and-extends-by-declaration.md)
- **Membership is a property of a subject; aperture is a property of looking — and looking is per-timeline.** — [#42](https://github.com/winniel123/verge-asm/issues/42) · [ADR-0014](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0014-only-revealed-generalises.md)
- **A facet's value space is the commitment; the signal set over it is free forever.** — [#41](https://github.com/winniel123/verge-asm/issues/41) · [ADR-0015](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0015-the-value-space-is-the-commitment.md)
- **"100% coverage" is always coverage *of something* — check what, before quoting it.** — [#43](https://github.com/winniel123/verge-asm/issues/43) · [ADR-0012](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0012-a-proposer-is-not-a-source.md)
- **`not-evaluable` needs a subject, and the aperture half never has one.** — [#44](https://github.com/winniel123/verge-asm/issues/44) · [ADR-0004](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0004-signals-are-release-coupled-rules.md)
- **A one-legged reading is not a state, and an axis must hold one kind of thing.** — [#45](https://github.com/winniel123/verge-asm/issues/45) · [ADR-0017](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0017-exposure-needs-both-legs.md)
- **A proposer's toggle widens no aperture, and a surface that says it does is telling the operator to expect growth that will not come.** — [#47](https://github.com/winniel123/verge-asm/issues/47) · [ADR-0003](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0003-third-party-source-consent-bar.md)
- **A group that renders when populated must render when empty, and say why it is empty.** — [#47](https://github.com/winniel123/verge-asm/issues/47) · [ADR-0008](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0008-derivation-versions-move-on-content.md)
- **A conflict needs two `enumerable` sources — and the zone is readable for its names, not for its records.** — [#48](https://github.com/winniel123/verge-asm/issues/48) · [ADR-0020](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0020-a-conflict-needs-two-enumerable-sources.md)
- **A version leaf is named for what it decides, never for the artefact that ships it — and something is a `Derivation` exactly where its output can move while the world does not.** — [#49](https://github.com/winniel123/verge-asm/issues/49) · [ADR-0021](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0021-a-version-leaf-is-a-decision-not-a-binary.md)
- **Confirmation is singular, declining is plural, and the asymmetry is the safety property.** — [#50](https://github.com/winniel123/verge-asm/issues/50) · [ADR-0022](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0022-confirmation-is-singular.md)
- **A correct configuration may never sit inside a completion counter, and a hazard that fails silently may never sit behind a disclosure.** — [#51](https://github.com/winniel123/verge-asm/issues/51) · [ADR-0013](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0013-custody-is-control-and-extends-by-declaration.md)
- **`consent` names the door, never who walked through it — and an operator may be taken at their word about themselves, never about a third party.** — [#52](https://github.com/winniel123/verge-asm/issues/52) · [ADR-0023](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0023-consent-names-the-door.md)
- **A rule is four parts, and its domain is the extension of its name.** — [#53](https://github.com/winniel123/verge-asm/issues/53) · [ADR-0024](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0024-a-rules-domain-is-the-extension-of-its-name.md)
- **An offer is scope only where the value enumerates it — and a default is not a declaration.** — [#54](https://github.com/winniel123/verge-asm/issues/54) · [ADR-0025](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0025-an-offer-is-scope-only-where-the-value-enumerates-it.md)
- **A Declared act made once, about a set that is recomputed, is not a Declared widening — it is a standing licence the world moves.** — [#55](https://github.com/winniel123/verge-asm/issues/55) · [ADR-0013](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0013-custody-is-control-and-extends-by-declaration.md)
- **A source may put a subject in the estate without observing anything about it — and a decoder translates a source's *shape*, never its *fact*.** — [#56](https://github.com/winniel123/verge-asm/issues/56) · [ADR-0027](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0027-a-source-may-admit-without-observing.md)
- **The project may only claim an outbound act it can produce — and the ask behind four surfaces could not be produced.** — [#57](https://github.com/winniel123/verge-asm/issues/57) · [ADR-0003](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0003-third-party-source-consent-bar.md)
- **An alert fires on a leg, never on a state — and only the internet leg alerts.** — [#58](https://github.com/winniel123/verge-asm/issues/58) · [ADR-0029](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0029-an-alert-fires-on-a-leg.md)
- **A declared parameter is authored by the project and ships in the release — an operator's dial may sit anywhere outside every derivation and nowhere inside one.** — [#60](https://github.com/winniel123/verge-asm/issues/60) · [ADR-0004](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0004-signals-are-release-coupled-rules.md)
- **A facet's cadence is the cadence of the exchange that measures it — and a measurement needing a cadence of its own takes a `Scan` of its own.** — [#61](https://github.com/winniel123/verge-asm/issues/61) · [ADR-0028](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0028-a-facets-cadence-is-the-cadence-of-its-exchange.md)
- **An offer asserts nothing, so it is admitted on a finding it reaches or a falsity it prevents — and the expensive list gets the *lower* bar.** — [#62](https://github.com/winniel123/verge-asm/issues/62) · [ADR-0030](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0030-an-offer-is-admitted-on-a-finding-or-on-a-falsity-it-prevents.md)
- **A newly-entered subject has no transitions, so the census is the carrier — and only two of the four subjects can be a root.** — [#63](https://github.com/winniel123/verge-asm/issues/63) · [ADR-0031](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0031-membership-alerts-at-the-root-of-the-entering-subtree.md)
- **The facet layer is evidence, not a channel — a `Transition` is a message only where it is the sole carrier.** — [#64](https://github.com/winniel123/verge-asm/issues/64) · [ADR-0026](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0026-the-facet-layer-is-evidence-not-a-channel.md). **The v1 rule set is sixteen, not ten** — ADR-0021, ADR-0029 and ADR-0031 still say ten and are annotated; [#12](https://github.com/winniel123/verge-asm/issues/12) must carry sixteen
- **An evidence standard attaches to a table, not to a rule — and there are three of them, cut on whether the table says what we ask, what we conclude, or what we assert.** — [#33](https://github.com/winniel123/verge-asm/issues/33) · [ADR-0032](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0032-an-evidence-standard-attaches-to-a-table-not-to-a-rule.md)
- **A move carries the rule that opens at `fired` beneath it — and nothing carries a schedule.** — [#65](https://github.com/winniel123/verge-asm/issues/65) · [ADR-0033](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0033-a-move-carries-the-rule-that-opens-at-fired.md)
- **Derive the claim from what the rule reads before looking for the owner — and where a constant is a fraction of a moving world quantity, ship the fraction rather than its product.** — [#67](https://github.com/winniel123/verge-asm/issues/67) · [ADR-0034](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0034-derive-the-claim-before-looking-for-the-owner.md). An **underived claim** and a **genuine absence** are indistinguishable from inside a retrieval, so a negative owner-hunt establishes nothing until the derivation is done
- **To place a source, name the artefact its statement is about — and a cryptographic primitive's owner is the body whose standard specifies it.** A body that decides which primitives a population may *accept* is a distributor: its floor attests about its own acceptance, never about the algorithm — [#68](https://github.com/winniel123/verge-asm/issues/68) · [ADR-0035](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0035-a-cryptographic-primitives-owner-is-its-specifier.md)
- **A shipped default is the configuration that *takes effect* and that the project *documents as its default* — an example attests nothing, and installing one transfers operativeness rather than ownership.** — [#69](https://github.com/winniel123/verge-asm/issues/69) · [ADR-0036](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0036-a-shipped-default-is-the-configuration-that-takes-effect.md). §10.4's own *costly act* test decides it **against** the example, a file no daemon reads producing no first run. A distributor's packaging corroborates under §2.3 and is **never sole grounds**. **[measured]** nine config artefacts, nine self-declarations: the file always says which it is, and surface syntax never does
- **An attestation is retrieved over the artefact, not over the row.** Every subject the artefact names is a candidate; a subject it names and the table lacks is **ticketed, never admitted**; a negative retrieval states its own extent — [#70](https://github.com/winniel123/verge-asm/issues/70) · [ADR-0037](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0037-an-attestation-is-retrieved-over-the-artefact-not-over-the-row.md). **[measured]** [#69](https://github.com/winniel123/verge-asm/issues/69) opened the right file at the right tag and read the two lines it came for: Cassandra's prohibition names **7000 and 7001** as well as 9042. The same defect one level up leaves **19 of 37 pairs** covered by the footing table
- **A constant is a product only where the quantity is readable from the subject at evaluation time — and the watch is defined by shape, not by cause.** — [#71](https://github.com/winniel123/verge-asm/issues/71) · [ADR-0038](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0038-a-constant-is-a-product-only-where-the-quantity-is-readable.md). Four limbs: form · motion · **reach** · silence. Where reach fails the fraction is **no cure** — it relocates the staleness — and the answer is a **scheduled edit**. So ADR-0032 §8's watch covers it already: **watched · chased · scope are causes of weakness, not kinds of watch**
- **A rule is named for the fact it reads, never for the content of the table or parameter that decides it — and where the predicate needs stating, the census states it, as the rule expresses it and never as a statistic over the estate.** — [#72](https://github.com/winniel123/verge-asm/issues/72) · [`CONTEXT.md`](https://github.com/winniel123/verge-asm/blob/main/CONTEXT.md)
- **A permission granted to the project does not travel to AGPL-3.0 recipients — so a licence incompatibility is cured by not depending on the thing, never by permission.** — [#78](https://github.com/winniel123/verge-asm/issues/78) · [ADR-0003](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0003-third-party-source-consent-bar.md) fifth amendment. `operator-credentialed` is untouched — that grant runs to the **operator**, a different party. **[measured]** the licence text a session already holds may be a **superseded version**: NPSL §3's obligation was narrowed by a parenthetical in 2023, and #71 §5.4 reported the pre-2023 reading, accurately quoted
- **A specification's silence is not the owner's silence — and a scope weakness is disclosed as a searched corpus, never as a caveat.** A body's specification, its deployment BCP and its implementation guidance disagree by design, so enumerate an owner's document *classes* before recording that it sets no number. A residue that survives its search is **bounded**, not permanent — falsifiable by naming one document outside the boundary — [#73](https://github.com/winniel123/verge-asm/issues/73) · [ADR-0040](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0040-a-specifications-silence-is-not-the-owners-silence.md). **[measured]** #68's negative was correct about every document it read; the floor was in a BCP and in an appendix
- **A census member is the only cut of a rule's population that the rule itself versions, and each member's count is the length of its own list — so every member is enumerable in full and none is ever sampled, ranked, grouped or truncated.** — [#74](https://github.com/winniel123/verge-asm/issues/74) · [`CONTEXT.md`](https://github.com/winniel123/verge-asm/blob/main/CONTEXT.md). **Length was never the defect**, since ADR-0015 already forbids a census rendered *as* a findings list and #51 bars `Finding`'s two refused properties at every length; what moves is placement — **the census card carries no rows at all**. **[measured]** a 4-row sample of a 214-row member shows **none** of the 20 rows that matter, and cannot
- **A rule that reads an always-current wall clock against an observed value must bound its evidence in the subject's own units, not the observer's.** — [#77](https://github.com/winniel123/verge-asm/issues/77) · [ADR-0043](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0043-a-clock-reading-rule-bounds-its-evidence-in-the-subjects-own-units.md). `certificate-expired` fires exactly when `age > not_after − t_obs`, so it can only ever fire on an observation that has outlived the remaining life of the certificate it observed; ADR-0028 let `age` grow to a number chosen **with no reference to the certificate at all**. The horizon beat a fresh fraction because it **mints nothing** — the declared parameter set stays `{⅓, ½, 10 days}`
- **A measurement with no cadence has no currency, so there is no one-off aperture — and the widest tier is the one that is asked for.** Every aperture is a `Scan`, and a `Scan` recurs; the widest tier ships **configured and disabled** with an empty scope list, so *"the operator has not enabled it"* is a legible state rather than an absence. A tier bounds **which subjects exist**, never **which rules can speak** — [#80](https://github.com/winniel123/verge-asm/issues/80) · [ADR-0044](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0044-a-one-off-measurement-has-no-currency.md). **[measured]** the default aperture costs **`0 of 37 sensitive pairs unread · 0 of 16 rules unevaluable`**
- **A squat is contested where the other convention is live.** Determinacy fails a port where the competing service's *own owner* currently documents that service on that number, and passes where the competing registration has no deployed population; liveness is read off the competing owner's documentation and **never** off a frequency source, and the test is on the **competing** service, so the candidate's own attestation strength is irrelevant — [#75](https://github.com/winniel123/verge-asm/issues/75) · [ADR-0042](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0042-a-squat-is-contested-where-the-other-convention-is-live.md). First applied by [#87](https://github.com/winniel123/verge-asm/issues/87), which confirmed all three of its predictions by use
- **A determinacy finding is made on *placement statements* and on nothing else** — a party's own current statement, in its documentation or its shipped bytes, that **its own software listens on a `(port, transport)` pair by default**. Established by the candidate's owner; **contested** where another party places a different protocol on the same pair; **displaced** where the candidate's own owner moves it or version-depends it. The unit is the **protocol, not the vendor**; *live* means **current, never numerous**; and every determinacy refusal names the artefact that defeated the convention. Everything else — IANA rows, `nmap-services`, cloud and government port tables, and this project's own frequency half — corroborates and never carries — [#82](https://github.com/winniel123/verge-asm/issues/82) · [ADR-0048](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0048-a-convention-is-evidenced-by-placement-never-by-catalogue.md). Its source rule is #31's **conclusion** test, not #21's assertion standard, so a third-party port catalogue is exactly #31's *signature database*
- **An address scope is its own enumeration and a name scope is not — so an address scope has a `Coverage` denominator and a name scope has none, which is the same fact twice.** The enumeration is exactly one hop, `Seed` → `Address`; everything below `Service` needs an open port, and no `Name` is ever enumerated — [#81](https://github.com/winniel123/verge-asm/issues/81) · [ADR-0047](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0047-an-address-scope-is-its-own-enumeration.md)
- **A declared address scope's addresses are subjects from the declaration, holding `Reach` = `not-reached` — a measured value, not a `Gap` and not nothing.** Declaring or widening one is an aperture widening: `revealed`, one coverage-class message **at the scope**, never one per address. Narrowing one withdraws the addresses with their timelines and opens no `Gap` unless a resolution still cites them — [#81](https://github.com/winniel123/verge-asm/issues/81) · [ADR-0047](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0047-an-address-scope-is-its-own-enumeration.md)
- **Ownership is tested per port, not per sentence** — where one attestation names several ports, each port is tested against §10.5 separately, and **a sentence that is an owner's for one port may be a third party's for another** — [#76](https://github.com/winniel123/verge-asm/issues/76) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §16.6 (no ADR; this states what §10.5 already required)
- **A curated table states its own coverage** — which members of the population it places and which it deliberately does not — so the extent is **in the artefact** rather than reconstructed by each reader — [#76](https://github.com/winniel123/verge-asm/issues/76) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §2.2, §16.7
- **A negative's corpus is its owner's class list, and only a sole-ground negative is exposed — a class sweep is complete as of a table state, and a footing move re-arms it for the row that moved.** — [#79](https://github.com/winniel123/verge-asm/issues/79) · [ADR-0046](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0046-a-negatives-corpus-is-its-owners-class-list-and-only-a-sole-ground-negative-is-exposed.md). Three limbs: an **overdetermined** negative is **bounded on arrival** and swept for the record, never for the verdict; the corpus **terminates**, because a standards body has three classes and a single-project owner two, and **an owner cannot invent a fourth to defeat a sweep**; and a **breadth** weakness is not a **class** weakness, so ADR-0040 does not reach it
- **An address scope is family-agnostic, and the range cap counts addresses rather than prefix lengths — so IPv6 needs no rule of its own.** `1,024` addresses per scope is one knob at one setting (`/22` in IPv4, `/118` in IPv6); the `Seed` kind does not split and no family check enters the model. Every prefix an operator is assigned is refused by the cap, because **IPv6 space is not sweepable and no configuration makes it so** — [#85](https://github.com/winniel123/verge-asm/issues/85) · [ADR-0049](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0049-an-address-scope-is-family-agnostic-and-the-cap-counts-addresses.md). **ADR-0013 §6's containment test is over every address a `Vantage` holds**, of either family, one uncovered address verifying `internet`
- **An owner's category statement reaches the members its own artefacts place inside it — §2.3 refused the *party*, never the *grammar*.** Three limbs: **standing** under §10.5 · **membership**, established by the owner's own artefact and never by a corroborator's port number · **defeat, tested per member**, wherever the owner elsewhere names that member's internet-facing deployment as supported. The category's unit is a **protocol or interface, never a vendor's product line** — [#88](https://github.com/winniel123/verge-asm/issues/88) · [ADR-0050](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0050-an-owners-category-statement-reaches-the-members-its-own-artefacts-place-inside-it.md). **A category sentence is tested per member, not per sentence** — the sibling of #76's per-port ownership rule
- **A default the owner's own bytes label *legacy* or *deprecated* is not its shipped default, however operative it is — and §10.4's one-way rule reads the default, not the artefact class that records it.** — [#83](https://github.com/winniel123/verge-asm/issues/83) · [ADR-0036](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0036-a-shipped-default-is-the-configuration-that-takes-effect.md) amendment. Claim 1 asks whether the protocol requires the operator to **supply authentication**, never whether the port is **on** — the reading that deletes a default-off port deletes `2375/tcp` Docker. **[measured]** a rendering carries no provenance about **which** of an owner's positions it renders: a correct rendering of a deprecated interface reads identically to a current one
- **A claim's cells are attestations too, and an operation only counts where the caller in question can reach it.** §10.1's two-step test is answered *"from the specification or the owner's own documentation"*, so §10.5's owner rule governs its cells — a corroborator may not answer a step — and a Step 2 operation must be one the **anonymous remote** caller reaches, read off the dispatch rather than the prose — [#84](https://github.com/winniel123/verge-asm/issues/84) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §20.6. **The mirror rule on the evidence side:** an ownership repair in one table does not propagate
- **An owner's documentation is what it has *issued*, not what it has *drafted* — and an unissued document is silent in both directions rather than a corroborator.** Issued means carried by a release artefact the owner publishes or served on its own documentation site; the prose analogue of §12's *takes effect* is **reaches a reader without the reader acting on the owner's behalf**; issuance is **per version**, so a negative names the release and the date and never says *never issued* — [#86](https://github.com/winniel123/verge-asm/issues/86) · [ADR-0045](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0045-an-owners-documentation-is-what-it-has-issued.md)
- **A subject key is the thing denoted, not the text that named it — and its normalisation is fixed rather than versioned, because a re-key is a sentence the model has no object for.** A canonicaliser is versioned because the model can price it being wrong — a `Break`; a key normalisation may not be, so it is **prevented** instead: total, specified, fixed at v1, no version leaf, may consult only the value being keyed, enforced by a corpus with an **inverted** gate. `Address`'s key is family and octets; `::ffff:0:0/96` folds and nothing else does; an ambiguous text form has no key and is **refused rather than interpreted** — [#89](https://github.com/winniel123/verge-asm/issues/89) · [ADR-0051](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0051-a-subject-key-is-the-thing-denoted-and-its-normalisation-may-never-move.md). **[measured]** on all three paths there is **no text** — an A record is 32 bits of RDATA, an AAAA 128, a certificate `iPAddress` SAN an OCTET STRING. `Name`'s key carries the identical seam and is [#94](https://github.com/winniel123/verge-asm/issues/94)
- **Limb 2 holds for `623/udp`, and the number was never CISA's to supply** — IPMI v2.0 §13.1.2 / Table 13-1 and DMTF **DSP0136** §3.2.1 both number the pair, so the row is three owner statements concatenated with **no reader inference at any hop** and [ADR-0048](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0048-a-convention-is-evidenced-by-placement-never-by-catalogue.md)'s unit rule never has to reach — [#90](https://github.com/winniel123/verge-asm/issues/90) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §23, [ADR-0050](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0050-an-owners-category-statement-reaches-the-members-its-own-artefacts-place-inside-it.md) amended. **[measured]** Dell's VU#843044 statement is ~1,700 characters and §3.4 quoted **one sentence**; the unquoted half enumerates which IPMI version each DRAC firmware ships, which is the owner placing the DRAC inside IPMI. No row moves, no ADR minted, and `4369` is still not rescued
- **A client-list cell is tested per member, and `Self` admits where `Self, Load balancers` refuses** — `10259/tcp` and `10257/tcp` are admitted to Class C on Claim 3, `10256/tcp` is refused because its cell names a member outside every branch of §10.3's enumeration — [#91](https://github.com/winniel123/verge-asm/issues/91) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §24, [ADR-0009](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0009-verge-core-is-a-union.md) amended. **The list is 39 pairs, classes `11 / 7 / 21`** — the first section since §11 to spend [ADR-0008](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0008-derivation-versions-move-on-content.md)'s price, and ADR-0009's **first widening**. §18.12's reliance objection is answered by the per-cell test **separating** the three rows where a count could only admit or refuse all three. **[measured]** `10259`/`10257` ship `Anonymous.Enabled = true` and refuse with **403 from authorization**, not the kubelet's 401. No ADR minted
- **A claim step is answered only by evidence about that step — and Step 2 is read at the internet-reached frame.** A Claim-3-shaped prohibition may not answer Step 1's *is publication the purpose?*, and a named operation counts only where the **anonymous remote** caller reaches it, read off the dispatch rather than the prose — [#92](https://github.com/winniel123/verge-asm/issues/92) · [ADR-0054](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0054-a-claim-step-is-answered-only-by-evidence-about-that-step.md). `2379`/`2380` re-founded on etcd's own `THREAT_MODEL.md` sentence §16.3 stopped one line short of; **all eleven Step 2 cells hold** and both of the ticket's suspicions were refuted from the bytes. **[measured]** `6379`'s cell was factually wrong — at `8.10.0` protected mode no longer consults `bindaddr_count`, so the shipped default serves an anonymous remote caller **nothing**. The row survives on the frame, and **seven of eleven Class A rows turn on it**
- **An owner's class list is fixed at the time of the sweep, not for all time** — ADR-0046 limb 2 closes the **taxonomy**, not the **membership**, so a sweep's output is a **dated** negative and [ADR-0045](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0045-an-owners-documentation-is-what-it-has-issued.md)'s per-version issuance rule carries it unamended — [#93](https://github.com/winniel123/verge-asm/issues/93) · [ADR-0046](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0046-a-negatives-corpus-is-its-owners-class-list-and-only-a-sole-ground-negative-is-exposed.md) amendment, [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §26. The positive prose footings are now swept: population **24, not 23**, filters carried 22, two retrievals spent, **no footing moves tier**. A ninth curation trigger **lost on cost** — the equivalent watch is *read every release of every owner forever for a document that did not previously exist*, the obligation ADR-0046 exists to deny. **[measured]** §20.8's *"every prohibition-tier sentence names the public internet"* is **refuted** on `873/tcp` — 13 clean · 1 marginal · 1 failing
- **A `Name`'s key is the label sequence on the wire, and we fold only the equivalence the protocol supplies.** Ordered octet-string labels terminated by the root label; **ASCII case folds** (26 letters, per octet), the **trailing dot is consumed by the parse** and load-bearing only inside a master file, and a **U-label presented as text is refused** — UTS-46 is reference data a key may not consult. **Variance is not ambiguity**: a form is refused where *we* would have to supply the equivalence, folded where the specification already has — [#94](https://github.com/winniel123/verge-asm/issues/94) · [ADR-0055](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0055-a-names-key-is-the-label-sequence-and-we-fold-only-what-the-protocol-folds.md). The key function's **domain is the label sequence**, so the format parse is versionless too, or a decoder bump re-keys a subject. **[measured]** a name on the wire is not text: RFC 1035 §3.1 length-prefixed labels, RFC 2181 §11 arbitrary octet strings — so ADR-0051's limb transfers with a **stronger** warrant, not by analogy
- **A port constant in a library is not a shipped listener — and ADR-0037 limb 1 fires on any owner artefact opened *in the table's service*, whatever the retrieval was for.** §2.2's third form and [ADR-0048](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0048-a-convention-is-evidenced-by-placement-never-by-catalogue.md)'s *established* limb need the party stating the default to **ship software that listens on the pair**; an owner's **example program** is §12(a)'s example file one artefact class over and attests nothing — [#95](https://github.com/winniel123/verge-asm/issues/95) · [ADR-0056](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0056-a-port-constant-in-a-library-is-not-a-shipped-listener.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §27; ADR-0037, [ADR-0009](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0009-verge-core-is-a-union.md) and [ADR-0054](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0054-a-claim-step-is-answered-only-by-evidence-about-that-step.md) amended. **The list is 41 pairs, classes `12 / 7 / 22`** — [ADR-0008](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0008-derivation-versions-move-on-content.md)'s price spent a second time. **[measured]** the trigger widening is forced by the *first* determinacy retrieval paying out: §24 decided nothing on determinacy and its by-catch produced two rows
- **A claim step is answered from the dispatch, and a handshake is not an operation** — `623/udp`'s pre-authentication surface is four commands plus the RAKP exchange, all discovery, so Claim 1 fails at §10.1 Step 2 and the row stays Class C — [#96](https://github.com/winniel123/verge-asm/issues/96) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §28, [ADR-0054](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0054-a-claim-step-is-answered-only-by-evidence-about-that-step.md) annotated. **Mandatory *support* for an unauthenticated mode is not a shipped *configuration*** — the contrary reading moves `5432`, `3306`, `1433` and `2049` into Class A — and a pre-authentication credential leak is a **Claim 2**-shaped fact, which §10.8 had already ruled structurally unavailable where encryption is negotiated in-band. **[measured]** exactly four App commands carry Table G-1's `p`; §10.2's closed claim set met its sharpest candidate fourth and held
- **A superseded mechanism is withdrawn at the site that *specifies* it, not only at the site that *supersedes* it** — a pointer from the superseding document reaches only the reader who already noticed the absence, and the failing reader is the one who believes the mechanism exists and never looks — [#97](https://github.com/winniel123/verge-asm/issues/97) · [ADR-0058](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0058-a-superseded-mechanism-is-withdrawn-at-the-site-that-specifies-it.md). The test: *if the superseded sentence, read alone and in the present tense, would cause a competent session to build the thing, it is not withdrawn.* **[measured]** ADR-0009 dissolved §6's build-time containment test and left §6 standing; [#91](https://github.com/winniel123/verge-asm/issues/91) re-asserted the mechanism **inside ADR-0009's own body**, #97 inherited it one hop further, and the third hop was [#12](https://github.com/winniel123/verge-asm/issues/12). Detectable rather than watched, and **not** a curation trigger. **The rule was violated twice by its own batch** — §27 and ADR-0009's #95 amendment both re-asserted the dissolved invariant, #95 having landed after #97 withdrew #91's copy; both withdrawn in the reconciliation
- **A footing tier grades *evidential distance*, never the owner's conviction** — the count of premises the reader must supply between the owner's own sentence and the proposition the row asserts, so naming **the public internet** is zero and naming a **trust boundary** is one; **mood, force, hedging and priority label are inadmissible in both directions**, and the lexical test is ~~**necessary for the top tier and not sufficient**~~ — **that clause is withdrawn by [#101](https://github.com/winniel123/verge-asm/issues/101), which closed it as sufficient too** — [#98](https://github.com/winniel123/verge-asm/issues/98) · [ADR-0059](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0059-a-footing-tier-grades-evidential-distance-never-the-owners-conviction.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §30, §20.8 amended. §20.8's *"every prohibition-tier sentence names the public internet"* is made **true** by applying it — `873/tcp` demotes and **no row moves**; composed with §27 the tiers are **14 / 13 / 3**, coverage **30 of 41**. **[measured]** the restatement option promotes `4369/tcp` too, reversing §20.8's own ruling: **a criterion that stretches to fit its one counterexample takes its neighbours with it**
- **A wildcard SAN is a pattern over names and admits none of them — it denotes a set rather than a thing, so there is nothing for a subject key to be the key of.** No name of its own, none beneath it, and **not the parent either**: `*.example.com` matches `foo.example.com` and never `example.com` `[spec]` RFC 6125 §6.4.3 rule 2, so admitting the apex would invent a subject the evidence expressly excludes. `*.example.com` is a `Subject` from **no source**, zone file included — [#99](https://github.com/winniel123/verge-asm/issues/99) · [ADR-0060](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0060-a-wildcard-san-is-a-pattern-over-names-and-admits-none-of-them.md). **Matching is not admitting** — `certificate-hostname-san-mismatch` reads a presented wildcard under RFC 6125 and is vindicated, not contradicted. The refusal takes the **protocol's precise test where DNS supplies one** (RFC 4592 §2.1.2 — `the*` and `**` are ordinary labels, and `\042` writes a literal asterisk) and the **blunt one on the certificate path**, where RFC 6125 §6.4.3 leaves a partial wildcard's reading to the client. No leaf, no version, no `Break`
- **A comment takes a position only where it *outlives the value it annotates*; where its content is exhausted by that value it is a label.** Necessary and not sufficient — §2.3's and §4.4's position-versus-preference discrimination runs **second**, on the survivor. A **published config-API doc comment** is §2.2's ordinary artefact reached by the ordinary route and answers the third form's *documented* limb **only**; the defaulting code answers *takes effect* and must be retrieved, and **artefact class never grades a footing tier** ([ADR-0059](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0059-a-footing-tier-grades-evidential-distance-never-the-owners-conviction.md) limb 1: the artefact is not a premise) — [#100](https://github.com/winniel123/verge-asm/issues/100) · [ADR-0061](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0061-a-comment-is-a-position-only-where-it-outlives-the-value-it-annotates.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §31; ADR-0036's thin-ground flag **discharged**. **[measured]** the instrument reproduces **all nine** comment verdicts the note carries, across five projects and three artefact classes, eight at step one — and the two kubelet doc comments separate at **two different steps twelve lines apart in one struct**. `10248/tcp` stays in the weak tier and **nothing moves**
- **§20.8's lexical test is sufficient as well as necessary, on the four-limb conjunction ADR-0059 limb 3 already enumerated — owner's · reaches the pair · names the public internet · takes a position — and the list is closed the way §10.2 closed the claim set.** A footing satisfying all four leaves the reader nothing to supply, and **zero supplied premises is the top tier by limb 1's own definition**, so sufficiency is a theorem of limb 1 rather than a new grading rule. Limb 1's unit widens from *sentence* to **the owner's statement of the port's permitted network**, which brings the non-prose footings inside it and makes §24.12's *"more than a default"* and limb 1's *"one premise"* the same test — [#101](https://github.com/winniel123/verge-asm/issues/101) · [ADR-0059](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0059-a-footing-tier-grades-evidential-distance-never-the-owners-conviction.md) **amended in place, no ADR minted**, [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §32. `2181/tcp` and `25672/tcp` **promote**; tiers **16 / 11 / 3**, coverage 30 of 41, **two cells and no rows**. **[measured]** the criterion takes no neighbour with it: run promoting across all thirteen scoping members it promotes exactly two — `4369` refused on limb 1, RabbitMQ being a non-owner for it, and `2049` on limb 4 — and run demoting it refuses none of fourteen
- **ADR-0058's shape has a population — nine sites over six superseding decisions, and it runs in every direction rather than note-to-ADR alone.** — [#102](https://github.com/winniel123/verge-asm/issues/102) · [ADR-0058](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0058-a-superseded-mechanism-is-withdrawn-at-the-site-that-specifies-it.md) annotated; ADR-0009, ADR-0032, ADR-0034, `sensitive-ports.md` §24, `safe-active-probing.md` §2.5, `insecure-listener-rules.md` §9.1 and `weak-key-and-signature.md` §9 repaired in place. **[measured]** **197 superseding moves over 39,382 lines at `c0881ae`** — the bound holds because **no ADR here carries `Status: Superseded`**, so the amendment surface *is* the whole corpus. **Stays detectable, not a ninth curation trigger** — the map's eight triggers fire when the *world* moves and this fires when *we* do, so it is the wrong list on shape before cost
- **A name is discriminated at its *parent*, and the population of those parents is aperture rather than a declared parameter — because a parameter is authored data and this is a function of the batch's own scope.** The depth rule RFC 4592 forces on any *probe at depth n* procedure **dissolves**: a control label under P shares N's closest encloser, so it finds the wildcard at any depth and **whether or not P exists or is a `Name` we hold** — a probe site is a label sequence we construct, not a subject we cite. Aperture inputs were **never coextensive with offers** (three of the six settled are subject populations), and the widening is **structurally vacuous**, a parent entering the population only in step with the names beneath it. **An undiscriminated answer is never a value** — ADR-0025's truncation rule one term over — [#108](https://github.com/winniel123/verge-asm/issues/108) · [ADR-0066](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0066-a-control-probe-is-generated-under-a-names-parent-and-that-population-is-aperture.md), [`passive-discovery-sources.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/passive-discovery-sources.md) §3.2. **Aperture count 6 → 7.** **[measured]** on `%.iana.org`, **one of six parents is a zone cut**, so the zone-cut index leaves 44% of the estate undiscriminated and 13 of 18 probed cuts carry no wildcard at all
- **A claim fails on the owner's affirmative naming, not on the reach of its own prohibition — and an `applies to` banner scopes the artefact, not the pair.** §10.3's failure condition is answered by the owner **affirming** the public internet as a supported deployment environment; whether the owner's **prohibition** reaches the pair is ADR-0050 limb 3 / ADR-0059 limb 4 at the attestation gate. **A narrowing reading of the prohibition does not rescue the claim** — it removes the tension and leaves the affirmation standing alone. Symmetrically, a naming that defeats a footing removes a row only where **no claim in §10.2's closed set fits**, tested per claim — [#109](https://github.com/winniel123/verge-asm/issues/109) · [ADR-0067](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0067-a-claim-fails-on-the-owners-affirmative-naming-not-on-the-reach-of-its-own-prohibition.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §35. **Limb 2**: a deployment-scoped owner document speaks about the pair wherever the sentence's subject is the protocol's clients and its **addressee is the operator**; where the addressee is the owner's **own managed service**, §33.2's addressee rider governs and the document corroborates without defeating. **[measured]** `1433/tcp` is the one prohibition-tier member the riders do not dispose of, because Microsoft does not run the operator's SQL Server; the row is **removed** and the list is 40. Detectable rather than watched — a row's claim and the owner's affirmative sentences are both already written down. **Thin ground, flagged**: limb 1 is a separation no prior section states, and limb 2 rests on **one** instance whose partition is **silence** rather than a disclaimer, which is ADR-0040's failure mode with the subject changed
- **A limb of a closed enumeration is repaired by writing down what it *denotes*, never by widening the enumeration — and a closure over *kinds* survives a live counterexample only if the limb it hit is restated at its own clause.** Direction is what the prohibition-tier conjunction's limb 2 denotes (*this pair as the endpoint being reached, on the estate the statement addresses*), addressee is that limb's second coordinate, and the conjunction stays at **four** — [#110](https://github.com/winniel123/verge-asm/issues/110) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §36, [ADR-0059](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0059-a-footing-tier-grades-evidential-distance-never-the-owners-conviction.md) amended in place. The test: **a candidate is a fifth *kind* where it introduces an object the proposition lacks, and a qualifier *inside* a limb where it fixes which already-named object fills which place.** **[measured]** the owner supplies the direction in **24 of 26** graded-tier carrying statements — two owners write it in a column headed `Direction` — so the reader supplies one **nowhere**; *"closed over the terms the note has rules for"* loses for being unfalsifiable
- **A wildcard is discriminated only where its synthesis is *determinate*, and determinacy is measured per `(qtype asked, RR type answered)` component.** Per component the signature is `NoSynthesis` │ `Determinate(RRset)` │ `Indeterminate`; a candidate is discriminated iff it differs at some **determinate** component and is `Shadowed` otherwise, an indeterminate component is **never consulted**, and where no component is determinate every name beneath that parent is `Shadowed` — §3.2 step 5's escape hatch withdrawn there. The determinacy verdict is neither a declared parameter nor an aperture input but a **measured in-batch fact**, so the aperture count stays at **seven** and `resolution`'s value space does not move — [#111](https://github.com/winniel123/verge-asm/issues/111) · [ADR-0068](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0068-a-wildcard-is-discriminated-only-where-its-synthesis-is-determinate.md), [`passive-discovery-sources.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/passive-discovery-sources.md) §11. **[measured]** the synthesised answer is **not a function of the label at all** — four repeats of *one* label direct to `ns01.herokudns.net` returned four disjoint address sets — so *the* answer set was never an object; and **intersection-with-the-union**, the ticket's own leading candidate, is *literally the same predicate* as set equality where it is needed (`herokuapp.com`, **7 of 25** both ways) while catching **25 of 25** on `vercel.com`, its correctness being a property of the provider's load balancer that the probe cannot observe
- **A rider on §33.2's discriminator travels to the claim gate only if it bounds the *affirmation* — conditioning of the *prohibition* stops at the attestation gate.** Addressee and direction travel; a bound on the affirmation travels; **conditioning does not**, because narrowing a prohibition leaves the owner's affirmative naming standing alone, which is [ADR-0067](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0067-a-claim-fails-on-the-owners-affirmative-naming-not-on-the-reach-of-its-own-prohibition.md) limb 1 read backwards. §33.2's addressee rider and ADR-0067 limb 2 are **two tests sharing one coordinate**, not one instrument stated twice — folding them converts silence about banners into a boundary, ADR-0040's failure mode — [#112](https://github.com/winniel123/verge-asm/issues/112) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §37, ADR-0067 amended in place at limbs 1 and 2, **ADR-0069 unused**. **[measured]** the sweep's population is **32**, not the 26 graded-tier members — limb 2's row-moving power runs through §10.3, whose population is **Class C**, so six rows outside both tiers (`5432`, `5984`, `10248`, `139`, `137`, `138`) are in it. **§33 spared `9200`/`9300` on the rider that does not travel and `3306` on the rider that does**, which is the whole result
- **A control label is one label, and a control-label set that cannot falsify label-independence is not a measurement of it.** The set is ~~5 random + 1 structured~~ **9 random + 1 structured** (`<a>-<b>-<c>-<d>` over a random RFC 5737 address) — **count raised by [#115](https://github.com/winniel123/verge-asm/issues/115)** — each **exactly one label**, each over the declared qtype set. **One label is [ADR-0066](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0066-a-control-probe-is-generated-under-a-names-parent-and-that-population-is-aperture.md)'s own warrant restated** — the probe is admitted because a control label falls off the tree where the candidates do, and a multi-label label does not — [#113](https://github.com/winniel123/verge-asm/issues/113) · [ADR-0069](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0069-a-control-label-is-one-label-and-the-set-must-falsify-label-independence.md), [`passive-discovery-sources.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/passive-discovery-sources.md) §12; ADR-0066's licence clause qualified, [ADR-0068](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0068-a-wildcard-is-discriminated-only-where-its-synthesis-is-determinate.md)'s **two** residues discharged **without amending it** — its `Indeterminate` limb already reached both. **[measured]** the dotted quad manufactures a wildcard on **3 of 5** un-wildcarded zones, because `1.pages.dev` is a *third party's* project carrying its own wildcard two labels below the probe site; and the structured label costs **0 false `Indeterminate` in 22 zones**. `wildcard-discrimination` now has **no parameter without a value**- **A claim is answered from the corpus that owns the deployment, and two rows can fall on different evidence.** `9200/tcp` and `9300/tcp` Elasticsearch are **both removed** on §10.3's failure condition, tested per pair on a **row-scoped** retrieval across all four Elastic corpora — `9200` on the **self-managed** corpus [#112](https://github.com/winniel123/verge-asm/issues/112) never opened (*"**The HTTP layer**: Used for communication between your cluster or deployment **and the internet**"*, the interface `http.port` binds), `9300` on **ECE alone** via [ADR-0050](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0050-an-owners-category-statement-reaches-the-members-its-own-artefacts-place-inside-it.md) limb 2. **A cell cannot outlive its row**, so the footing cells leave with them — [#114](https://github.com/winniel123/verge-asm/issues/114) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §38, [ADR-0067](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0067-a-claim-fails-on-the-owners-affirmative-naming-not-on-the-reach-of-its-own-prohibition.md) **applied and amended in place, no ADR minted**. **[measured]** Claim 1 and Claim 2 tested rather than assumed at `v9.5.1`; **`9200` is an affirmation rather than a default, so §37.13's counter never has to be answered**. **THE CURRENT COMPOSED STATE IS `38 pairs · classes 12 / 7 / 19 · tiers 13 / 11 / 3 · coverage 27 of 38 · verge-core 136 · aperture inputs 7`** — every earlier figure on this map is a dated record, not a live absolute- **A wildcard authority's answer varies per-query or per-label, never per-time — so control-label draws *are* independent samples and the count buys exactly what the arithmetic says.** `herokuapp.com` and `vercel.com` are **per-query** (one label, eight repeats at the authority → 4 and 6 distinct answers); `appspot.com` and `surge.sh` are **per-label** (repeats identical, distinct labels differ); **[measured]** re-probing at +5/+10/+20/+35 min leaves the per-label maps **byte-identical over 36 minutes**. `Determinate` therefore needs **no shelf life** — ADR-0011 already confines it to the batch, and #115 makes that confinement **load-bearing** rather than incidental — [#115](https://github.com/winniel123/verge-asm/issues/115) · [`passive-discovery-sources.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/passive-discovery-sources.md) §13, [ADR-0069](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0069-a-control-label-is-one-label-and-the-set-must-falsify-label-independence.md) **amended in place, ADR-0071 unused**. **Count 5 → 9 random**, cost 42 → **70 queries per site**

## Decisions so far

<!-- index: one line per closed ticket, gist + link. Detail lives in the ticket, never here; where a
     rule above already states it, this line only names the decision. Compacted 2026-08-13 and again
     on 2026-08-14 — the map was compacted three times that day. The third pass re-cut the entries
     written since the second, whose median had drifted from 557 characters to about 1,400; entries
     already at gist length were left alone. The body before each pass is at
     docs/wayfinder/map-archive-2026-08-14{,b,c}.md. If an entry here disagrees with
     its ticket, the ticket wins. -->

- [Prior art: what existing OSS ASM platforms already do, and where the gap is](https://github.com/winniel123/verge-asm/issues/2) — **no technically novel gap exists**; the defensible claim is drift as a first-class queryable object across ports, certs, DNS and HTTP, not subdomains alone. `docs/research/prior-art-oss-asm.md`
- [Free passive discovery: what CT logs, DNS, and RDAP yield without API keys](https://github.com/winniel123/verge-asm/issues/3) — Tier 0 keyless (own resolver, mandatory RFC 4592 wildcard detection, zone file or AXFR, IANA→RIR RDAP); Tier 1 CT direct, crt.sh throttled, Common Crawl. **The measurements stand; the tiering does not** — superseded by #15, RIPE recipe corrected by #20. `docs/research/passive-discovery-sources.md`
- [Safe active probing profile for scanning your own production](https://github.com/winniel123/verge-asm/issues/4) — TCP connect, non-root, no added capabilities; tiered port sets because `nmap-services` frequency data is from **2008**; limits **per target host**; `GET /`, redirects unfollowed. §7.3 damping overruled by #35, §2.5 UDP list by #29, timeout versioned per #49. `docs/research/safe-active-probing.md`
- [Auth and access model for a self-hosted deployment](https://github.com/winniel123/verge-asm/issues/11) — local accounts only, admin/viewer roles, first admin bootstrapped by a single-use setup token in container logs, optional TOTP, a permission check on every mutating endpoint from the first commit.
- [External vantage: does v1 ship a second prober outside the network?](https://github.com/winniel123/verge-asm/issues/14) — **yes, optional, as an agentless binary pushed over SSH**; `network_position` rejected as relative; **exposure is derived, never observed**. Five-state enum superseded by [ADR-0010](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0010-exposure-composes-two-reaches.md) and [ADR-0017](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0017-exposure-needs-both-legs.md).
- [Trial reNgine: is it already good enough that verge-asm shouldn't be built?](https://github.com/winniel123/verge-asm/issues/13) — **verdict: build**, on code reconnaissance rather than the trial asked for, so the usability half is untested: `release/3.0.0` models ports, IPs, DNS and TLS yet every diff is still subdomain-only. Contributing rejected on GPL-3.0 against AGPL-3.0.
- [Build, vendor, or shell out for discovery and probing?](https://github.com/winniel123/verge-asm/issues/5) — **build both, as one measurement binary in Go** exec'd locally and pushed over SSH; seam is the **vantage boundary**, contract job spec in / NDJSON out. subfinder's output cannot express a failed source. **No fingerprinting** — only observed values enter drift.
- [Stack and runtime for web, worker, and persistence](https://github.com/winniel123/verge-asm/issues/6) — **Postgres · Postgres-backed queue via `SKIP LOCKED` + `LISTEN/NOTIFY` · Go throughout · `html/template` + htmx + SSE · one image, two compose services · no JSON API in v1.** [ADR-0001](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0001-stack-and-runtime.md). The queue is Postgres because job outcome and observations **must commit together**.
- [Core domain model and ubiquitous language](https://github.com/winniel123/verge-asm/issues/7) — three layers, only the middle drifting; **`Asset`, `Finding`, `ScanRun`, `Host` rejected**; **one `Observation` across five facets**; `Source` carries `authority` *and* `completeness`. [`CONTEXT.md`](https://github.com/winniel123/verge-asm/blob/main/CONTEXT.md); probing gate in [ADR-0002](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0002-ownership-gates-probing.md); superseded in part by [ADR-0011](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0011-a-facet-is-six-parts.md) and [ADR-0007](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0007-drift-is-a-timeline-of-spans.md).
- [Core UI screens and the landing view](https://github.com/winniel123/verge-asm/issues/10) — **the landing view is the exposure board**: rows what a `Service` was, columns what it is now. Nav **Exposure · Subjects · Signals · Seeds · Settings**, plus `Coverage` from #22. **IA decisions live in the ticket, not an ADR.** [`prototypes/landing-view/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/landing-view)
- [Which third-party discovery sources may ship enabled by default, given their ToS?](https://github.com/winniel123/verge-asm/issues/15) — the two-limb modal-operator bar. [ADR-0003](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0003-third-party-source-consent-bar.md). It blocks **two sources, not seven**; absence of terms clears it; `Consent` joins `authority` and `completeness`. Consent record withdrawn by #34, *depth-not-capability* falsified by #20, aperture claim withdrawn by #47.
- [Risk signals: which ones does v1 emit from cert and protocol facts alone?](https://github.com/winniel123/verge-asm/issues/16) — the release-cadence test for a `Signal`. [ADR-0004](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0004-signals-are-release-coupled-rules.md). Ten rules ship; **no severity**; absent evidence is `not-evaluable` (#44 renders it); versions are per rule. Its v1 list is the test's output, not a second bar (#35); its TLS-version rule re-founded by #36.
- [Scan scheduling and job execution model](https://github.com/winniel123/verge-asm/issues/9) — **one queue job is one `Batch`.** [ADR-0005](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0005-scan-execution-model.md). `ScanRun` returns as **`Dispatch`**, barred from the comparison path; three `Scan`s for the port tiers; no catch-up, no jitter; vantage `Availability` is Derived. Opens #22.
- [Keyless org-name to prefix coverage for RIPE, APNIC, LACNIC and AFRINIC](https://github.com/winniel123/verge-asm/issues/20) — **the ARIN-only gap half-closes with nobody's permission**: CAIDA ⋈ delegated-stats gives keyless org→prefix for **AFRINIC** (100%) and **APNIC** (84.36%); **RIPE and LACNIC do not close at all**. RouteViews replaces RIPEstat's `announced-prefixes` under CC BY 4.0. `docs/research/non-arin-prefix-coverage.md`
- [Where does the operational record live, in an IA with no operations screen?](https://github.com/winniel123/verge-asm/issues/22) — **a sixth destination, `Coverage`, which is not an operations screen**; the reframe from *jobs* to *coverage* is the decision. Coverage is per `Seed` scope per vantage, never per batch; **no alerting on an individual batch failure** — only a vantage going `unavailable` and a coverage threshold, a **third notification class**. Two-axis cut corrected to *role* by #28.
- [Which ports are never legitimately internet-facing, and on what evidence?](https://github.com/winniel123/verge-asm/issues/21) — **38 `(port, transport)` pairs in three claim-classes** (37 after #66). **The headline is what is absent: no remote-administration ports at all.** The list publishes its own weak tier. `docs/research/sensitive-ports.md`. Opens #29, #30, #31, #32, #33.
- [Ask RIPE NCC whether verge-asm may ship RIPEstat enabled by default](https://github.com/winniel123/verge-asm/issues/19) — **closed as *asked, no answer*: RIPEstat and the RIPE Database ship off under `operator-accepted`, indefinitely**, which **falsifies ADR-0003's *depth, not capability* for this region** — a RIPE operator gets no org→prefix coverage at all by default. Whether anything was sent is #57.
- [Ask APNIC whether verge-asm may query its registry by default](https://github.com/winniel123/verge-asm/issues/23) — **closed as *asked, no answer*: the live registry path ships off, the `unencumbered` 84.36% path untouched.** A retrieval-system clause is a **limb 1** question and `operator-accepted` a **limb 2** remedy, so there is nothing there to accept. Raised as #34; bulk-file half settled by #46.
- [Ask AFRINIC whether verge-asm may query its registry by default](https://github.com/winniel123/verge-asm/issues/24) — **closed as *asked, no answer*. The cleanest loss of the four**: 100% opaque-id coverage exists keylessly, so what ships off is the best org→prefix endpoint of any RIR rather than the region. Folded into #34; its commercial-service clause resurfaces in #47.
- [Ask LACNIC for a retrievable statement of terms](https://github.com/winniel123/verge-asm/issues/25) — **closed as *asked, no answer*: ships off, terms still unretrievable**, five documented URLs returning one byte-identical JavaScript shell. **Unreadable is not absent**, or a source earns `unencumbered` by breaking its own link. Raised as #34.
- [Subject lifecycle: when may something leave the inventory with no enumerable source to say it is gone?](https://github.com/winniel123/verge-asm/issues/17) — subjects leave by measurement. [ADR-0006](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0006-subjects-leave-by-measurement.md). The premise was false: our own resolver is `enumerable` over a single `Name`, so **the batch must record the scope its silence covers**. `Address` has no lifecycle; `Seed` gains **exclusions**; **`Shadowed`** enters; only `returned` alerts. Opens #35.
- [Drift model: how change over time is represented and surfaced](https://github.com/winniel123/verge-asm/issues/8) — drift is a timeline of `Span`s. [ADR-0007](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0007-drift-is-a-timeline-of-spans.md). The stored object is the **period**, a `Transition` adjacency derived on read, so **duration is queryable**; **one timeline per source**, conflicts reported not resolved; not-comparable is `Gap`/`Break`/`Shadowed`; **no hysteresis**; currency is `k` cadences. Opens #36; re-scopes #18.
- [How does a Derived value's effective version avoid moving every release?](https://github.com/winniel123/verge-asm/issues/18) — a version moves on content; a `Break` clamps. [ADR-0008](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0008-derivation-versions-move-on-content.md). Bump gated by a golden corpus in CI; **the version is a vector**; **the break is uniform**, since a narrow predicate fails silently; truncated figures render as **labelled floors**.
- [Second attestation pass for rpcbind 111, LDAP 389 and finger 79](https://github.com/winniel123/verge-asm/issues/30) — **all three stay out and the list stays at 38 pairs**: 111 fails on its own shipped defaults, 389 because **RFC 4513 does not deprecate `ldaps://636` — it does not mention them**, 79 on determinacy. **The load-bearing result is the instrument** — Claim 1 read literally admits every public service, opened as #37, blocking #33. Unblocks #29.
- [Is the modal operator ASN-less, and does that hollow out the keyless prefix path?](https://github.com/winniel123/verge-asm/issues/26) — **they are *registry-less*, which makes the ASN the wrong axis entirely**: **128,233** organisations hold any RIR delegation against 6.4M US firms and 32.3M EU27 enterprises. Org→prefix is a **convenience, not a capability**. Largest unmeasured hole: self-selection. Opens #38, #39, #40.
- [What can an observation-driven insecure-listener rule establish?](https://github.com/winniel123/verge-asm/issues/31) — *offered no TLS* is an **input, not a signal**; what saves the narrower rule is the aperture-versus-signature-database test (187 `Probe` directives against 12,171 `match` lines), a rule staying legal only where every fact is read from a **spec-defined field**. **The deflating result is most of the value** — no new firings across the 38 rows, only refinement. Opens #41, #42.
- [Bundle the CAIDA and delegated-stats tables in the image, or fetch them live?](https://github.com/winniel123/verge-asm/issues/27) — **neither: CAIDA may not be bundled at all** (the Public-AUA is non-transferable — the redistribution rule), **and the table that matters is not CAIDA** — the delegated-stats opaque-id is on every allocated row in all five RIRs. **The third result amends ADR-0002**: one AWS-resident seed proposes **76,046,336** addresses, so registry expansion becomes an operator-confirmed **proposal**.
- [Prototype the Coverage screen and the board's coverage statement](https://github.com/winniel123/verge-asm/issues/28) — **variant D: scope-first, split by the verb *inside* it, statement above the board at three densities.** [`prototypes/coverage/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/coverage). Three corrections carry the value: the second axis is two axes and the cut that holds is **role**, with **the propose half carrying no percentage and no bar, ever**; and **a census survives a `Break`, not a precondition failure**.
- [Hot-set transport defects and the sensitive-subset-of-hot invariant](https://github.com/winniel123/verge-asm/issues/29) — **`verge-core` stops being a list and becomes a union — `frequency-set ∪ sensitive-list`** — turning a build-time invariant into a definition that cannot fail. [ADR-0009](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0009-verge-core-is-a-union.md). The transport conflation is in #4's **evidence**, not only its list; the pre-release exemption is **refused as vacuous rather than waived**; **a port the operator can hide is a signal they can silence**, so they edit the frequency half alone. Its `not-evaluable`-for-free claim is **wrong**, repaired by #44.
- [Does sensitive-port-exposed fire on edge-only as well as exposed?](https://github.com/winniel123/verge-asm/issues/32) — **yes**, and a rule reads a leg. [ADR-0010](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0010-exposure-composes-two-reaches.md). #14's five states are **not a partition**, so `state == exposed` is a silent bug. The signal becomes **`sensitive-port-reached-from-internet`**, the only v1 signal reading `Exposure`; **the flagship is a column move**; `fired` → `not-evaluable` notifies in the coverage class.
- [Are provider-aggregatable assignments name-searchable outside ARIN?](https://github.com/winniel123/verge-asm/issues/38) — **the SWIP correction generalises, but it splits across two layers**: policy generalises completely, while **name search splits 3–2** — ARIN, RIPE and LACNIC reach renters, APNIC and AFRINIC by no interface at any tier. **Default set unchanged**: ARIN is uniquely *keyless*, not uniquely *capable*. `descr:` carries a genuine End User on **89.48%** of RIPE PA against `org:`'s 4.24%. §13 of `docs/research/asn-less-operators.md`
- [Can `operator-accepted` carry a limb-1 ambiguity or unreadable terms?](https://github.com/winniel123/verge-asm/issues/34) — **yes to both, once limb 1 governs the *default* rather than the capability**; the axis is whether the residual unknown **varies by operator or is constant across every install**. `operator-accepted` is **redefined**: taking on a reading the project declined to make. #15's consent record is **withdrawn** — store the retrieved document or the retrieval failure, never a bare URL. ADR-0003's **second amendment**.
- [Does dangling-delegation join the v1 signal set, on evidence that is a failure?](https://github.com/winniel123/verge-asm/issues/35) — **yes, as `lame-delegation` and `cname-target-name-error`**; ADR-0004 had already answered it. Attribution is bought **structurally** by querying the delegated authorities directly; `resolution` gains **`Lame`**; **no threshold in either**, overruling #4 §7.3; **the first signals to alert on *clearing***. Unblocks #48.
- [Canonical form per facet: when are two observations of one facet equal?](https://github.com/winniel123/verge-asm/issues/36) — a facet is six parts, every canonical value a closed union. [ADR-0011](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0011-a-facet-is-six-parts.md). A decoder per `(facet, source)`, a canonicaliser per facet; `dns-record` is **one timeline per qtype**; TTL excluded, body hash refused. #16's TLS-version signal was reading **our own ClientHello** and becomes `tls-1.0-accepted` on a sixth facet.
- [Is the ARIN SWIP customer path a Source in its own right, and may it gate probing?](https://github.com/winniel123/verge-asm/issues/39) — **it is not a `Source` at all, it does not gate probing, and it ships.** [ADR-0012](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0012-a-proposer-is-not-a-source.md). All three `Source` properties presuppose observations, so *what is SWIP's `authority`?* is **malformed, not hard**. Yields a **`Proposal`**; declining is a `Seed` exclusion, extending exclusions to **address scopes**.
- [What is the right Seed primitive for a cloud-resident estate?](https://github.com/winniel123/verge-asm/issues/40) — **a `Seed` asserts control, not title; `Ownership` becomes `Custody`; `unknown` is deleted; a name-scope `Seed` may carry an off-by-default `custody extension`.** [ADR-0013](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0013-custody-is-control-and-extends-by-declaration.md). Transitivity stops **where the resolution chain leaves the declared zone**; the apex-flattening hole rides on the declaration's **wording** rather than a check. Refined by #42; words written by #51.
- [Does revealed cover a value appearing where a Gap was?](https://github.com/winniel123/verge-asm/issues/42) — **`revealed` widens to any timeline; `Gap` → value is a different event.** [ADR-0014](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0014-only-revealed-generalises.md). **Check whether the timeline existed before reaching for a name.** No fourth member — the `Gap` records its cause; creating timelines writes no `Break`; values either side of a `Gap` may be shown, **labelled undatable**.
- [Does the observation-driven listener rule ship in v1, and at what granularity?](https://github.com/winniel123/verge-asm/issues/41) — **v1 ships one new signal, `unauthenticated-request-answered`, over the existing `http-identity` facet; the wire-protocol prober does not.** [ADR-0015](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0015-the-value-space-is-the-commitment.md). **The land-grab argument is dead map-wide** except for widening the value space of a facet that already has observations — so the question with a deadline is *does this facet record this field*.
- [Does the CAIDA org-name lookup ship in v1 at all?](https://github.com/winniel123/verge-asm/issues/43) — **yes, and the stakes were overstated in both directions**: the box ships regardless, CAIDA only extending it from one region to three. Decided on **sibling expansion not crossing an opaque-id boundary**, and **subsidiaries are exactly where forgotten exposure lives**. **A proposer may be added or dropped in any release with no drift consequence whatever.** Ruled on thin ground.
- [Where does the operator see a signal that is not-evaluable?](https://github.com/winniel123/verge-asm/issues/44) — **`Signals`, as the third member of each rule's census — but the word covered two populations and only one can ever have a row**: a per-subject `Gap` row where evidence is absent, no row at all where no `Service` was ever observed, the honesty landing on a **standing aperture statement on `Coverage`** counting our own rules and lists. #29's *obtained for free* is **wrong**, and #21 §6.1's UDP justification had been unbacked. Denominator is the rule's **predicate domain over subjects in the estate** (#53). [`prototypes/signal-evaluability/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/signal-evaluability)
- [Which of the four unnamed Exposure cells need names, and what does the board render for the rest?](https://github.com/winniel123/verge-asm/issues/45) — **none of them, and a fifth name is withdrawn.** [ADR-0017](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0017-exposure-needs-both-legs.md). The projection is the **2×2** — `exposed` · `edge-only` · `firewalled` · `unreachable` — **`internal-only` withdrawn**; a one-legged install renders the surviving leg's **`Reach`**, splitting #28's third density. The board **groups both axes on the internet leg**, making the flagship a contiguous block counting **6** where the hero cell read **3**. [`prototypes/exposure-cells/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/exposure-cells); opens #58
- [May verge-asm index APNIC's bulk file, given its header bars storage in a retrieval system?](https://github.com/winniel123/verge-asm/issues/46) — **the header is clear, and clear in the *permitting* direction — so no email was sent, and the capability is deferred on a loophole rather than a licence.** [ADR-0018](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0018-a-clear-conditional-is-not-an-ambiguity.md), ADR-0003's third amendment. The tier **would be `operator-credentialed`** but **does not ship in v1** — `ftp.apnic.net` gates nothing, so the toggle would assert an agreement the software never sees. Opens #52
- [What does the source-enablement prompt render, now that it carries two groups of unresolved questions?](https://github.com/winniel123/verge-asm/issues/47) — **its own surface opened from `Coverage`, both groups rendering even when empty, acceptance showing a receipt and never the document, and it stays one control.** [`prototypes/source-enablement/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/source-enablement). **Four renderings, not two.** Adds a fourth block — *what enabling buys*, a capability and never a figure. Opens #57
- [Does zone-declared-name-does-not-resolve join the signal set, and what else does per-source keying expose?](https://github.com/winniel123/verge-asm/issues/48) — **two rules ship, and the *enumerate the rest* half returns a negative result worth more than the list it was asked for.** [ADR-0020](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0020-a-conflict-needs-two-enumerable-sources.md). Both read the *composed* `resolution`; `NameError` alone fires. In v1 exactly one `enumerable` pair exists, so all three named candidates are something else — two vantages **compose**, a `Seed` observes nothing, CT is `corroborative` with no subject to key on (#56).
- [Is the measurement binary a versioned Derivation, and what is its golden corpus?](https://github.com/winniel123/verge-asm/issues/49) — **the binary is not a `Derivation`; five named decisions inside it are, and the corpus is authored peer scripts rather than captured wire.** [ADR-0021](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0021-a-version-leaf-is-a-decision-not-a-binary.md). #5's seam rule forbids two *implementations*, not two leaves. The five: `connect-outcome` · `tls-handshake` · `http-exchange` · `resolution-walk` · `wildcard-discrimination`. **The unsurvivable fallback is structurally unreachable.** Opens #54, **blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**
- [What does the operator see when confirming or declining a Proposal?](https://github.com/winniel123/verge-asm/issues/50) — **the surface is `Seeds`.** [ADR-0022](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0022-confirmation-is-singular.md); [`prototypes/proposal-confirm/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/proposal-confirm). **Bulk confirm refused, bulk decline shipped** — because *declined* and *never answered* have the identical effect on the gate, while a bulk confirm batches #27's 76,046,336 addresses. Three riders: **no proposer runs on a timer**, **a re-offered record is a new `Proposal`**, and #43's obligation renders as a *what answered* panel.
- [What does the operator type on first run, and what does the custody extension declaration say?](https://github.com/winniel123/verge-asm/issues/51) — **the line is *declare your domain*, and the `custody extension` is a decision inside step 1 rather than a fifth checklist step** — a counter reading `4 of 5` forever is the nag by construction. **The declaration's words are the deliverable**, naming shared hosting, a CDN, a load balancer the operator does not run, and the flattened `ALIAS` at the apex — the last with **no disclosure over it**. [`prototypes/first-run-custody/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/first-run-custody); opens #55, **blocking #12**
- [Can an operator's own act move a source's `consent`, or is it fixed per instrument?](https://github.com/winniel123/verge-asm/issues/52) — **fixed** — the act *satisfies* the value, never moves it. [ADR-0023](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0023-consent-names-the-door.md). **Of the three values only `operator-credentialed` asserts a third party's conduct**, so it is honest only where the instrument **enforces** the grant, and it has **no v1 instance at all**.
- [What is a Signal's predicate domain, and is it a seventh thing a rule must declare?](https://github.com/winniel123/verge-asm/issues/53) — not a seventh thing: a rule is **four parts**. [ADR-0024](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0024-a-rules-domain-is-the-extension-of-its-name.md). Three registers replace #44's two, **a census may never be rendered as a delta or a series**, and `plaintext-http-no-https` loses its `80/tcp` literal. Opens #60
- [Is everything our client offers an aperture input, or only the TLS candidate set?](https://github.com/winniel123/verge-asm/issues/54) — **only the TLS candidate set**; the other three are declared parameters, and **a library default may not stand in for a declaration in either home**. [ADR-0025](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0025-an-offer-is-scope-only-where-the-value-enumerates-it.md). **`crypto/tls` has defaulted to `MinVersion: TLS 1.2` since Go 1.18**, so a default left in place records a TLS-1.0-only listener as `NoTLS`. `NotHTTP` → `NoHTTPResponse`; the aperture count has been **six** since ADR-0017. Opens #62
- [Is an address entering a live custody extension worth a message, and which class?](https://github.com/winniel123/verge-asm/issues/55) — **yes, and it needs nothing new: a ninth member of the coverage class.** Amendment to ADR-0013 §7 — one message per extending scope per cadence, at the cause, carrying no verdict. Every narrowing predicate lost, and §7 is now *carried on an unmeasured assumption* rather than closed.
- [Which subject does a certificate-transparency observation key on, and is CT a certificate source at all?](https://github.com/winniel123/verge-asm/issues/56) — **neither: CT observes no facet, so there is no CT observation to key — and `certificate` keys on `Endpoint`.** [ADR-0027](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0027-a-source-may-admit-without-observing.md). `crt.sh`'s JSON carries **no fingerprint at all**, so ADR-0011's decoder has an **empty domain**. `crt.sh` stays a `Source` because **`authority` governs admission and admission is not a facet**. Opens #61
- [Is there anything behind "asked, nobody replied"?](https://github.com/winniel123/verge-asm/issues/57) — **nothing is behind it, and *we did not ask* is refused too**: whether anything was sent is a `Gap`, and `operator-accepted` survives untouched because it rests on the refusal to read, never on a letter. ADR-0003's fourth amendment. `Coverage`'s *"Asked 2026-06-02"* was **fabricated**. Outbound acts now have an artefact under [`docs/correspondence/`](https://github.com/winniel123/verge-asm/blob/main/docs/correspondence/README.md). Opens [#59](https://github.com/winniel123/verge-asm/issues/59), which is the dev's
- [On a one-vantage-class install there is no Exposure — which bare Reach transitions wake someone?](https://github.com/winniel123/verge-asm/issues/58) — the internet leg's `not-reached` → `reached` is the flagship message on any install, the internal leg recorded and never alerted in either direction, and the first vantage of a class **opens** `Exposure` rather than breaking it. [ADR-0029](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0029-an-alert-fires-on-a-leg.md). The ticket's forcing fact was false — **nine of the ten v1 signals fire on that install**. Opens #63
- [Is `certificate-expiring`'s operator-configurable `N` a declared parameter, and does an operator's edit `Break` the rule?](https://github.com/winniel123/verge-asm/issues/60) — **yes it is, and that is precisely why it may not be a dial**: `N` ships fixed at 30 days (later moved by #67), since an operator's edit would move a version **without a release, without a corpus row moving, and without knowing it did**. Also corrects ADR-0004: the **clock class has three members**, not one.
- [On which scan tier is the TLS handshake that feeds `certificate` performed?](https://github.com/winniel123/verge-asm/issues/61) — **none of them: the handshake rides the `reachability` exchange**, so `certificate` inherits the `Service`'s tier (daily modally), and `tls-acceptance`'s *weekly* is a cadence rather than a tier, making it a **fourth `Scan`**. [ADR-0028](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0028-a-facets-cadence-is-the-cadence-of-its-exchange.md). The clock class's staleness window narrows to **two days modally**.
- [What does the measurement binary actually offer, now that a default may not stand in for a declaration?](https://github.com/winniel123/verge-asm/issues/62) — all five enumerated in [`docs/spec/measurement-offers.md`](https://github.com/winniel123/verge-asm/blob/main/docs/spec/measurement-offers.md) under a **third** evidence standard ([ADR-0030](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0030-an-offer-is-admitted-on-a-finding-or-on-a-falsity-it-prevents.md)), where **the expensive list gets the *lower* bar**. The map's *six qtypes* was uncited; the answer is **seven**.
- [Is `appeared` alertable, and what is the cause when an Address brings tens of Services with it?](https://github.com/winniel123/verge-asm/issues/63) — a membership message fires once at the root of the entering sub-tree, on a `Name` or an `Address` alone; a first run *reveals* rather than appears. [ADR-0031](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0031-membership-alerts-at-the-root-of-the-entering-subtree.md). The kind restriction is a **theorem** from who supplies the key, so a `Service` `appeared` is never a message. Opens #64
- [Which facet `Transition`s besides the internet `Reach` leg are messages?](https://github.com/winniel123/verge-asm/issues/64) — `dns-record`, `certificate`, `http-identity` and `tls-acceptance` are silent in every direction; the one new message is a `resolution` move that opens an `Endpoint` no membership message covers. [ADR-0026](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0026-the-facet-layer-is-evidence-not-a-channel.md). The cut is **per predicate, not per facet**. **The v1 rule set is sixteen, not ten.** Opens #65
- [Repair the claim/attestation standard: an unstated qualifier, and are the three claims closed?](https://github.com/winniel123/verge-asm/issues/37) — Claim 1 gets a **two-step qualifier read off the spec** (publication test first, then authority — the order load-bearing); the set is **closed by construction**; a shipped default attests **only where it restricts**; **owner keys on the artefact**. **All 38 pairs survive, walked not asserted** — no `Break`, no version bump. `161/udp` disclosed as the second-weakest row, open as #66. Unblocks #33, whose ADR this deliberately is not.
- [Does the claim/attestation/determinacy standard generalise to the other nine signals?](https://github.com/winniel123/verge-asm/issues/33) — **no: it attaches to a curated *table*, and thirteen of the sixteen rules do not have one.** [ADR-0032](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0032-an-evidence-standard-attaches-to-a-table-not-to-a-rule.md). Only the **middle gate** generalises. **Three instruments, not two, partitioned on what a table does** — ADR-0030 what we **ask**, #31 what we **conclude**, #21 what we **assert** — the unit being the **artefact**. **The curated count is three, not two.** **Silent de-attestation** is named, so **the weak tier is the curator's watch list**.
- [Does a rule opening at `fired` need a carrier, and can one be built without a three-way case analysis?](https://github.com/winniel123/verge-asm/issues/65) — **yes, and it takes one term rather than three cases.** [ADR-0033](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0033-a-move-carries-the-rule-that-opens-at-fired.md). The **screen** answer is ruled out on ADR-0024's own fourth binding. The case analysis is **dissolved** — no `Transition` crosses a `Gap` or a `Break` and an opening emits none. **A schedule stays silent**; **nothing minted**.
- [Is `161/udp`'s Claim 3 boundary attested by an owner, or does the row come off?](https://github.com/winniel123/verge-asm/issues/66) — **it is not, and the row comes off: the list is 37 pairs, Class C is 18.** #37's own criterion decided it — a verdict changes on **retrieval**, and the retrieval was performed across the SNMP standards family and net-snmp. **It came back pointing the other way**, #30's shape a third time; the strongest option lost because **an example is not a default, a distributor is not the owner, and loopback-only SNMP describes no deployment anybody runs**. §11 of `docs/research/sensitive-ports.md`. Opens #69, **blocking #12**
- [Is `certificate-expiring`'s 30-day horizon attested by an owner, or does the number move?](https://github.com/winniel123/verge-asm/issues/67) — **the number moves: `N` is ⅓ of the certificate's validity period, and ½ of it below a 10-day validity.** [ADR-0034](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0034-derive-the-claim-before-looking-for-the-owner.md) · `docs/research/acme-renewal-timing.md`. **Two owners, one half each** — the IETF on *form*, Let's Encrypt on *value* — and **the owner appeared the moment the claim was derived**, a defect in ADR-0032 §3 rather than a discovery about the world. No fixed number could have worked: six-day certificates are GA, so the predicate stops partitioning. Opens [#71](https://github.com/winniel123/verge-asm/issues/71), [#72](https://github.com/winniel123/verge-asm/issues/72)
- [What is `certificate-weak-key-or-signature`'s table, and who owns a claim about the WebPKI?](https://github.com/winniel123/verge-asm/issues/68) — **five rows, and §10.5's owner definition survives with one clause: a primitive's owner is its specifier.** [ADR-0035](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0035-a-cryptographic-primitives-owner-is-its-specifier.md) · [`weak-key-and-signature.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/weak-key-and-signature.md). MD5 and SHA-1 on the signature digest; RSA < 2048, ECDSA `len(n)` < 224, DSA below (2048, 224) on the key; **Ed25519 needs no row**. The predicate ranges over the **chain**, the signature limb skips a self-signed certificate where the key limb does not, and an unnamed algorithm makes the rule **false, never `not-evaluable`**
- [An upstream example config a distributor ships as the default: documentation, or a shipped default?](https://github.com/winniel123/verge-asm/issues/69) — **documentation: an example attests nothing in either direction, and installing one transfers *operativeness*, not *ownership*.** [ADR-0036](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0036-a-shipped-default-is-the-configuration-that-takes-effect.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §12. §10.4's *costly act* decides it **against** the example — a file no daemon reads produces no first run. No pair moves and the list stays at 37; the shipped `cassandra.yaml` prohibits 9042 twice, so **the weak tier is two rows, not three**. Opens [#70](https://github.com/winniel123/verge-asm/issues/70)
- [Is §2.2's footing table right? It was built from web docs, and the shipped config bytes have never been read](https://github.com/winniel123/verge-asm/issues/70) — **it is: fourteen artefacts from twelve projects re-derived, every cell holds, no footing and no pair moves.** [ADR-0037](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0037-an-attestation-is-retrieved-over-the-artefact-not-over-the-row.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §13. **No config file names the internet as a supported environment** — three near-misses refused, including Elastic shipping two operative defaults that contradict each other. Opens [#75](https://github.com/winniel123/verge-asm/issues/75) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**), [#76](https://github.com/winniel123/verge-asm/issues/76)
- [Which project-authored constants are products of a moving world quantity rather than the quantity itself?](https://github.com/winniel123/verge-asm/issues/71) — **one instance, and it is the one already fixed.** [ADR-0038](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0038-a-constant-is-a-product-only-where-the-quantity-is-readable.md) · `docs/research/project-authored-constants.md`. Eighteen walked: two products fail **reach** and needed no cure, fifteen are not products, and **`k` is the rule already satisfied**. The worst finding is not an instance — ADR-0028's currency bound is a **stale argument**. Opens [#77](https://github.com/winniel123/verge-asm/issues/77), [#78](https://github.com/winniel123/verge-asm/issues/78) (**both blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [What does `certificate-expiring`'s census say when every endpoint carries its own horizon?](https://github.com/winniel123/verge-asm/issues/72) — **the header glosses the rule as the fraction and never a day count or a range; the row shows days remaining against its own horizon and the validity that produced it; the name stands.** Drawn, [#60](https://github.com/winniel123/verge-asm/issues/60)'s objection is worse than it was stated: two rows read the *identical* 30 days remaining and land in **opposite census members** — a contradiction rather than an incomparable column, living entirely in the row. A range loses on an **undecidable denominator**, not on #28. [`prototypes/certificate-expiring-census/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/certificate-expiring-census). Opens [#74](https://github.com/winniel123/verge-asm/issues/74)
- [Is there an unscoped owner statement of a certificate key-size floor, or is 112 bits permanently NIST's?](https://github.com/winniel123/verge-asm/issues/73) — **it exists, and it was in a document class nobody searched.** [ADR-0040](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0040-a-specifications-silence-is-not-the-owners-silence.md) · [`weak-key-and-signature.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/weak-key-and-signature.md) §13. RFC 9325 §4.5 (BCP 195, `MUST`, 2048-bit modulus) and RFC 9846 §C.2 — the latter also in **RFC 8446**, which #68 cited as setting none. **A footing moves, not a row**: the weak tier is now DSA's `N ≥ 224` limb alone. ~340 documents searched. Opens [#79](https://github.com/winniel123/verge-asm/issues/79)

- [Does deriving `verge-core` from `nmap-services` make us a derivative work under the NPSL, and is that AGPL-3.0 compatible?](https://github.com/winniel123/verge-asm/issues/78) — **no, and the licence flag is checked and lowered rather than raised.** [ADR-0003](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0003-third-party-source-consent-bar.md) fifth amendment · [`nmap-services-licence.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/nmap-services-licence.md). NPSL §3's obligation is conditional on accepting rights we do not need; the hot set is not a derivative work on **two independent grounds** and `verge-core` does not change. **The weekly top-1000 tier is retired**, being the one place we would reproduce nmap's selection whole. Losing option: **self-generating a ranking**, refused on **capability**. IANA is CC0 1.0 and adopted for **naming**, rejected as a ranking. Opens [#80](https://github.com/winniel123/verge-asm/issues/80) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [Is every census member drillable, and what does drilling into a member that is most of the estate render?](https://github.com/winniel123/verge-asm/issues/74) — **all three, unconditionally, one rule for the census — and the census card carries no rows at all.** A member's list is its own surface: complete, ordered by subject, unfiltered, untruncated. **A row carries what differs between rows; what is the same on every row belongs above them** — so three of the sixteen rules produce a bare-subject row. Sample, roll-up and threshold all lose; **breadth is not the discriminator, evidence variation is**. [`prototypes/census-member-drilldown/`](https://github.com/winniel123/verge-asm/tree/main/prototypes/census-member-drilldown). No ADR, per [#10](https://github.com/winniel123/verge-asm/issues/10)'s precedent
- [Is `certificate`'s currency bound still safe for the clock class, now that six-day certificates are generally available?](https://github.com/winniel123/verge-asm/issues/77) — **no: `k × cadence` bounds the window in our units where the hazard is in the subject's.** [ADR-0043](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0043-a-clock-reading-rule-bounds-its-evidence-in-the-subjects-own-units.md). The clock class gains a second per-rule gate at the certificate's own horizon — `min(k × cadence, N)` — sitting **beside** `k`, which is untouched, and landing in `not-evaluable` on #44's fourth cause. **The hot tier does not move at all**, so *two days modally* survives; on the **cold** tier `not-evaluable` may become the clock class's largest census member
- [Is the onboarding full-range sweep opt-in, or does it run unconditionally?](https://github.com/winniel123/verge-asm/issues/80) — **opt-in, and the unconditional limb was not an expressible object.** [ADR-0044](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0044-a-one-off-measurement-has-no-currency.md). A one-off has no cadence, so `k × cadence` is undefined; ADR-0005 has no configured object for it; and it would make #44's aperture statement non-constant. **#4 §6.4's *"never scan on config save"* kills the *operator is present and watching* premise** — the sweep would run overnight, unattended, on the operator's first night. The losing option, a cold `Scan` enabled monthly, **could not complete inside its own cadence**. Repairs two counts #78 stranded: `Scan`s four→**three**, port tiers three→**two**. Opens [#81](https://github.com/winniel123/verge-asm/issues/81) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [Cassandra's shipped config prohibits internet exposure of 7000 and 7001 too — are they rows?](https://github.com/winniel123/verge-asm/issues/75) — **no: both refused on determinacy, and the list stays at 37 pairs.** [ADR-0042](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0042-a-squat-is-contested-where-the-other-convention-is-live.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §14. **`7000/tcp` is the first row in the note refused with its claim *and* its attestation both established** — better attested than `5432` — which demonstrates §13.5's *the strongest sentence in the corpus does not clear a gate it does not speak to*. Apple documents `AirPlay · 7000 · TCP`, so the squat is contested by a live competing convention; `7001` fails twice over. Losing option: admit `7000` on attestation strength, refused as a **frequency argument**. **Free in the strong sense.** Opens [#82](https://github.com/winniel123/verge-asm/issues/82)
- [§2.4's determinacy gate has no evidence standard — what may establish that a convention is contested?](https://github.com/winniel123/verge-asm/issues/82) — **a convention is evidenced by placement, never by catalogue**; the unit is the **protocol, not the vendor**; *live* means **current, never numerous**; and every determinacy refusal must now name the artefact that defeated it. [ADR-0048](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0048-a-convention-is-evidenced-by-placement-never-by-catalogue.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §15. **No second sense of *owner* was minted**, which is why gate 2's owner apparatus does not travel. Nine rulings re-run; no pair moves, the list stays at 37. Amends ADR-0042. Closes §8's question 10; two candidates **priced rather than deferred**, into Out of scope
- [Does an address-scope `Seed` enumerate, or does it only bound?](https://github.com/winniel123/verge-asm/issues/81) — **it enumerates**, one hop deep (`Seed` → `Address`): every address inside a declared CIDR is a subject from the declaration, walked every cadence, whether or not anything ever answered. A **name** scope enumerates nothing. [ADR-0047](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0047-an-address-scope-is-its-own-enumeration.md). **The decider was the flagship message, not the gloss and not the load** — under any bounding reading a listener appearing in declared address space cannot fire `not-reached` → `reached`. **Six existing structures already presupposed enumeration.** §9's `/22` cap is a validation on the declaration, adjudicating **cost rather than truth**, which is why it is not the size cap #27 refused. **Thin ground, flagged not smoothed.** Opens [#85](https://github.com/winniel123/verge-asm/issues/85), deliberately **not** wired to block #12
- [§2.2's footing table covers 19 of 37 pairs — seven listed rows have no footing cell](https://github.com/winniel123/verge-asm/issues/76) — **all seven placed, and the table now states its own coverage: 26 of 37, with the other 11 named as deliberately out of subject.** [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §16. **The ticket's central prediction is refuted**: etcd ships `THREAT_MODEL.md` at `v3.7.1` naming **Port 2379** and **Port 2380** by number, so both join the **explicit prohibition** tier. **The document is at the repository root rather than where §3.4's citation points, so a session that fetched the cited URL would have confirmed the hypothesis and been wrong** — ADR-0037 paying out a second time. The weak tier grows to three, on `10255/tcp`. No pair moves. Opens [#83](https://github.com/winniel123/verge-asm/issues/83) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**) and [#84](https://github.com/winniel123/verge-asm/issues/84)
- [Which curated-table negatives were established over specifications alone, and does ADR-0040 reopen any?](https://github.com/winniel123/verge-asm/issues/79) — **sixteen enumerated with their document classes; six overdetermined, seven swept, one class list already exhausted, two the named residue. No row moves.** [ADR-0046](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0046-a-negatives-corpus-is-its-owners-class-list-and-only-a-sole-ground-negative-is-exposed.md) · [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §17 · [`weak-key-and-signature.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/weak-key-and-signature.md) §14. **`161/udp` does not re-open**, and RFC 4778 is the predicted frequency trap. **`5672`/`15672`'s negative strengthens into a positive.** **`9092/tcp` Kafka's negative fell and the row still does not move.** MD4 measured for the first time — #68 had cited RFC 6149, which is about MD2. Opens [#87](https://github.com/winniel123/verge-asm/issues/87) and, via §17.6, [#88](https://github.com/winniel123/verge-asm/issues/88)
- [Can an address-scope `Seed` be an IPv6 prefix, and what does the range cap mean if it can?](https://github.com/winniel123/verge-asm/issues/85) — **yes, and there is no IPv6 rule**: the cap was never a prefix length but **1,024 addresses**, `/22` in IPv4 and `/118` in IPv6, so the `Seed` kind does not split and no family check enters the model. [ADR-0049](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0049-an-address-scope-is-family-agnostic-and-the-cap-counts-addresses.md). Beat a **family check** on the vantage residue, ADR-0013 §6 barring the `custody extension` from the class check by design. **[measured]** one `/64` is ≈ 4.1 × 10¹¹ years on the daily tier, so **sweeping IPv6 space is impossible rather than undecided** and goes to Out of scope. **One rule is extended rather than restated, and flagged as such**: the containment test is over every address a `Vantage` holds, of either family. No knob ceiling was invented, so the residue lands as `Seeds`-screen copy. Opens [#89](https://github.com/winniel123/verge-asm/issues/89)
- [Does an owner's category statement reach a port the owner has not numbered?](https://github.com/winniel123/verge-asm/issues/88) — **yes, on three limbs: standing, membership established by the owner's own artefact, and defeat tested per member.** §2.3's refusal was always about the **party**, never the **grammar**. [ADR-0050](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0050-an-owners-category-statement-reaches-the-members-its-own-artefacts-place-inside-it.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §18. `10250/tcp` and `10255/tcp` join the explicit prohibition tier — tiers **15 / 9 / 2**, coverage 26 of 37, **no pair moves**. §2.3 is **narrowed, not weakened**: CISA and the hyperscalers still carry nothing. The *hardening instruction* counter-argument was argued and refused on its own consistency; `4369` is deliberately **not** rescued. Opens [#90](https://github.com/winniel123/verge-asm/issues/90) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [10250 and 10255 kubelet: Class A rests on a generated page the shipped source contradicts](https://github.com/winniel123/verge-asm/issues/83) — **the premise is refuted: the kubelet ships *two* defaults per setting, not one position and one stale rendering.** `applyLegacyDefaults` **overwrites** the config-API defaults, so the flags do not inherit them and the generated page is accurate; [ADR-0036](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0036-a-shipped-default-is-the-configuration-that-takes-effect.md) limb 1 decides it on the owner's own **deprecation label**. **`10250` moves Class A → Class C; `10255` stays in Class A**, a default that turns the port *off* not defeating Claim 1. **No pair moves — 37 pairs, classes 11 / 7 / 19 — so the removal it was priced for is not spent.** [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §19. §16.10's generated-page hazard is **withdrawn as a mis-diagnosis**: the hazard is **provenance, not currency**. Opens [#91](https://github.com/winniel123/verge-asm/issues/91) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [4369/tcp epmd: the "why" cell describes the distribution cookie, and the attestation is a non-owner's](https://github.com/winniel123/verge-asm/issues/84) — **the cell names the wrong port's protection, and the owner had spoken all along**: the cookie guards the distribution handshake, not epmd, and Erlang/OTP's `secure_coding.md` rule `DEP-001` names EPMD and calls its unauthenticated answering a leak. §16.9's residue **withdrawn** — the first firing of [ADR-0040](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0040-a-specifications-silence-is-not-the-owners-silence.md)'s falsification clause, and from a **fourth class** #76's corpus did not contain. By-catch is sharper than the ticket: §10.1's Class A walk cites a **non-owner** in two Step 1 cells and names an operation an internet caller **cannot reach**. Row stands, tier stands, 37 pairs. [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §20. Opens [#92](https://github.com/winniel123/verge-asm/issues/92), [#93](https://github.com/winniel123/verge-asm/issues/93)
- [Is an owner's unreleased document the owner's documentation? — and does 9092/tcp become a row if it is?](https://github.com/winniel123/verge-asm/issues/86) — **no**: the second form reads a document the owner has **issued**; an unissued one attests nothing in either direction and is **not a corroborator** — right party, wrong time. [ADR-0045](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0045-an-owners-documentation-is-what-it-has-issued.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §21. **`9092/tcp` stays out and the list is 37 on a rule rather than on a gap.** §12's **costly-act** test decides it a second time, and the committed reading dies on §12.3's stability ground with **no stopping point above an open pull request**. The reopening criterion is **one release away**, not hypothetical: the sentence sits on the `4.4` branch with no tag. Ruled on thin ground and marked as such
- [Has ADR-0042's liveness test ever been run on esmagent and fmtp, and do 5601 and 8500 survive it?](https://github.com/winniel123/verge-asm/issues/87) — **both registrations are live, both squats stay contested, and neither row moves — the list stays at 37.** [ADR-0048](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0048-a-convention-is-evidenced-by-placement-never-by-catalogue.md)'s first application, and the first pointed at an **excluded** row's squat. **[measured]** `esmagent`'s registrant traced through IANA's **XML contact records, which nobody had opened**, to Broadcom, still placing the CCS Windows Agent on `5601`; EUROCONTROL puts FMTP on `8500` in a `shall` and still catalogues it, with Adobe's ColdFusion defeating `8500` independently. **§17.8's residue goes to zero and no ADR-0040 sweep is owed.** The ticket's steer that limb 3 would decide `esmagent` is **refuted** — limb 2 decides it. [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §22. No ADR minted
- [What is an `Address`'s natural key — the text, or the address it denotes?](https://github.com/winniel123/verge-asm/issues/89) — **the address**, compared as octets; RFC 5952 is rendering computed on read. The general half is the rule above: **a subject key gets no ADR-0011 canonicaliser**, because a canonicaliser is versioned and a key normalisation cannot be. [ADR-0051](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0051-a-subject-key-is-the-thing-denoted-and-its-normalisation-may-never-move.md); `CONTEXT.md` and ADR-0011 amended, ADR-0049's ticketed seam discharged. **Two thin points flagged rather than smoothed**: the re-key migration is described and has **never been designed or priced**, and no source in this repo has been *measured* emitting a mapped-form address. Opens [#94](https://github.com/winniel123/verge-asm/issues/94) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [`623/udp` IPMI: the owner names DRACs and the port number comes from CISA — does limb 2 hold?](https://github.com/winniel123/verge-asm/issues/90) — **yes; the row stays, footing re-founded in place.** §20's shape, not §18's: two owner artefacts number the pair and Dell's own statement places the DRAC inside IPMI, so `623/udp` sits in §18.5's *numbered by the owner* bucket and the unit-rule objection dissolves. [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §23. **No ADR minted** — three candidate rules all follow from existing ones. Two open items handed on: **HPE and NEC produced no retrievable first-party document** (breadth, not class), and the specification's **clear-text `Get Channel Authentication Capabilities`** is a live Class A argument against a Class C row — both are [#96](https://github.com/winniel123/verge-asm/issues/96) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [10259, 10257 and 10256: three subjects the kubelet's own ports table names and the list lacks](https://github.com/winniel123/verge-asm/issues/91) — **two admitted, one refused, and the list is 39 pairs with classes `11 / 7 / 21`.** [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §24; ADR-0009 amended, no ADR minted. **The inversion is the finding**: the most-exposed of the three — `10256`, plaintext, no auth, wildcard-bound on every kubeadm node — is the one **refused**, which is §1's *never correct* versus *commonly attacked* working. Opens [#95](https://github.com/winniel123/verge-asm/issues/95) (`10249/tcp`, a bare mux with no authn or authz) and [#97](https://github.com/winniel123/verge-asm/issues/97) (the hot-set `+2` is **asserted, not measured**), both **blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**
- [Claim 1's Class A walk cites a non-owner in two Step 1 cells, and the Step 2 operations were never walked against the bytes](https://github.com/winniel123/verge-asm/issues/92) — **both repaired, all eleven Step 2 cells hold, no row moves.** [ADR-0054](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0054-a-claim-step-is-answered-only-by-evidence-about-that-step.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §25. etcd's prohibition was **refused** for Step 1 — a Claim-3-shaped boundary in a Step 1 cell is §10.1's own *"the tell nobody read"* — and the refusal forced the retrieval that found the right sentence. ZooKeeper's *world-writable* and memcached's UDP both confirmed from the bytes. **Limb 1 is marked *obiter* in the ADR itself.** `2375` Docker is a second documented-versus-shipped divergence after #83's kubelet, annotated in place
- [The class sweep's other half: were the positive prose footings ever gathered class by class?](https://github.com/winniel123/verge-asm/issues/93) — **swept, and no row or footing moves** — the note's positive footings can now say what §17 let its negatives say: *a searched corpus with a stated boundary*. [ADR-0046](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0046-a-negatives-corpus-is-its-owners-class-list-and-only-a-sole-ground-negative-is-exposed.md) amendment, [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §26. It corrected its own ticket's premise first — the population is **24, not 23**, because #88 restated the table `13/10 → 15/9`. **`2049/tcp` is the one to read**: RFC 7530/8881 name the Internet as a *stated design goal*, and **Appendix C of the same current document records the goal as unmet** — ruled *failure condition not met* on contested ground, with the criterion that would move it named. Opens [#98](https://github.com/winniel123/verge-asm/issues/98)
- [What is a `Name`'s natural key — case, the trailing dot, and A-label vs U-label](https://github.com/winniel123/verge-asm/issues/94) — **the label sequence**: case folds, the dot is the parser's, the A-label is stored and the U-label refused. [ADR-0055](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0055-a-names-key-is-the-label-sequence-and-we-fold-only-what-the-protocol-folds.md); ADR-0051 **confirmed** and its residue discharged, ADR-0011 amended in one further detail, `CONTEXT.md` in four entries. The losing option was the most ADR-0051-shaped one — **refuse mixed case as `010.1.1.1` is refused** — which applies the refusal test to variance rather than ambiguity. Containment on three gates is **label-wise suffix equality**, which is what stops `evilexample.com` reading as inside `example.com`. **Four thin points flagged**, chiefly the A-label-only rendering and an unexercised SNI case-sensitivity residue. Leaves one residue, now ticketed as [#99](https://github.com/winniel123/verge-asm/issues/99): **whether a wildcard SAN admits a `Name`**
- [Is the hot set actually a superset? 10259 and 10257 were asserted into it, never measured](https://github.com/winniel123/verge-asm/issues/97) — **it is, analytically rather than measurably, and §6.1's arithmetic survives re-derivation in every cell.** [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §29; [ADR-0058](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0058-a-superseded-mechanism-is-withdrawn-at-the-site-that-specifies-it.md), ADR-0009 amended. **[measured]** the frequency half is **123 TCP ports**, not `~140`; neither `10259/tcp` nor `10257/tcp` is in it, so §24.10's flag is discharged. All rows were tested and §6.1's enumeration of the 28 is set-exact — **composed with §27 the containment is `28 + 8 + 5 = 41` and `verge-core` is 136 pairs (131 TCP), the widening costing +3.1 % of the daily tier's per-host budget.** **The larger result is a subtraction from [#12](https://github.com/winniel123/verge-asm/issues/12)**: §6's build-time invariant was dissolved by ADR-0009 and three documents still asserted it, so the spec must carry the union definition and **not** a containment test. **The enumeration had already been done in [#78](https://github.com/winniel123/verge-asm/issues/78) for a licence question and nothing pointed to it**
- [§20.8's prohibition-tier criterion is refuted by 873/tcp — apply it or restate it](https://github.com/winniel123/verge-asm/issues/98) — **applied, not restated: `873/tcp` moves to the scoping tier and the criterion becomes true over the rows that remain.** [ADR-0059](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0059-a-footing-tier-grades-evidential-distance-never-the-owners-conviction.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §30. **`3306/tcp` is clean** — MySQL names the Internet one sentence before the prohibition, in the same `<p>`, at **two issued versions** — so `873` is the **sole** counterexample and the losing option carried its maximum weight. **Tiers `15 / 11 / 2` → `14 / 12 / 2` by this pass alone, and `14 / 13 / 3` composed with §27; no row moves.** The ADR is minted against a strong no-mint norm because *what the tier grades* was written down nowhere, which is why §18.6 and §20.8 reconstructed it in two vocabularies. **The demotion moves the tier's most forceful sentence downward, which is the proof the column is not a ranking of owners.** Sufficiency, and what sorts the tier's non-prose members, is now [#101](https://github.com/winniel123/verge-asm/issues/101)
- [`10249/tcp` kube-proxy metrics: a bare mux with no authn or authz, and two weaker numbers beside it](https://github.com/winniel123/verge-asm/issues/95) — **two admitted, one refused, and the list is 41 pairs with classes `12 / 7 / 22`.** [ADR-0056](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0056-a-port-constant-in-a-library-is-not-a-shipped-listener.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §27. **Step 1 was the live question and it admits**: the owner's own metrics document says reading `/metrics` requires a ClusterRole, which [ADR-0054](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0054-a-claim-step-is-answered-only-by-evidence-about-that-step.md) limb 1 accepts *because* it is prescriptive — and limb 1 **cuts both ways**, barring the same owner's `curl http://localhost:10249/proxyMode` instruction from refusing it. **[measured]** one function returns the wildcard for healthz and loopback for metrics, and §24.3 quoted it for the `10256` half only. `10248` is the first row admitted on a **published config-API doc comment**, flagged as the section's thinnest joint and ticketed as [#100](https://github.com/winniel123/verge-asm/issues/100)
- [`623/udp`: is it Class A, and the positive-footing retrieval #93's fired conditional now owes](https://github.com/winniel123/verge-asm/issues/96) — **no and swept: the row stays Class C, and the footing leaves a third party's advisory for the owners' own security guides.** [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §28; [ADR-0054](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0054-a-claim-step-is-answered-only-by-evidence-about-that-step.md) annotated, **no ADR minted**. Two arguments stronger than the ticket's were found and refused — **Cipher Suite 0**, which the spec mandates *support* for, and **RAKP Message 2's password-keyed HMAC**. §26.2's footing was the only one of 24 whose artefact sat **outside ADR-0040's taxonomy**, which is why it could not be filtered; all three vendor cells §23.4 recorded as blocked are now **open and prohibitive**, and **HPE and Dell publish the same sentence**. **§10.3's failure condition is unmet by every co-owner**, discharging #90's reopening criterion. **[measured]** two of the four co-authors ship IPMI-over-LAN **disabled by default**. §26.5's arithmetic moves `13+0+8+1+2` → `12+0+8+1+3`; no global figure moves
- **Merge note, 2026-08-14 — the five passes above ran concurrently and their figures were reconciled afterwards.** Each pass walked from a pre-merge baseline of *39 pairs · `11 / 7 / 21` · coverage 28 of 39 · tiers `15 / 11 / 2 / 11` · `28 + 6 + 5 = 39` · exclusions 19*, so **many figures in their own resolution comments are correct-as-of-that-pass and jointly stale**. The composed state is **41 pairs · classes `12 / 7 / 22` · tiers `14 / 13 / 3 / 11` · coverage 30 of 41 · `28 + 8 + 5 = 41` · `verge-core` 136 · exclusions 20 · §17.1 negatives 15 · watch list 3**, anchored in a *Merge reconciliation* table after §1's summary table in [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) — **read that table, not a pass's own figure line.** No verdict, class, row or footing moved in reconciliation. It also found two things no pass had done: **ADR-0032 §8's watch list and §18.6's membership table were never amended by #95 at all**, which is a missing amendment rather than a stale numeral
- [Does a certificate's wildcard SAN admit a `Name`, and what is the subject if it does?](https://github.com/winniel123/verge-asm/issues/99) — **no, and there is no wildcard subject to admit it as**: a wildcard denotes a **set** of names, so ADR-0051's *a subject key is the thing denoted* has nothing to bind, and `*.example.com` is a `Subject` from **no source**. [ADR-0060](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0060-a-wildcard-san-is-a-pattern-over-names-and-admits-none-of-them.md); `CONTEXT.md` amended in three entries (`Source`, `Name`, `Shadowed`), ADR-0055 **confirmed** and its named residue discharged, ADR-0011's differ prohibition and ADR-0032's `certificate-hostname-san-mismatch` row both untouched — **matching is not admitting**. The losing option was **admit it as an ordinary `Name`**, and it lost on drift twice: where the zone has no wildcard record — the ordinary case, since the CA validates the **pruned FQDN** and never the record — RFC 4592 §2.3 returns a Name Error and ADR-0006 withdraws the subject **in the first cadence after it entered**; where it does, the exact-match answer **is** the poison signature, so `wildcard-discrimination` reads `Shadowed` on the one name that is not, the `Endpoint` sends an SNI RFC 6066 §3 has no reading for, and `certificate-hostname-san-mismatch` gets a reference identity RFC 6125 §1.8 does not define **inside its own domain**. **[measured, [#3](https://github.com/winniel123/verge-asm/issues/3) §2.1]** the repo already lists this as CT's own Miss — *"wildcard certs hide the specific hostnames behind `*.example.com`"* — so the ruling ratifies a documented limitation rather than creating one. `Coverage` gets **a sentence in #28's fill half and no figure**: the set is unbounded, so any denominator is #28's refused completeness score. **Four thin points flagged**, chiefly that the zone-file limb is by-catch taken from the model's shape rather than from a document, and that *commonest branch* is argued rather than counted. Cost **zero** — no wildcard SAN ever admitted anything. Opens [#103](https://github.com/winniel123/verge-asm/issues/103), deliberately **not** wired to block [#12](https://github.com/winniel123/verge-asm/issues/12)
- [Is a config-API doc comment a label or a position — and does it attest?](https://github.com/winniel123/verge-asm/issues/100) — **a comment takes a position only where it *outlives the value it annotates*; where its content is exhausted by that value it is a label.** Necessary and not sufficient — §2.3's and §4.4's discrimination runs **second**, on the survivor. [ADR-0061](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0061-a-comment-is-a-position-only-where-it-outlives-the-value-it-annotates.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §31; ADR-0036's thin-ground flag **discharged**, ADR-0059 **confirmed by use**. A **published config-API doc comment** is the ordinary artefact answering the third form's *documented* limb only — the defaulting code answers *takes effect* and must be retrieved — and **artefact class never grades a tier**. **[measured]** the test reproduces **all nine** comment verdicts in the note across five projects and three artefact classes, eight of them at step one, and the two kubelet doc comments separate at **two different steps twelve lines apart in one struct**: `readOnlyPort`'s survives and is refused as a description, `healthzPort`'s *"the port of the **localhost** healthz endpoint"* goes **false** under `healthzBindAddress: "0.0.0.0"`. **`10248/tcp` stays in the weak tier and nothing moves.** Opens [#105](https://github.com/winniel123/verge-asm/issues/105)
- [Is §20.8's tier criterion sufficient as well as necessary — and what sorts the scoping tier's non-prose members?](https://github.com/winniel123/verge-asm/issues/101) — **yes, on the four-limb conjunction [ADR-0059](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0059-a-footing-tier-grades-evidential-distance-never-the-owners-conviction.md) limb 3 already enumerated — owner's · reaches the pair · names the public internet · takes a position — and the list is closed the way §10.2 closed the claim set.** `2181` ZooKeeper and `25672` RabbitMQ **promote**; tiers **16 / 11 / 3**, coverage 30 of 41, **two cells, no rows**. [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §32. **[measured] the criterion takes no neighbour**: run promoting across all thirteen scoping members it promotes exactly two, `4369` refused on limb 1 (RabbitMQ is a non-owner for it) and `2049` on limb 4; run demoting it refuses none of fourteen. **ADR-0059 amended in place, no ADR minted, 0062 unused** — limb 1's unit widens from *sentence* to **the owner's statement of the port's permitted network**, so the non-prose footings come inside it, §24's and §27's placements are **confirmed**, and §30.7's two-population disclosure is **dissolved**. The closure is the thin ground and is flagged: *there is no fifth limb* is a ruling, and **direction** — inbound versus outbound — is the named candidate no member turns on today. Opens [#107](https://github.com/winniel123/verge-asm/issues/107)
- [Is ADR-0058's shape present elsewhere in the corpus?](https://github.com/winniel123/verge-asm/issues/102) — **it recurs, and it now has a population: nine unrepaired sites over six superseding decisions, running ADR-to-ADR, ADR-to-note, note-to-note and ADR-to-glossary rather than the note-to-ADR the rule was argued from.** [ADR-0058](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0058-a-superseded-mechanism-is-withdrawn-at-the-site-that-specifies-it.md) annotated; eight repaired in place across ADR-0009, ADR-0032, ADR-0034, `sensitive-ports.md` §24, `safe-active-probing.md` §2.5, `insecure-listener-rules.md` §9.1 and `weak-key-and-signature.md` §9, and [`docs/agents/domain.md`](https://github.com/winniel123/verge-asm/blob/main/docs/agents/domain.md)'s `Vantage class` rider generalised into the rule. **[measured]** **197 superseding moves examined over 39,382 lines at `c0881ae`**, 76 in scope as mechanisms; the bound held because **no ADR carries `Status: Superseded`** — every supersession here is partial and inline, so the amendment surface is the complete corpus, and the negative is **dated rather than permanent**. Sharpest measurements: §24 has **zero** withdrawals in 682 lines while §27's word-for-word mirrors were all struck; ADR-0032 and ADR-0034 cite ADR-0038 **zero** times; `insecure-listener-rules.md` cites ADR-0015 **zero** times. **Stays detectable, not a ninth trigger** — the eight triggers watch the *world* moving and this fires when *we* move, so it is the wrong list on **shape before cost**; detection already fires as a batch by-product at zero marginal cost. **The earliest instance predates the ADR by a day** and is `CONTEXT.md`'s own `Vantage class` ← ADR-0002's #27 amendment, which cost [#39](https://github.com/winniel123/verge-asm/issues/39). One instance moves a **ruling** rather than a document and was held back: opens [#104](https://github.com/winniel123/verge-asm/issues/104) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**) and [#106](https://github.com/winniel123/verge-asm/issues/106)
- **Merge note, 2026-08-14 — the four passes above ran concurrently and were merged serially afterwards.** ADR and section numbers were **pre-assigned per agent** rather than reconciled on merge, which is why nothing collided: 0060/0061/0062/0063 and §31/§32/§33 were reserved, and **0062, 0063 and §33 went unused and are now gaps** alongside 0039, 0041, 0052, 0053 and 0057. Each pass walked from `main` at `c0881ae` — *41 pairs · classes `12 / 7 / 22` · tiers `14 / 13 / 3 / 11` · coverage 30 of 41* — and **only #101 moved a figure**, so the composed state is **tiers `16 / 11 / 3 / 11`, everything else unchanged**; #99, #100 and #102 are figure-neutral by their own rulings. The one merge conflict was three append-collisions between §31 and §32 in `sensitive-ports.md`, resolved by keeping both sides in section order rather than accepting git's interleave, and verified by the branch's own insert count. **The map's sub-issue list stands at 86 of GitHub's 100**, five having been added by this batch — the next fan-out should detach the next oldest closed children before it runs

- **A wildcard's synthesis is a fact about the `Name` it was probed under, and *apex* is that name's description rather than the rule.** A facet of its own and never a value on `dns-record` — the `Span` key is already occupied by what the authority served for that subject itself, and the meaning fails before the key does — and it **admits nothing**, so a repoint is one `Transition` on one timeline against the N-subject *message* burst `Shadowed` refuses, silent under [ADR-0026](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0026-the-facet-layer-is-evidence-not-a-channel.md) exactly as `dns-record` is. **Deferred rather than refused**, and not on price: [ADR-0015](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0015-the-value-space-is-the-commitment.md) makes waiting free, while [ADR-0011](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0011-a-facet-is-six-parts.md)'s **sixth part is unwritable** because **[measured]** nothing declares which `Name`s are control-probed — not ADR-0021's parameter table that `project-authored-constants.md` §6 calls *the authoritative population*, not [`measurement-offers.md`](https://github.com/winniel123/verge-asm/blob/main/docs/spec/measurement-offers.md), not [ADR-0025](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0025-an-offer-is-scope-only-where-the-value-enumerates-it.md) — which is §3.2's fictional-inventory hazard live in v1 and is [#108](https://github.com/winniel123/verge-asm/issues/108), **blocking [#12](https://github.com/winniel123/verge-asm/issues/12)** — [#103](https://github.com/winniel123/verge-asm/issues/103) · [ADR-0062](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0062-a-wildcards-synthesis-is-a-fact-about-the-name-it-was-probed-under.md)
- **A rule is excluded by its fact or by its aperture, never by the shape of the set — and `smb-signing-not-required` is excluded by its aperture, so the v1 set stays at sixteen.** — [#104](https://github.com/winniel123/verge-asm/issues/104) · [ADR-0065](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0065-a-rule-is-excluded-by-its-fact-or-by-its-aperture-never-by-the-shape-of-the-set.md). **Both** of §9.2's grounds are gone, not one: *it fits neither rule* is a claim about our two rules, and a fact fitting neither wants a third — ADR-0004's #35 amendment already refused *a bar erected to protect the list rather than the property*. An aperture ground **cannot go stale unnoticed**; a principled one rots, and this one was dead from #41 to #104 in a note citing ADR-0015 zero times. **[measured]** **Eight protocols read against their own specifications** — SMB is the **only** one where the integrity requirement is server-originated, pre-credential, listener-scoped and a *requirement* rather than a capability, so ADR-0015's generality test is **closed in the admitting direction** and a cross-protocol abstraction would carry one real implementation and a set of degenerate ones. Probe measured at **112 bytes, one round trip, no server state, a fixed constant byte string** — cheap, and not the cost that excludes it. **Deferrable with no rework**, but the **third field** on `listener-negotiation` is owed at that facet's specification time, since a value space is decided once. The `Exposure` question is **left open on genuinely split evidence** — the owners' shipped defaults leave signing not required off the DC, and the owners are simultaneously withdrawing that default.
- **A default's provenance is not read — *takes effect* reads the outcome, so a language's zero value satisfies it, and a zero value attests only where the owner documented it.** ADR-0036 limb 1's conjunction is untouched and is where a zero value fails when it fails: an undocumented value is not §2.2's third form at all, and reading one as a maintainer position is *the claim may not be asserted by us* failing through the language's type system. And **§10.4's one-way rule governs the attestation gate and not the claim gate** — a value read to establish what the shipped artefact *does* (§10.1's steps, §10.4.3's remedy test) is read in **both** directions, documented or not, under §20.6 and [ADR-0054](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0054-a-claim-step-is-answered-only-by-evidence-about-that-step.md) — [#105](https://github.com/winniel123/verge-asm/issues/105) · [ADR-0036](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0036-a-shipped-default-is-the-configuration-that-takes-effect.md) **amended in place, no ADR minted**, [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §34; [ADR-0061](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0061-a-comment-is-a-position-only-where-it-outlives-the-value-it-annotates.md) gains a *confirmed by use* note closing limb 3's empty-retrieval case. **The two instances never disagreed** — §16.5 read a zero value through the attestation gate, §27.2 through the claim gate — and §19.2's *"by omission plus documentation"* clause is **promoted** to the rule it always was. **The empty delta: nothing moves.** **[measured]** at `v1.34.0` `readOnlyPort` carries *"Default: 0 (disabled)"* and `enableProfiling` carries **no default line at all**, while `defaults.go` writes neither — the separator is a limb ADR-0036 already had. Decided on principle and **not** on a cell move: the losing option moves nothing either (§34.11)
- **ADR-0058's unit is the *sentence*, so a document supersedes itself — an amendment appended to a file does not discharge a clause standing unmarked elsewhere in that same file, above it or below it.** The narrow reading loses on the ADR's own founding measurement, which never leaves ADR-0009: #91 re-asserted the dissolved invariant **inside the body that dissolved it**, #95 repeated it, and #97 wrote the withdrawal 87 lines away in its own amendment section. Discharge is **strike the clause and name the decision, at the clause** — a blockquote in prose, an inline pointer in a table cell; `Alternatives rejected` and `Context` recitals fall outside by the **voice** test rather than by exemption, and a dated `## Amendment` heading does **not** exempt its bullets — [#106](https://github.com/winniel123/verge-asm/issues/106) · [ADR-0058](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0058-a-superseded-mechanism-is-withdrawn-at-the-site-that-specifies-it.md) **amended in place, no ADR minted** (#101's precedent). **Stays detectable, not a ninth trigger** — and the widening makes it **cheaper** to detect, both sides being in one file. **[measured]** **25 sites over 13 superseding decisions across 12 documents at `9a8f1df`** — 196 superseding moves over 41,941 lines, including a **third amendment convention #102's counts missed** (85 inline italic *Amended by …* notes). Six of the 25 trace to one misreading — ADR-0032 and ADR-0004 both wrote *"stands unrewritten per the name-and-withdraw convention"*, but the convention is *left standing **and marked***. **9 of 22 run the other way**, the amendment sitting *above* the stale clause, which an order-based rule misses; **one site reaches code** — ADR-0032's *"#68 blocks #12"*, discharged 400 lines above it. Ruled on **thin ground and flagged as such**: the population's size is measured, its *rate* is not, the corpus recording catches rather than misses
- [The prohibition tier is a membership test — walk limbs 2 and 4 per row over all sixteen members](https://github.com/winniel123/verge-asm/issues/107) — **run as a retrieval, fifteen of sixteen hold and `1433/tcp` fails on the limb nobody had walked.** [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §33; [ADR-0059](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0059-a-footing-tier-grades-evidential-distance-never-the-owners-conviction.md) and [ADR-0050](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0050-an-owners-category-statement-reaches-the-members-its-own-artefacts-place-inside-it.md) **confirmed by use**, **no ADR minted, 0064 unused**. **[measured]** the carrying page contains `1433` **zero** times, so reach runs through ADR-0050 — and limb 3's defeat test is met: Microsoft's own current docs name internet-facing **TCP/1433 SQL Server** a portal-provisioned supported option (*"Any client with internet access can connect to the SQL Server instance"*). Limb 4(c) fails on the same measurement; either suffices. The cell leaves the **graded table**, not the top tier — §13.1 records MS SQL ships no configuration artefact. Tiers **16 / 11 / 3 → 15 / 11 / 3 + 1 uncovered**, coverage **30 → 29 of 41**, **one cell, no rows**, cost **nothing**. **§18.7 armed this trigger two passes ago and named this cell.** The losing option was *"directly excludes a filtered exposure"* — refused because the rule Microsoft writes *is* *allow 1433 inbound from the internet*, and because it would make the sentence forbid nothing; **flagged as thin ground**. **The discriminator is this section's own act and is flagged**: a defeat bites only where the supported architecture is an *instance of the class the owner's statement forbids* — which spares Elastic (conditioned on *"unprotected"*, managed 9200 is a **proxy** port), Redis (**[measured]** `6379` occurs **zero** times in the owner's port table; endpoints are `10000-19999`), memcached (no service; SASL *"isn't suitable for using over the internet"*) and MySQL (conditioned on *"untrusted hosts"*). **§32.12's two named weaknesses both resolve**: `2181`'s limb 4 becomes a retrieval (the owner files it under *Things to Avoid*) so the `6379` coupling is withdrawn, and **direction gets its first live instance** — `445/tcp` turns on it and is spared only by direction and addressee. `139`/`137`/`138` re-founded on **enumeration**: Microsoft's table names them **NetBIOS**, not SMB. Opens [#109](https://github.com/winniel123/verge-asm/issues/109) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**) and [#110](https://github.com/winniel123/verge-asm/issues/110)


- [Which `Name`s does `wildcard-discrimination` generate control labels under, and is that population a declared parameter or an aperture input?](https://github.com/winniel123/verge-asm/issues/108) — **a name is discriminated at its *parent*, and that population is the seventh aperture input rather than a declared parameter.** [ADR-0066](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0066-a-control-probe-is-generated-under-a-names-parent-and-that-population-is-aperture.md); [`passive-discovery-sources.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/passive-discovery-sources.md) §3.2 **rewritten in place**, steps 1 and 3 struck at the clause. The population is *the immediate parents of the `Name`s in the batch's resolution scope*, recorded on the `Batch` **by content** — and the depth question **dissolves**, because a control label under P has the same closest encloser as any name under P, so it finds the wildcard at any depth and **whether or not P exists or is held**. **[measured]** on §7's own `%.iana.org` estate, 25 names → **6** parents of which **exactly one is a zone cut**, so *apex + each measured zone cut* — **the losing option** — probes the same single site as apex-only and leaves **11 of 25 (44%)** undiscriminated, while the parent set covers **25 of 25** from 6 sites against 25 for *every `Name`*, 19 of which are leaves; and `render.com`'s apex control label is **NXDOMAIN** while `*.staging.render.com` answers `10.4.3.1` at every depth. A parameter lost because the population is a **function of the batch's scope** — filed there it bumps the leaf on every newly-discovered subdomain and `Break`s the name half of the estate, ADR-0021's unsurvivable fallback inside one leaf. Two riders, both `{161}`: the probe runs the **declared seven-qtype set** (three left `Shadowed` unemittable on MX/TXT/NS/SOA), and **an undiscriminated answer is never a value** — an incomplete probe yields a **`Gap`**, not a value, and `resolution`'s value space does not widen. The widening is **structurally vacuous**, a theorem of the rule's shape. **Aperture count 6 → 7**, and **[measured]** **nine stale sites over seven ADRs carrying three different counts** are struck at the clause, **six of them already stale before this ruling**. [ADR-0062](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0062-a-wildcards-synthesis-is-a-fact-about-the-name-it-was-probed-under.md)'s blocker **discharged** — but its *"we do not probe under names we do not hold"* is **falsified by measurement** (`ns.iana.org` is NXDOMAIN, in no `Citation`, and a necessary probe site), leaving the facet one new open item. Thin ground flagged: the under-selection limb is `[spec]` not `[measured]`, and the 4.2× reduction is one estate and not a rate. Cost **zero**. Opens [#111](https://github.com/winniel123/verge-asm/issues/111) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [`1433/tcp`'s footing is gone and §10.3's failure condition is met — does the row survive?](https://github.com/winniel123/verge-asm/issues/109) — **a claim fails on the owner's *affirmative naming*, never on the reach of the owner's own prohibition — so a reading that narrows the prohibition cannot rescue the row, it only leaves the naming unopposed.** §10.3's failure condition and [ADR-0050](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0050-an-owners-category-statement-reaches-the-members-its-own-artefacts-place-inside-it.md) limb 3's defeat test are two propositions sharing an artefact, read at the **claim** gate and the **attestation** gate — [ADR-0054](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0054-a-claim-step-is-answered-only-by-evidence-about-that-step.md) moved up one level, and §34.3 limb (c) from the other side. **`1433/tcp` MS SQL is REMOVED and the list is 40 pairs, classes `12 / 7 / 21`** — the note's second row removal after `161/udp`, and the first to leave by losing a claim it held — [ADR-0067](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0067-a-claim-fails-on-the-owners-affirmative-naming-not-on-the-reach-of-its-own-prohibition.md), [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §35; ADR-0050, ADR-0054, ADR-0059 and ADR-0037 **confirmed by use**, **0063/0064 stay gaps**. **[measured]** on a retrieval **scoped to the row** (#37) over nine Microsoft artefacts: *"select **Public (internet)** to allow connections to SQL Server from machines or services on the internet"* (`ms.date` **2026-03-18**), *"Any client with internet access can connect to the SQL Server instance"*, and the portal opens the VM firewall and the NSG on 1433. **The losing option is not the one the ticket named** — it is that an `applies to` banner makes the Azure pages §10.5's *distributor* position; refused on four grounds, the load-bearing one being that **Microsoft does not run that SQL Server**, so §33.2's addressee rider (which spares `445`) does not reach it. **Two of the map's own figures are corrected: `verge-core` does NOT move** — **[measured]** `1433/tcp` is in the frequency half, so the union stays at **136 pairs / 131 probed** and the port is still probed daily — **and a removal narrows the aperture rather than widening it**, a denominator whose numerator is `0` for every `|S|`. §4.6 gains a **21st** exclusion worded from Microsoft's sentence on arrival, §6.1 reads `27 + 8 + 5 = 40`, §2.2's uncovered-in-subject returns to **0** at coverage **29 of 40**, and **ADR-0008 is triggered** for the first time in the removing direction. Opens [#112](https://github.com/winniel123/verge-asm/issues/112) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**)
- [Is direction a fifth limb of the prohibition-tier conjunction, or a qualifier inside limb 2?](https://github.com/winniel123/verge-asm/issues/110) — **direction is not a fifth limb; it is what limb 2's *"this `(port, transport)` pair"* denotes**, the pair *as the endpoint being reached, on the estate the statement addresses*, and limb 2 is restated at its own clause. **Addressee is that limb's second coordinate**, not a sixth candidate: at the one member where it decides anything it is the same disposal as direction read from the other end of one TCP connection. The conjunction stays at **four** and §32.2's closure over the **kinds** of gap is confirmed; what was incomplete was limb 2's **denotation** — [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §36, [ADR-0059](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0059-a-footing-tier-grades-evidential-distance-never-the-owners-conviction.md) **amended in place, no ADR minted, ~~0068 unused~~ — 0068 was reserved again for [#111](https://github.com/winniel123/verge-asm/issues/111) and *taken*, so it is no longer a gap**. **[measured] the conjunction as written *was* defective**: §32.12's hypothetical outbound sentence is a **section heading** in the document carrying `445/tcp`'s footing — *"## Block outbound SMB access — Block TCP port 445 outbound to the internet at your corporate firewall"* (`ms.date` 2024-10-25) — satisfying all four limbs and entailing nothing about the row; the note had quoted the page's **bidirectional** directive in full since §3.4 without noticing half of it is egress. **[measured] direction and addressee run as candidate limbs over all 26 members of both graded tiers: the *owner* supplies the direction in 24** — six in the word, three in the verb's preposition, fifteen in the verb — **and the two silent members already fail limb 3, so the reader supplies a directional premise nowhere and the zero-premise arithmetic holds.** **Two owners write it in a column headed `Direction`**: Kubernetes for `10250`/`10259`/`10257`, and **HPE for `623/udp`** — *"`IPMI/DCMI over LAN port | 623 | UDP | Inbound⁴`"* against *"`Remote support port | 7906 | TCP | Outbound¹`"*, footnotes defining the initiator on both. **Two verdicts turn on direction** (`445/tcp`; and **`623/udp` at §28.9, which no prior section counted — so §32.12's *"no member turns on direction today"* was false when written, not merely after #107, and #107's *"refused fourteen times"* hid it by counting outcomes rather than grounds**) **and one on addressee** (`445/tcp`). The losing option was **restating the closure as *"closed over the terms the note has rules for"*** — refused because it *cannot be wrong*, withdrawing the closure's content while keeping its word and licensing the next session to put any inconvenient candidate outside it; **a fifth limb** lost on the term-versus-argument-place test and on being satisfied by the owner's own words in **every** live member. **[measured] the named falsifier was retrieved and is not met**: MongoDB Atlas is an **inbound** internet-reachable listener on `27017` that MongoDB scopes away from its own trusted-networks guidance **by page title**, so addressee genuinely comes apart from direction — and it is not load-bearing, `27017` failing limb 3 a limb earlier. §28.9's instance is **re-founded on a current artefact**, HPE having retired *Insight Online direct connect*; §33.11's *"the string `6379` occurs zero times"* is **corrected at its clause** to a token claim (it occurs once, inside `36379`) without moving the spare it carries. **Nine stale sites struck at their clauses** across two files per [ADR-0058](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0058-a-superseded-mechanism-is-withdrawn-at-the-site-that-specifies-it.md) as widened by #106 — **including ADR-0059's own recital of the clause §33 had withdrawn in the note**, the intra-document defect #106 widened the rule to catch, found in the same file one section later. **The empty delta: nothing moves, and the cost is nothing.**

- **Merge note, 2026-08-14 (second batch of the day) — [#108](https://github.com/winniel123/verge-asm/issues/108), [#109](https://github.com/winniel123/verge-asm/issues/109) and [#110](https://github.com/winniel123/verge-asm/issues/110) ran concurrently from `main` at `94ff868` and were merged serially afterwards.** Numbers were **pre-assigned per agent** again — ADR-0066/0067/0068 and §35/§36 reserved — and again nothing collided: **0068 went unused** ~~and joins the gaps alongside 0039, 0041, 0052, 0053, 0057, 0063 and 0064~~ — **struck at the clause: 0068 was re-reserved for [#111](https://github.com/winniel123/verge-asm/issues/111) in the next batch and taken, so the gap list is 0039, 0041, 0052, 0053, 0057, 0063, 0064.** *An unused reservation is a gap only until it is re-issued* — the third batch's own instance of the stale-absolute shape this note was written to record. Only #109 moved a list figure, so the composed state is **40 pairs · classes `12 / 7 / 21` · tiers 15 / 11 / 3 · uncovered-in-subject 0 · coverage 29 of 40 · §6.1 `27 + 8 + 5 = 40` · §4.6 21 exclusions**, with **`verge-core` unchanged at 136 / 131 probed** and the **aperture input count 6 → 7** from #108. One merge conflict, in `sensitive-ports.md`, of the same both-sides-appended shape as the last batch — the §1 summary table plus the section body and the Sources appendix — resolved by keeping both sides **in section order** rather than accepting git's interleave. **The reconciliation then found three stale absolutes that no pass's own sweep had reached** and struck them at their clauses: ADR-0011's decision-table row still read *"Aperture inputs | Now **five**"* while ADR-0066's sweep had repaired that file's prose clause **313 lines below it** — the intra-document shape [#106](https://github.com/winniel123/verge-asm/issues/106) widened ADR-0058 to catch, recurring inside the very batch that widened it; ADR-0031 read *"two of the six enumerated aperture inputs"* and was outside ADR-0066's stated sweep list entirely; and ADR-0032 read *"The list is **41 pairs**"* with no date qualifier. ADR-0061's 41-pair recital was **left standing** — it carries an explicit *figures as of `c0881ae`* parenthetical, so it is dated rather than present-tense, which is the discriminator. **The lesson for the next fan-out is that a per-agent sweep is scoped to what that agent touched, and the composed corpus is nobody's scope — so the reconciliation owns the summary rows.**
- [Send the four registry asks and the two defect reports, or record that they will not be sent](https://github.com/winniel123/verge-asm/issues/59) — **the dev ruled on 2026-08-14 that none of the six will be sent, discharging the debt by the second branch: *recorded as deliberately not sent*.** [`docs/correspondence/README.md`](https://github.com/winniel123/verge-asm/blob/main/docs/correspondence/README.md) carries the decision, its cost and its reopening condition; [ADR-0003](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0003-third-party-source-consent-bar.md)'s ambiguity-corollary debt is **amended at the clause** per ADR-0058 and its *"has not been shown to have been followed"* withdrawn. **Nothing moves and nothing regresses** — no source changes ship state, [#34](https://github.com/winniel123/verge-asm/issues/34)'s five-registry table stands, ambiguous terms ship off indefinitely either way, and [#57](https://github.com/winniel123/verge-asm/issues/57)'s wording (*no reply has ever come, and no record of an approach exists*) was built to be true under exactly this outcome and is now true **and settled**. Cost stated rather than glossed: two regions keep no keyless fallback and LACNIC's one-sentence route to `unencumbered` is **forgone, not lost**. The two defect reports are dispositioned with the asks because the ticket closes as a unit, but are **separable and separately reversible** and need no ticket to send later. **Reopens on sending, never on elapsed time.** **The map's one un-runnable ticket is now closed, and the frontier is entirely agent-runnable again.** [measured] **the mailbox branch had been closed before this session touched it** — the search ran on 2026-08-14 over all folders with no date bound and returned **zero** registry-scoped threads against a ~201-thread control, and the result was recorded in the README rather than on the ticket, so a later session read the ticket's stale step 3 and re-ran it: **ADR-0058's shape one surface further out than [#102](https://github.com/winniel123/verge-asm/issues/102) or [#106](https://github.com/winniel123/verge-asm/issues/106) swept, the issue tracker being in neither's corpus.** Reported, not ticketed, on [#93](https://github.com/winniel123/verge-asm/issues/93)'s cost reasoning
- [What is `wildcard-discrimination`'s match predicate, given that a synthesised answer set is not stable across control labels?](https://github.com/winniel123/verge-asm/issues/111) — **a wildcard is discriminated only where its synthesis is *determinate*, and determinacy is measured per `(qtype asked, RR type answered)` component.** Per component the signature is `NoSynthesis` │ `Determinate(RRset)` │ `Indeterminate`; a candidate is discriminated iff it differs at some **determinate** component and `Shadowed` otherwise; an **indeterminate component is never consulted**, and where no component is determinate every name beneath that parent is `Shadowed`, §3.2 step 5's escape hatch **withdrawn** there — [ADR-0068](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0068-a-wildcard-is-discriminated-only-where-its-synthesis-is-determinate.md), [`passive-discovery-sources.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/passive-discovery-sources.md) §11; ADR-0021, ADR-0062 and ADR-0066 amended, `CONTEXT.md` and `measurement-offers.md` carried. The predicate stays a **declared parameter** and now has a value, while the **determinacy verdict** is neither parameter nor aperture but a **measured in-batch fact** — so the **aperture count stays at seven**, `resolution`'s value space does not move and there is **no `Break`**. **[measured]** the forcing measurement went past the ticket's own: the answer is **not a function of the label at all** — four repeats of *one* label direct to `ns01.herokudns.net` returned four different ingress nodes with four **disjoint** address sets — so the rotation is the authority's and *the* answer set was never an object. **The losing option is the ticket's own leading candidate**, non-empty intersection with the union: on `herokuapp.com`'s pairwise-disjoint blocks it is *literally the same predicate* as set equality (**7 of 25** both ways, leaving **72%** of fictional labels `Resolved`) while catching **25 of 25** on `vercel.com` — a predicate whose error rate is a property of the provider's load balancer and **unknowable in band**. Runner-up loser: *the CNAME target instead of the address set*, measured half-right with the wrong half — `s3.amazonaws.com` holds still, `herokuapp.com` rotates over eight targets. **`wildcard-synthesis`'s deferred value space is settled at three members**, so ADR-0062's second open item closes. **Thin ground flagged**: determinacy is evidence, not proof, and every indeterminate zone measured is a **provider's** rather than a small org's. Opens [#113](https://github.com/winniel123/verge-asm/issues/113) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**) — a synthesis can be a function of the label's *content*, which a random label cannot detect
- [ADR-0067 limb 2, swept across the list — does a deployment-scoped owner document move any other row?](https://github.com/winniel123/verge-asm/issues/112) — **a rider on §33.2's discriminator travels to the claim gate only if it bounds the *affirmation*; conditioning of the *prohibition* stops at the attestation gate.** Addressee and direction travel, a bound on the affirmation travels, conditioning does not — carrying it across is ADR-0067 limb 1 run backwards — [`sensitive-ports.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/sensitive-ports.md) §37, [ADR-0067](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0067-a-claim-fails-on-the-owners-affirmative-naming-not-on-the-reach-of-its-own-prohibition.md) **amended in place at limbs 1 and 2, no ADR minted, 0069 unused**. **The sweep's population is 32, not 26** — limb 2's row-moving power runs through §10.3, whose population is **Class C**, so the six rows outside both graded tiers (`5432`, `5984`, `10248`, `139`, `137`, `138`) are in it and the tiers are the wrong cut. **[measured] thirty hold and two do not**: `9200/tcp` and `9300/tcp` **meet §10.3's failure condition** on Elastic's own ECE documentation addressed to the operator — *"By default, all your deployments are accessible over the public internet"*, with Elastic's inbound table numbering both pairs under the heading **"Inbound traffic from any source"**, and ECK's unwarned section heading *"Allow public access"* over the node's own `9200`. **Routed, never removed** — a tier-scoped sweep may not move a row (#37) — so **nothing moves and the delta is empty**, and the successor is [#114](https://github.com/winniel123/verge-asm/issues/114) (**blocking [#12](https://github.com/winniel123/verge-asm/issues/12)**). **§33 spared `9200`/`9300` on the rider that does not travel and `3306` on the rider that does**, and their **cells hold** — limb 4 is asked of the *carrying* statement, whose *"unprotected node"* class has no supported instance — **the inverse of #107's shape: a footing surviving on a rider the claim gate does not read.** The two instruments are **not one stated twice**: they share the coordinate *whose listener* and partition its values, but the rider decides `445` where limb 2's other grounds are satisfied while limb 2 decides `1433` against a **banner** argument the rider is silent on. **Thin ground flagged**: ECE is strong on the network limb and weak on modality, ECK the reverse, neither strong on both. **§36.5's falsifier is met one gate over** — addressee is load-bearing at `27017`/`27018`/`27019` at the **claim** gate, limb 3 not existing there — qualified at its clause, §32.2's closure untouched. **[measured] two retrieval hazards, both new**: a summariser **fabricated** the clause *"When exposing MongoDB to the public internet…"*, which would have removed three rows on a sentence no owner wrote; and a **line-wrapped** string produced a false negative that would have collapsed `2049`'s spare, caught against 1,593,486 raw bytes of RFC 8881

- **Merge note, 2026-08-14 (third batch of the day) — [#111](https://github.com/winniel123/verge-asm/issues/111) and [#112](https://github.com/winniel123/verge-asm/issues/112) ran concurrently from `main` at `d3e78de` and were merged serially afterwards.** Numbers were **pre-assigned per agent** for the third batch running — ADR-0068/0069 and §11 (`passive-discovery-sources.md`) / §37 (`sensitive-ports.md`) reserved — and this time the branches had **zero file overlap**, so the merge was clean with no conflict and no hand-splicing: the two tickets ran in different corpora, which is the cheapest fan-out shape there is and worth selecting for. ~~**0069 went unused** and joins the gaps.~~ **Struck by [#113](https://github.com/winniel123/verge-asm/issues/113), which took 0069** — this note's own lesson (*an unused reservation is a gap only until it is re-issued*) firing one batch after it was written, on this very sentence. **The batch's own lesson is about the *previous* batch's bookkeeping**: #110's merge note recorded ADR-0068 as joining the permanent gap list, and #111 was then handed 0068 and took it — so **an unused reservation is a gap only until it is re-issued**, and both sites saying otherwise were struck at their clauses. The composed state is **40 pairs · classes `12 / 7 / 21` · tiers 15 / 11 / 3 · coverage 29 of 40 · aperture inputs 7**, all unchanged: **#111's delta is a value space and #112's is empty**, so for the first time in three batches no summary row moved and the reconciliation had nothing to reconcile. Both tickets **routed rather than ruled** at their edges — #111 opening [#113](https://github.com/winniel123/verge-asm/issues/113) on the one door neither it nor #108 closed, #112 opening [#114](https://github.com/winniel123/verge-asm/issues/114) under [#37](https://github.com/winniel123/verge-asm/issues/37)'s bar on a tier-scoped sweep moving a row. **Both new tickets block [#12](https://github.com/winniel123/verge-asm/issues/12), so the assembly ticket has gained two blockers by resolving two** — the third consecutive batch to do so, and the honest read is that the sensitive-list and wildcard threads are each still generating successors faster than they retire them
- [Is a random control label the right construction, given that a synthesis can be a function of the label?](https://github.com/winniel123/verge-asm/issues/113) — **a control label is one label, and the set must be able to falsify label-independence**: 5 random + 1 structured, the structured one a hyphenated quad over a random RFC 5737 address. **Both halves are refusals of the ticket's own candidates.** The dotted quad loses on ADR-0066's warrant rather than on taste — a multi-label control label falls off the tree at a *deeper* encloser than the candidates do, and **[measured]** it manufactures a wildcard on **3 of 5** un-wildcarded zones off a **third party's** subtree (`1.pages.dev`). A candidate-shaped label loses as unaffordable *and* circular. Hex and IPv6 shapes lose as **[measured]** redundant — 3 of 3 parsers decode hyphenated and 0 of 3 decode another family without it. ~~And **raising the count above 5 loses on evidence pointing the other way**: ten random labels under `surge.sh` saw *one* answer where five saw *two*, the mechanism unisolated — ADR-0068's own objection to intersection-with-the-union, reused.~~ **REVERSED by [#115](https://github.com/winniel123/verge-asm/issues/115), which the refusal itself asked for: the count is 9 random, and `surge.sh` is not unstable but a two-member hash of the label. The ten-labels-saw-one run was a ~0.2% tail draw that 30 fresh trials did not reproduce, and its label generator was never recorded — so the evidence the refusal rested on was one unreproduced sample.** A footing tier for `Determinate` is refused four ways, the first being that its motivating instance stops being determinate. **Aperture stays 7, no value space moves, cost zero.** Two corrections against our own prior work: ADR-0068's base rate is **8 of 14**, not 10, and §6.8's `Product?` column answers *is a world quantity in here* rather than *does this have a value* — which is how a **range** and an unspecified construction sat in a `No` cell through two rulings. Residue ticketed as [#115](https://github.com/winniel123/verge-asm/issues/115), which **does not block [#12](https://github.com/winniel123/verge-asm/issues/12)**

- **Merge note, 2026-08-15 — [#113](https://github.com/winniel123/verge-asm/issues/113) ran alone from `main` at `53b7ef4` and merged clean at `5ffe61c`.** No concurrency, so no pre-assignment and nothing to reconcile. It took **ADR-0069**, the number [#112](https://github.com/winniel123/verge-asm/issues/112) reserved and did not use — and the previous merge note's *0069 joins the gaps* clause is **struck above**, that note's own lesson (*an unused reservation is a gap only until it is re-issued*) firing one batch later on the sentence that stated it. **The three-batch trend of gaining blockers by resolving them is broken**: #113 retired a blocker of #12 and its successor #115 deliberately does **not** block, the parameter having a value and the open question being only whether that value can be improved. **#12 is now blocked by [#114](https://github.com/winniel123/verge-asm/issues/114) alone.** The composed state is **40 pairs · classes `12 / 7 / 21` · tiers 15 / 11 / 3 · coverage 29 of 40 · aperture inputs 7**, all unchanged — this ticket's delta is a **declared parameter's value**, and no summary row moves
- [Do 9200/tcp and 9300/tcp survive — Elastic Cloud Enterprise names the public internet for the pair, and #112 could only route it](https://github.com/winniel123/verge-asm/issues/114) — **they do not; both rows are removed**, the note's first two-row removal and its third and fourth overall. Decided **per pair on different evidence**, which is the finding: `9200` on the **self-managed** corpus (an *affirmation*, so #112's *"ECE describes a default"* counter is never reached), `9300` on **ECE alone** through ADR-0050 limb 2 — the self-managed corpus points the *other* way for `9300`, its next bullet naming the transport layer's clients as nodes. Claim 1 and Claim 2 **measured** at `v9.5.1` and both unavailable. The re-founding of the footing cell was ruled on and is moot — *a cell cannot outlive its row*. **ADR-0067 applied, not extended; no ADR minted and ADR-0070 left unused.** New state **38 pairs · `12 / 7 / 19` · tiers 13 / 11 / 3 · coverage 27 of 38**; **`verge-core` unchanged at 136**, both pairs being in the frequency half, and **one ADR-0008 bump for two rows** — the price is a property of the content moving, not of how much moved. **Thin, and thin toward costing a row: `9300` rests on one corpus**; a reader requiring the failure condition in the pair's *primary* corpus keeps it and the list is 39, stated at full strength at §38.10

- **Merge note, 2026-08-15 (second batch) — [#114](https://github.com/winniel123/verge-asm/issues/114) and [#115](https://github.com/winniel123/verge-asm/issues/115) ran concurrently from `main` at `5ffe61c` in separate worktrees, ADR numbers pre-assigned 0070 / 0071 and section numbers §38 / §13.** Zero file overlap again — different corpora, the shape the previous note recommended selecting for — so #114 merged clean at `f1aea02`. **The reconciliation found one defect and it was the intra-document shape**: ADR-0009's #114 amendment section did not discharge the composed absolutes stated in its own #109 section further up the file, every *other* figure site chaining correctly (ADR-0032, 0050, 0054, 0059, 0067, §1). Struck at the site in `244d372` — [#106](https://github.com/winniel123/verge-asm/issues/106)'s rule that ADR-0058 is on the **sentence**, firing again. **`9200`'s ruling also corrects §37**: its ECE quotation *"assuming that your ECE proxies are accessible"* occurs **zero** times in the current issued corpus — legacy [3.8] wording, the live text being weaker — which is [ADR-0045](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0045-an-owners-documentation-is-what-it-has-issued.md)'s per-version issuance rule biting a *quotation* rather than a document
- [Is a wildcard authority's answer instability per-label, per-query or per-time?](https://github.com/winniel123/verge-asm/issues/115) — **per-query on two authorities, per-label on two, per-time on none**, measured direct-to-authority with the resolver cache out of the path. **It reverses [#113](https://github.com/winniel123/verge-asm/issues/113)'s count refusal, which is what #113 filed it to do**: draws *are* independent samples, `surge.sh` is a **two-member hash of the label** (188/172 over 360 labels) rather than an unstable zone, and #113's *ten labels saw one answer* was a ~0.2% tail draw that **30 fresh trials did not reproduce**. False-`Determinate` rate is monotone in the count — 13.3% at 4 draws, **6.7% at #113's 6**, 0/30 at 8+ — so the count goes to **9 random + 1 structured**, chosen to hold below 1% across the whole CI of the measured split. **Spacing** lost (no zone needs it, and its value is unbounded); **repeating one label** lost on a measurement — over a resolver 8 repeats are **one cache entry** at a 1800 s TTL where 8 distinct labels are eight. `Determinate` gets **no shelf life**: ADR-0011's batch confinement already supplies one and is now stated as load-bearing. **ADR-0069 amended in place rather than minting ADR-0071** — a new ADR would have restated the construction, ADR-0058's two-places defect. Also corrects ADR-0068: **`s3.amazonaws.com` is not a fifth mechanism** but a **resolver splice** — its authority returns only the CNAME and answers REFUSED for the delegation the eight rotating addresses live in. Base rate **8 of 14**, and the four summary rows that had desynced from the table are fixed. **Thin: `surge.sh` carries the whole count argument, 1 of 14 zones**; per-time is **unfalsified, not disproved** (36 minutes, one day); and the count can never reach a rare second answer — a 5%-share member is missed 60% of the time at ten draws, a **permanent** boundary

- **Merge note, 2026-08-15 (second batch, concluded) — [#115](https://github.com/winniel123/verge-asm/issues/115) merged at `faa31ef`, again with zero file overlap against #114.** Both agents **declined their reserved ADR numbers** — 0070 and 0071 are unused and, per this map's own standing lesson, are gaps only until re-issued. **Both rulings were amendments rather than mints**, which is the third batch running where the corpus absorbed a decision without growing an ADR. **The batch's real lesson is about #113**: it refused a count on one unreproduced sample, flagged that refusal as thin, and **ticketed the residue that overturned it within a day** — the flag did the work it exists to do, and the cost of being wrong was one ticket rather than a shipped parameter. **#12 was unblocked for a matter of minutes** and is blocked again by [#116](https://github.com/winniel123/verge-asm/issues/116): #115's own residue is that **the probe's vantage is an undeclared parameter**, ADR-0021's table being *the authoritative population*, and #113's precedent puts a hole in that table in front of the assembly ticket. The wildcard thread has now produced a successor at every one of its four resolutions

## Not yet specified

<!-- The open questions, and only what a session must not re-derive. Findings appended here by
     tickets that have since closed are reduced to pointers — the detail is in those tickets.
     Compacted 2026-08-14, the third of three compactions that day; the third re-cut the eight
     patches that had grown by accretion, one of which had reached 12,191 characters because every
     ticket appended its findings to the same bullet. The body before each pass is at
     docs/wayfinder/map-archive-2026-08-14{,b,c}.md. -->

- **Finding lifecycle** — triage, suppression, accepted-risk states. Reduced to one question: **is operator opinion worth a Declared-layer term at all, or does it collapse into the signal-suppression case?** There is no stored `Finding` to carry a lifecycle (#7); removing a subject belongs to `Seed`, and doing it by opinion is #22's refused suppression under another name (#17); and `Span`s are immutable, so `acknowledged`/`triaged` on a drift record rebuilds `Finding` with better provenance (#8). That leaves `Annotation` holding exactly one job — *accepted risk on a thing we are still measuring*
- **How deep the model goes on `third-party` addresses** — [ADR-0002](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0002-ownership-gates-probing.md) stops us probing them, but they still serve the operator's names and every facet that would populate them is measured by probing. Since #40 deleted `unknown`, `third-party` is **everything not covered by a `Seed`** — for a cloud-resident operator who leaves the extension off, the entire estate — so the question is **what the product shows an install that is honest about holding custody of nothing.** Four constraints have landed: the flagship rule renders as a **no-population panel**, prose with no numerals and no `Enable` button (#44); its commoner producer is an empty `Predicate domain` the estate produced rather than the operator, so the copy must state the **measured** reason (#53); an address whose gate just closed keeps its last measured value for up to `k` cadences and #42's safety argument *depends* on a current `third-party` rendering beside it — **nobody has drawn that cell, and it is the only remaining carrier** (#51); and **an exclusion is load-bearing only where a confirmed scope would otherwise cover it** (#50)
- **Notification and alert channels for drift** — the flagship internet `Reach` `not-reached` → `reached` alert is the whole programmatic-integration story, since #6 ruled a pull API out of scope. Fog: **the channel question, and whether one operator-facing vocabulary can carry all of it.** Settled, and not to be re-derived — the detail is in the tickets named. **Four causes, and the classes partition *messages*, not events**: *the world moved* · *we stopped looking* · *we changed how we look* (two triggers — an aperture widening and #18's re-baseline — whose payloads differ and must not be levelled) · *a clock crossed*. **The coverage class has nine members** — `resolving → shadowed`, `revealed`, a closing custody gate, a `Derivation` vector move, `fired → not-evaluable` (#32, never worded as a clear), a lame delegation (#35), a `Gap` closing (#42, never silent), a declared timeline ageing into a `Gap` (#48), and a live `custody extension` gaining an address (#55, the only member caused by neither our act nor the operator's). **The clock class has three members** (#60), is **`certificate-expiring`'s only carrier** so may never be folded into the drift class, and its stale-fact window is `min(k × cadence, N)` (#61, re-bounded by [#77](https://github.com/winniel123/verge-asm/issues/77)) — *two days modally* survives on the hot tier, while on the **cold** tier `not-evaluable` may become the class's largest census member. **All flap suppression lives here**, since the model damps nothing by design: the ACME case (#60) and **three unmeasured volumes** — preview-environment `Name` `appeared`, the re-point on a churning CDN edge set, and `NoHTTPResponse` → `Responded` on an internal estate — whose remedy if they drown the channel is **coalescing here, never a predicate change**. **Alerting fires at the cause, never per affected subject.** **A clear is not always good news**: four of sixteen signal clears are messages (#35, #48). **The census payload has five producers**, so one shape carries five messages and **five wording pairs must be written** (#28/#22, #55/#51, and #63/#64/#65's three, which can land on one subject in one fold and must not read as duplicates). #63's *incremental per `Batch`, or wait for a set of tiers?* **does not arise on the default path** — [#80](https://github.com/winniel123/verge-asm/issues/80) measured one tier — and survives only for an install that has enabled the full-range tier
- **Data retention and history depth** — **three corpora, not one**: observations, the operational record (where an aged-out `Dispatch` may be the only evidence a believed-in measurement never happened), and `Span`s, which are materialised Derived history that **may never be recomputed**. **Hard floor**: the open `Span` and the one before it can never be compacted, or ageing out history silently converts `returned` into `appeared` — and since the floor reads *within one derivation*, a `Break` inside the retained window makes `returned` detection **unrecoverable**. Retention is therefore partly a **release** question. Volume is settled and sizeable, and the arithmetic is in #36, #56, #61 and #62: `dns-record` multiplies by seven qtypes, `certificate` keys on `Endpoint` so every open `Service` is the floor, and #49 adds a break cause we do not control on a **dependency** cadence. **[#81](https://github.com/winniel123/verge-asm/issues/81) re-sizes the premise**: one declared `/22` is 1,024 addresses, 143,360 `Service` subjects and ~52M rows/year — **2× [ADR-0001](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0001-stack-and-runtime.md)'s entire sized estate from one line of typing**, and [#85](https://github.com/winniel123/verge-asm/issues/85) makes that the **ceiling** per scope in either family. Size retention against **declared scope size**, the one input the operator sets directly
- **The `Subjects` screen** — #10 chose the exposure board over the inventory, making *what do I own* a second-position screen. Current membership is a **lookup** (the open `Span`), so the fog is **which populations render alongside the living, and which can be listed without a comparison at all.** The absence vocabulary is **six populations read off one record**, since every `Gap` states its cause (#42): withdrawn · `Shadowed` · *we never looked* · *we stopped looking* · *you stopped answering* (#35, the only one carrying an operator action) · a closed custody gate. Two presentational obligations: **every duration and count must be able to declare itself a floor** (#18, worked in #28), and **a value arriving after a `Gap` cannot be dated**. Two warnings: rendering the custody extension here as a `custody: operator` filter is the obvious implementation and is **wrong** — the rows are right, the screen is not (#51) — and this screen must never acquire **per-row controls**, or it is ADR-0002's rejected approval queue under another name. **[#74](https://github.com/winniel123/verge-asm/issues/74) adds a second question nobody has asked**: for the three rules whose evidence is constant across a member, a census drill-down is a list of bare subjects, **visually indistinguishable from an inventory**. #74 rules the two may not be the same object, but nobody has drawn them side by side — so, what distinguishes a rule's member list from `Subjects` filtered by that rule
- **The `Seeds` screen** — it appears in the navigation of #28's prototype and nowhere else, and **no ticket has drawn it**. Required rather than convenient, with at least six jobs beyond listing: the two `Seed` kinds, **exclusions** (names, subtrees, and address scopes since [ADR-0012](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0012-a-proposer-is-not-a-source.md)), retained `Proposal` provenance, the `custody extension` declaration surface, #50's *what answered* panel, and the range cap as a **declaration-time validation** (#81). #51 put a **load-bearing safety surface** here — the declaration and its current extension are [ADR-0013](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0013-custody-is-control-and-extends-by-declaration.md) §7's carriers for the apex-flattening hazard, now three after #55, and they only work together. Not sharp because *what an exclusion looks like* has never been drawn. Easy to break: **two tables that look alike ship here at once** — a `Proposal` list legitimately *is* an approval queue, while the extension's census must carry **no per-row control and no nav badge**; the axis is *whose claim*, not *whose addresses*. With the cap comes a shape this screen does not have: a **`/128` scope naming a prober's own address**, the only legal way a v6-only internal `Vantage` verifies `internal` (#85). **The weight has landed on refusal copy, and there are now three refusals with three different right answers.** (1) An **over-cap IPv6 declaration** is refused by the same family-blind validation as a typo'd `/8`, but for IPv4 *raise the knob* is correct advice and for IPv6 it is a **trap** — a knob-raised `/64` is admissible, never completable, and pins `Coverage` near zero forever — so an IPv6 refusal must name the `custody extension` as the route and **must not offer the knob** ([#85](https://github.com/winniel123/verge-asm/issues/85), whose whole residue lands here as copy, no knob ceiling having been invented). (2) A typed **U-label** is refused because UTS-46 is reference data a key may not consult, and the copy names the **A-label** as the route ([#94](https://github.com/winniel123/verge-asm/issues/94)); the legal future conversion is the `Proposal` pattern — the box offers, the operator declares — and is named so nobody builds the illegal one. (3) A typed **`*.example.com`** is refused because a wildcard denotes a set rather than a name and is a `Subject` from no source ([#99](https://github.com/winniel123/verge-asm/issues/99)), and the copy names the **subtree exclusion** as the route — **but the two are not the same object and must never be silently translated**, since a subtree exclusion runs on label-wise suffix equality over the key and therefore **includes** `example.com`, while `*.example.com` expressly excludes it (RFC 6125 §6.4.3 rule 2). The pattern across all three is that the refusal names a route rather than the reason, and **only one of them may be auto-corrected — none**
- **Packaging, default configuration, and the container/secret split** — the UI half is fully graduated; what remains is the `GOARCH` matrix for `linux/amd64` and `linux/arm64` (#6), declaring vantage intent and the optional prober's SSH setup, and **which container receives which secret** (`web`: session key + DB creds; `worker`: DB creds + SSH key + zone file). Settled elsewhere: the day-one checklist is the zero-coverage rendering of `Coverage` at two densities, not a wizard (#22 into #28); the per-region reason ships with **no figure attached** (#47, #57); and **the first-run line is *declare your domain*** (#51), superseding the registry-path versions in #26, #27 and #38. Two live obligations: **#48 makes the zone file more than a mount** — two signals read it and assume it is **re-read on a cadence**, so a file mounted once is stale-invisible rather than `not-evaluable`, and the batch reading it must record the **zone**, not the registrable domain — and **there are three `Scan`s and two port tiers** ([#61](https://github.com/winniel123/verge-asm/issues/61), corrected by [#80](https://github.com/winniel123/verge-asm/issues/80) after #78 retired the weekly tier), the fourth scoped to the open `Service` population, so whatever surface configures the tiers **must not assume a `Scan` has a port list**
- **How the tiered port sets are curated and revised over time** — **the governance half only**, since #21 discharged the evidence half and [ADR-0009](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0009-verge-core-is-a-union.md) removed the derived set's maintainer. Fog: **who pulls the trigger, and on what watch**, over inputs with incompatible evidence standards. **Eight triggers, and the list is known incomplete-until-checked**: (1) a primary source changing position; (2) a protocol changing its shipped defaults; (3) a footing never checked against the standard it is now held to — a **backlog with an end**, and **both class sweeps are now finished as of a stated table state** (#79 the negative half, [#87](https://github.com/winniel123/verge-asm/issues/87) the §2.4 half, [#93](https://github.com/winniel123/verge-asm/issues/93) the positive half — **all three are now finished as of a stated table state, and #93 makes that state *dated* rather than permanent**: a class list is fixed at the time of the sweep, so a sweep is re-armed by an owner publishing into a class that existed and was empty. A **ninth** trigger for exactly that was argued and **refused on cost** — the watch it implies cannot be run); (4) **silent staleness** (#67), a project-authored constant going wrong on arithmetic while every document it cites still reads true — swept by [#71](https://github.com/winniel123/verge-asm/issues/71), one instance, already fixed, and widened to *anything that must be noticed*; (5) **citation staleness on a different cadence from content** (#73); (6) **a footing move re-arming the class sweep for the row that moved** (#79) — fired in the **disarming** direction for the first time by [#88](https://github.com/winniel123/verge-asm/issues/88); (7) **a drafted owner document becoming issued** ([#86](https://github.com/winniel123/verge-asm/issues/86)) — the cheapest, because it is **announced, by a release**, and its first instance is owed and dated (Kafka's `security-model.md` on the `4.4` branch with no tag); (8) **a competing owner starting or stopping documenting its service on a listed number** (#75, [ADR-0042](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0042-a-squat-is-contested-where-the-other-convention-is-live.md)) — `8500` is its clearest live instance. **[#112](https://github.com/winniel123/verge-asm/issues/112) turned up one by-catch candidate under ADR-0037 limb 1**: **[measured]** Docker numbers `2377/tcp` and takes a placement position in one sentence — *"This port should only be opened to a trusted network, and **never at a perimeter firewall**"* — and it is on **neither the list nor §4.6**. Determinacy and §10.1's steps are unread; `7946/tcp+udp` and `4789/udp` inherit it. Plus **five detectable** defects rather than watches, all cheap because both sides are already written down: **a row whose footing tier disagrees with its class** ([#83](https://github.com/winniel123/verge-asm/issues/83)); **a footing whose tier disagrees with the tier's own stated criterion** — **closed in both directions and now cheap to check mechanically**: [#93](https://github.com/winniel123/verge-asm/issues/93) measured §20.8's universal **refuted** on `873/tcp`, whose owner scopes by trust boundary and never says *internet*; [#98](https://github.com/winniel123/verge-asm/issues/98) **applied** it, making the sentence true over the tier that remained, and named the residue — *a necessary condition can refuse a member and cannot place one*; [#101](https://github.com/winniel123/verge-asm/issues/101) **closed** it, ruling the criterion **sufficient** as well as necessary on a four-limb conjunction, so the tier is a **membership test** rather than an observation about its members and the defect is detectable in the **promoting** direction too — `2181` and `25672` were the two instances and both are resolved, leaving as detectable-rather-than-watched only the residue of the closure itself, **a footing whose owner statement fails a limb**, which is a retrieval per row against four written conditions — **run by [#107](https://github.com/winniel123/verge-asm/issues/107) over all sixteen members, and it found one: `1433/tcp` fails limbs 2 and 4(c) and leaves the graded table, tiers 16/11/3 → 15/11/3 + 1 uncovered, coverage 29 of 41 — and [#109](https://github.com/winniel123/verge-asm/issues/109) then removed the **row** itself on the claim gate, so the tiers are unchanged at 15/11/3, the uncovered population is back to **0**, coverage reads **29 of 40** and the list is **40****. The **weak-tier watch list is unmoved at `5432/tcp`, `5984/tcp` and `10248/tcp`** — #107 touched no weak-tier member, so the sequence stands at 3 → 2 → 3 → 2 → 3; **an owner documenting a hard failure its shipped bytes do not enforce** ([#92](https://github.com/winniel123/verge-asm/issues/92) — `moby` `docker-v29.7.2` warns, sleeps and starts, the second instance after #83's kubelet, and if it ever lands that error `2375` moves); and **a superseded mechanism still specified in the present tense at the site that specifies it** ([ADR-0058](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0058-a-superseded-mechanism-is-withdrawn-at-the-site-that-specifies-it.md), swept **cross-document** by [#102](https://github.com/winniel123/verge-asm/issues/102) — **nine sites over six superseding decisions**, all repaired — and re-swept **intra-document** by [#106](https://github.com/winniel123/verge-asm/issues/106), which ruled the unit is the **sentence** rather than the file: **25 sites over 13 superseding decisions across 12 documents, ADR-0058 itself among them**, all repaired in place, and **cheaper** to detect than the cross-document half because both sides sit in one file; the corpus is the amendment surface and the sweep is **dated at `9a8f1df`**, re-armed by the next supersession and never by a clock); and **an exclusion whose stated ground has since been withdrawn** ([ADR-0065](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0065-a-rule-is-excluded-by-its-fact-or-by-its-aperture-never-by-the-shape-of-the-set.md), [#104](https://github.com/winniel123/verge-asm/issues/104) — §9.2's SMB row was the instance and **both** its grounds were dead; cheap for the same reason as the others, since an exclusion's ground and the decision withdrawing it are both written down); **an owner's deployment-scoped document naming the public internet for a listed pair** ([ADR-0067](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0067-a-claim-fails-on-the-owners-affirmative-naming-not-on-the-reach-of-its-own-prohibition.md) limb 2, [#109](https://github.com/winniel123/verge-asm/issues/109) — cheap for the same reason as the others, both sides being written down, and **now swept in full by [#112](https://github.com/winniel123/verge-asm/issues/112) over all 32 members it can reach**: the two graded tiers for the **attestation** gate and **Class C** for the **claim** gate, a population the tiers do not describe and which holds **six rows outside them** (`5432`, `5984`, `10248`, `139`, `137`, `138`). **[measured] thirty hold and two do not** — `9200/tcp` and `9300/tcp` Elasticsearch meet §10.3's failure condition on Elastic Cloud Enterprise documentation addressed to the operator, and are **routed rather than removed** because a tier-scoped sweep may not move a row. #112 also sorted the riders that defend the tier: **a rider travels to the claim gate only if it bounds the *affirmation*** — addressee and direction travel, a bound on the affirmation travels, and **conditioning of the prohibition does not**, since narrowing a prohibition leaves the affirmative naming standing alone. The defect is now **detectable at two gates with different populations**, and running it costs a re-reading of artefacts the note already holds); and **a limb of the tier criterion whose denotation is supplied by the reader rather than written down** ([#110](https://github.com/winniel123/verge-asm/issues/110) — the criterion's own *text* is now a detectable defect too, and it had **two silent instances** (`445/tcp`, `623/udp`) across four sections before anyone looked). **Trigger 5 (citation staleness on a different cadence from content) gains a new shape and its second instance**: **[measured]** HPE **retired** *Insight Online direct connect*, the feature §28.9's disposal is named after, while the disposal's **verdict** is unchanged and re-founded on a current document — *cite the version the server returns* firing on a **feature name** rather than on a URL, which no citation-freshness check on the URL would catch. **Curated inputs stand at four**: the frequency set, the sensitive list, the five-row weak-key table (#68 — one edit per row per decade), and `certificate-expiring`'s horizon, which #67 removed from this patch. **The watch list is [ADR-0032](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0032-an-evidence-standard-attaches-to-a-table-not-to-a-rule.md) §8's silent-de-attestation exposure — the weak tier — and it is `5432/tcp`, `5984/tcp` and `10248/tcp`, three rows** — [#95](https://github.com/winniel123/verge-asm/issues/95) grew it and [#100](https://github.com/winniel123/verge-asm/issues/100) confirmed `10248` in it, while [#101](https://github.com/winniel123/verge-asm/issues/101) moved no weak-tier member. Its sequence is **3 → 2 → 3 → 2 → 3 with the membership changing at every step**, so **the count has been useless as an indicator five times running**, which is exactly how §8's stale enumeration survived unnoticed. **The frequency half is now wholly hand-authored** and #78 retired the weekly tier, so no upstream ranking remains to go stale; **the replacement hunt is closed on capability, not deferred** — there is no frequency ranking in verge-asm's future. **Costs compose, and [#109](https://github.com/winniel123/verge-asm/issues/109) re-derived the sensitive half's**: revising the frequency set manufactures no drift, while a sensitive-list revision costs a version bump under [ADR-0008](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0008-derivation-versions-move-on-content.md) plus an aperture move — ~~*and* an aperture widening (#80)~~ **and the direction depends on which way the list moves: an *addition* outside the frequency half widens; a *removal* narrows a denominator whose numerator is `0` for every `|S|`, reveals nothing, and [measured] costs `verge-core` nothing at all wherever the pair is in the frequency half — true of 27 of the list's 40 pairs** (`sensitive-ports.md` §35.12). **Two permanent exclusions**: a key-compromise blocklist (#31's signature database), and encoding NIST's dated transitions as date comparisons — *a scheduled cryptographic transition is a scheduled edit, never a live date in a predicate*. **Open backlog**: [#114](https://github.com/winniel123/verge-asm/issues/114) (do `9200/tcp` and `9300/tcp` Elasticsearch survive — [#112](https://github.com/winniel123/verge-asm/issues/112) found §10.3's failure condition **met** on Elastic Cloud Enterprise and Elastic Cloud on Kubernetes documentation and **routed** the rows, a tier-scoped sweep being unable to move one under [#37](https://github.com/winniel123/verge-asm/issues/37); **inherits #112's blocking relationship to [#12](https://github.com/winniel123/verge-asm/issues/12)**, and it can reach a **two-row** removal — the note's largest, priced at §35.12's corrected cost rule, and **[measured]** costing `verge-core` nothing, both pairs being in the frequency half); and §4.6's unfooted Hadoop refusal, grandfathered and owed at next individual use. **[#114](https://github.com/winniel123/verge-asm/issues/114) leaves three by-catch items, none folded in.** (a) **`9443/tcp`** — Elastic names the public internet as its *supported* connection path, so the pair could never be admitted; it is **not** in §4.6 and owes an entry. (b) **`2375/tcp` gains a third-party corroborator**, which §2.3 bars as sole grounds and which therefore changes nothing until a first-party sentence joins it. (c) **A new retrieval-hazard shape, and it is the interesting one**: a **comment in the docs source** said the opposite of the `9300` finding and is **invisible in rendered HTML** — refused under [ADR-0045](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0045-an-owners-documentation-is-what-it-has-issued.md) as unissued, but a session reading rendered pages would never have seen it to refuse it. That is a hazard about **which bytes a retrieval reads**, not about what they say, and it is the third such shape after the fabricated clause and the line-wrapped string. **#95, #96, #97, #98, #104, #105, #106, #107, #109, #110, #112 and #114 have all closed and left this list.** The governance half stays fog
- **Whether RouteViews joins the default set, and what it displaces.** #20 found it replaces RIPEstat's `announced-prefixes` outright under **CC BY 4.0**, and that it is the only source measured here whose rate limit a naive client cannot turn into an error — it paces to ~1 req/s rather than 429ing, a materially different safety property under the no-false-absence rule. Fog: **whether the BGP leg is worth having at all** — **0 of 100** announced Safaricom prefixes fell outside a registered block, and 2 of 11 for a small UK host, so the folklore case for BGP catching untidily-registered space is much weaker than assumed. Not sharp because it likely depends on #26 — an ASN-less operator gets nothing from a route collector either
- Importing existing inventory (CSV, DNS zone data)
- **Whether the audit trail is surfaced in the UI** — named accounts create the data; presenting it is a separate question. **Adjacent to #22's `Coverage`** but deliberately not folded into it: an audit trail records what *operators* did, the operational record what the *system* did, and conflating them would put "admin enabled RIPEstat" and "prober unreachable" in one list. Worth deciding whether they share a home even so. **Weakened as a requirement by #8**, which removed the strongest reason to need it — aperture changes are detected from the `Batch`'s own recorded source set, identically whether a release or an admin caused it. What is left is the ordinary accountability case
- **Whether a name resolving to a private-range address from an internet vantage joins the signal set.** Surfaced as by-catch by #48 and deliberately not ruled on there. Publishing an RFC 1918 / RFC 6598 / RFC 4193 address in public DNS is a real internal-topology disclosure that nothing in the v1 set catches — [ADR-0010](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0010-exposure-composes-two-reaches.md)'s `Exposure` does not, because the internet prober cannot reach the address and the service reads `firewalled`. The rule would read `resolution`'s existing address set against the IANA Special-Purpose Address Registry, which is closed and spec-defined, so it clears ADR-0004 on its face and needs no new field — meaning [ADR-0015](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0015-the-value-space-is-the-commitment.md) gives it no deadline and it is a **pure scope call**. Sharp enough to ticket whenever it is worth the slot
- **Whether `dns-record` keys on `Address` as well as `Name`.** Surfaced by #62 while ruling PTR out of the v1 qtype set. PTR is a `dns-record` qtype whose subject is an `Address`, so admitting it means the facet holds **two subject kinds** — a discriminator-and-key question of the same shape as [ADR-0011](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0011-a-facet-is-six-parts.md)'s qtype discriminator, one level up. Not sharp because the answer probably depends on whether reverse-walking an owned range earns its place at all, and #26 measured that population at under 1%. It is also the repair path for #55's one open item, since a PTR is a *measurement* rather than a list

- **Where service *naming* lives, now that a clean source for it exists.** Surfaced as by-catch by [#78](https://github.com/winniel123/verge-asm/issues/78). Rendering *"port 5432"* as *"PostgreSQL"* is a job the product has independently of every ranking question, and **IANA's Service Name and Transport Protocol Port Number Registry is CC0 1.0** — *"may be freely used by any party for any purpose"*, no attribution and no downstream condition. **It is the first third-party source this project has retrieved whose terms are clean without qualification**, against CAIDA, Shodan, Censys, Rapid7, four RIRs and nmap. Not sharp because nobody has established **what a name is for**: it is not a `Signal` input and not a facet, so the question is whether it is a rendering concern with no model consequence at all, or something the glossary must name. **Two warnings.** IANA's own header says registration *"does not mean that it is 'good' traffic, nor that it necessarily corresponds to the assigned service"* — so a name must never render as though **measured**, or it is [#5](https://github.com/winniel123/verge-asm/issues/5)'s refused fingerprinting arriving through a lookup table. And **it is a table asserting about the world**, so if any rule ever reads it [ADR-0032](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0032-an-evidence-standard-attaches-to-a-table-not-to-a-rule.md)'s gate applies — a second reason to establish first that nothing reads it. See also #82: *does a port name a service* is this question from the product side

- **The aperture-widening message has never been drawn at magnitude.** [ADR-0014](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0014-only-revealed-generalises.md) fixes the payload as *a count of timelines opened and no comparison at all*, and [#80](https://github.com/winniel123/verge-asm/issues/80) measured it for the one widening a v1 operator can perform: enabling the full-range tier over a ten-address estate opens **~1.3 million timelines**, carried by **one** coverage-class message. The model absorbs it, and only because #63/[ADR-0031](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0031-membership-alerts-at-the-root-of-the-entering-subtree.md) ruled a `Service` entering is never a message. Fog: **what a seven-figure count reads as, and whether it needs a treatment the four-figure case does not.** #44's decision 10 discharged #28's three-densities obligation for the aperture **statement**; the **message** is a second object and inherits nothing from that. Not sharp because nobody has drawn either at that scale. **[#81](https://github.com/winniel123/verge-asm/issues/81) adds the ordinary instance** — declaring an address scope opens ≈287,000 `Reach` timelines for a `/22`, **at onboarding** — and **[#85](https://github.com/winniel123/verge-asm/issues/85) closes the route to anything larger**: the cap counts addresses and is family-blind, so ≈287,000 is the **maximum** any single declaration can produce in either family. So the seven-figure case has exactly **one** producer, the cold-tier enable, and the ordinary case is six figures

- **A row can be exposed to silent de-attestation without being in the weak tier, and [ADR-0032](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0032-an-evidence-standard-attaches-to-a-table-not-to-a-rule.md) §8 has no name for that shape.** [#76](https://github.com/winniel123/verge-asm/issues/76) put `2379`/`2380` etcd in the **prohibition** tier on a document three months old and absent from the `3.5` and `3.6` lines most deployments run. A young prohibition can be withdrawn as easily as a default can be flipped, but the watch list keys on **footing tier** rather than volatility, so those ports sit on **no list the curator reads**. Fog: **whether the watch list should key on tier, on evidence age, or on how many supports a row has.** The population has since tripled without the question sharpening: [#88](https://github.com/winniel123/verge-asm/issues/88) put `10250`/`10255` in the prohibition tier on a **checklist line in a release branch**, editable in one commit; [#84](https://github.com/winniel123/verge-asm/issues/84) gave `4369` a warrant that calls itself *"a living document"*; and [#83](https://github.com/winniel123/verge-asm/issues/83) added a **third axis** — `10250`'s *claim* now rests on a single table cell with no second support, so a claim can be as thin as a footing and the list keys on neither. **Four of the fifteen prohibition-tier pairs are young or cheaply withdrawable, and none is on any list the curator reads.** [#86](https://github.com/winniel123/verge-asm/issues/86) gives the shape its vocabulary without sharpening it — *issued at `v3.7.1` and not at `3.5` or `3.6`* is the precise form of *a young prohibition*. [#100](https://github.com/winniel123/verge-asm/issues/100) adds a **fourth** candidate axis with the opposite property to evidence age — **artefact class**, trivially measurable and not obviously right: [ADR-0061](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0061-a-comment-is-a-position-only-where-it-outlives-the-value-it-annotates.md) limb 4 rules that artefact class **never grades a tier** but bears squarely on volatility, and `10248/tcp`'s default lives in a **Go struct comment one contributor can change in one commit with no release note**. [#105](https://github.com/winniel123/verge-asm/issues/105) gives the **artefact-class** axis its floor without sharpening the patch: a footing resting on a **language zero value** is flipped by **adding** a line rather than by changing one, so a diff-based watch on the artefact sees nothing at all. **[measured] no member of today's weak tier is in that state** — `5432`, `5984` and `10248` all rest on written values (§34.7) — so it is a property of the axis rather than a live exposure, and it is reported rather than ticketed on #93's ninth-trigger cost reasoning. **[#107](https://github.com/winniel123/verge-asm/issues/107) removes one of the four young-or-cheaply-withdrawable prohibition-tier pairs by removing its cell, and adds a fifth axis candidate the others do not have: *contradiction by the owner's own product documentation*.** `1433/tcp`'s footing was not withdrawn or edited — it still says what it said — and it stopped being evidence because a **different** Microsoft page names the forbidden architecture as supported. **That is de-attestation with nothing to watch on the attesting artefact**, so evidence age, artefact class and support count would all have missed it, and only a per-row re-run of the criterion caught it. **The count is now four of fifteen**, and the patch has **five** candidate axes with still no proposal for measuring any. **[#110](https://github.com/winniel123/verge-asm/issues/110) tested a candidate *sixth* axis against this patch and rules it out, so the patch stays at five**: a disposal whose **supporting artefact** goes stale while its **verdict** stands — HPE retiring the feature §28.9's `623/udp` disposal names, with the prohibition unchanged and in fact strengthened — is **citation staleness (trigger 5)**, not de-attestation, nothing having been withdrawn and no row's evidence weakened. Recorded so the next session does not add a sixth axis for it. Still not sharp to ticket: **five** candidate axes and no proposal for measuring any of them — nobody has proposed what **evidence age** would be measured on, and first-seen date, release line and edit cadence give different answers

## Out of scope

<!-- One line per ruling: what is out, one clause on why, and the condition that reopens it.
     The argument is in the ticket. Compacted 2026-08-14, the second of three compactions that day;
     the third left this section untouched. The body before each pass is at
     docs/wayfinder/map-archive-2026-08-14{,b,c}.md. -->

- **Full vulnerability scanning** (nuclei templates, CVE detection) — v1 reports only risk signals that fall out of probing for free
- **Operator-authored signal rules** — ruled out for v1 in [Risk signals: which ones does v1 emit from cert and protocol facts alone?](https://github.com/winniel123/verge-asm/issues/16): a user-written rule is an un-versioned derivation inside the comparison path, and the first thing anyone would write is the signature matching ADR-0004 excluded. **Reopens** for v1.1 once `Annotation` and the versioning story have run in production
- **Technology fingerprinting** (Wappalyzer-style signature detection) — ruled out in [Build, vendor, or shell out for discovery and probing?](https://github.com/winniel123/verge-asm/issues/5) on drift integrity, not cost: a fingerprint verdict is a function of the signature database, so diffing it reports a change in the observer as a change in the world. Generalises to *only observed values enter drift*
- **A JSON API and API tokens for programmatic access** — ruled out for v1 in [Stack and runtime for web, worker, and persistence](https://github.com/winniel123/verge-asm/issues/6): nothing falls out for free from a server-rendered htmx UI, and a bearer token **bypasses the TOTP** of #11 over the operator's complete attack surface. The integration need is **push, not pull** — the internet `Reach` leg going `not-reached` → `reached` (#45, #58 having moved this entry's wording from a cell pair to a block to a leg) — which the notification-channels question carries
- **Cloud provider connectors** (AWS/Azure/GCP CAASM) — deferred past v1
- **Hosted multi-tenant SaaS** — self-hosted only
- **Offensive / bug-bounty workflows** against assets the operator does not own
- **SSO / OIDC / reverse-proxy forward-auth** — decided against for v1 in [Auth and access model for a self-hosted deployment](https://github.com/winniel123/verge-asm/issues/11); header-trust auth is a bypass class of bug when the proxy is misconfigured, and OIDC is work the v1 destination doesn't require
- **A `partially-lame-delegation` signal** — ruled out for v1 in [Does dangling-delegation join the v1 signal set, on evidence that is a failure?](https://github.com/winniel123/verge-asm/issues/35): the name still resolves, so `resolution` has not moved. The per-nameserver detail is still **recorded** on `dns-record`, so **v1.1 can add the rule with no rework**
- **A rule comparing the zone file's records against what the authority serves** — deferred in [Does zone-declared-name-does-not-resolve join the signal set, and what else does per-source keying expose?](https://github.com/winniel123/verge-asm/issues/48): a zone export expresses things the wire does not (`ALIAS`/`ANAME`, apex flattening, `$GENERATE`, `$INCLUDE`, provider-side signing, SOA serials, rewritten apex NS sets), so the decoder needs a stripper per convention forever. **Reopens the moment somebody measures what the major providers' exports emit** — the same measurement that would price `resolved-name-absent-from-zone`'s noise floor. The v1 line: **a rule may read which names a zone contains, never what records it holds for them.**
- **The wire-protocol prober and the `listener-negotiation` facet** — ruled out for v1 in [Does the observation-driven listener rule ship in v1, and at what granularity?](https://github.com/winniel123/verge-asm/issues/41): a new facet, canonicaliser, prober leaves, per-protocol codecs and an expanded safety surface, buying **six ports** and **zero** net new firings. **Deferrable with no rework** at `revealed` plus one message and no `Break`. §5's per-protocol costing in [`docs/research/insecure-listener-rules.md`](https://github.com/winniel123/verge-asm/blob/main/docs/research/insecure-listener-rules.md) is preserved; **whoever takes it up must first close §7.2's skipped question** — a wrong dispatch guess fails safe for the *data*, but is it safe for the *listener*?
- **Third-party bulk registry dumps as an instrument** (APNIC's `apnic.db.inetnum.gz`, and the RIPE and AFRINIC dumps with it) — ruled out for v1 in [May verge-asm index APNIC's bulk file, given its header bars storage in a retrieval system?](https://github.com/winniel123/verge-asm/issues/46). **Not on the licence** but because **the credential is not machine-checkable and the download is not gated**, so the toggle would assert a signed agreement the software can never see. **v1.1 may take it up with no rework.** Cost stated: APNIC's 657,044 named PA assignments (#38) stay unreachable by name at any setting, now final rather than pending a licence question
- **The CAA and PTR qtypes** — deferred in [What does the measurement binary actually offer, now that a default may not stand in for a declaration?](https://github.com/winniel123/verge-asm/issues/62): CAA's only rationale was withdrawn when #56 ruled CT-fed facets out and the row survived it, and *the CAA record changed* is true of every qtype; PTR serves #26's sub-1% population and needs `dns-record` to key on `Address`, which is fog. **Deferrable with no rework** — a qtype is a discriminator in the key, so adding one costs `revealed` plus one message with no `Break`
- **DNSSEC records in `dns-record`, and the DO bit** — ruled out in #62: the DO bit enlarges every answer with RRSIGs and multiplies TCP fallback, to collect records no declared qtype holds and no v1 rule reads. Cost stated: **v1 records nothing about DNSSEC and a validation failure is invisible.** Decided on thin ground and marked as such
- **HTTP/3, and any QUIC listener** — ruled out in #62 while closing the ALPN list: `h3` cannot be selected in a TLS-over-TCP ClientHello, so an HTTP/3-only service is invisible one layer earlier, at `reachability`. **This is not an ALPN gap** — the honest statement is that v1's aperture is TCP
- **TLS 1.3 cipher-suite enumeration, and NULL / EXPORT / anonymous suites** — ruled out in #62, and neither is a cost ruling: Go ignores `Config.CipherSuites` for TLS 1.3, so those suites are **not ours to declare**, and NULL, EXPORT and anonymous suites are not implemented by Go at any setting. So **"does this listener accept a NULL cipher" is a question v1 cannot put** — the sharpest missing capability in the offer list, which **becomes available only if the TLS library changes or is replaced**
- **A facet-level change channel for `dns-record`** — refused in [Which facet `Transition`s besides the internet `Reach` leg are messages?](https://github.com/winniel123/verge-asm/issues/64) as ADR-0007's named burst shape verbatim, and because no v1 rule reads the facet at all. Cost stated: an MX, TXT or NS change reaches nobody in v1. **Deferrable with no rework** — the legal route is a `Signal` under ADR-0004's cadence test, which #35 established #16's list does not bar. **What is out of scope is the channel, not the rule**
- **Certificate-transparency mis-issuance detection, and any CT-fed facet** — ruled out for v1 in [Which subject does a certificate-transparency observation key on, and is CT a certificate source at all?](https://github.com/winniel123/verge-asm/issues/56) on three grounds: after ADR-0027 CT holds no timeline to evaluate; CT is append-only, so *mis-issued* and *outlived its name* are opposite facts under one predicate; and **the join key does not exist on the instrument that ships**. Priced at a new `Name` facet, so **v1.1 may take it up with no rework**. **Reopens in part if a fingerprint field ever appears on `crt.sh`** — the other two grounds survive it. Inherited warning: an instrument with a **999-row cap that truncates silently under an HTTP 200** is ADR-0009's `{161}` defect arriving through the *value*
- **A `sensitive-port-reached-from-internal` signal** — refused in [On a one-vantage-class install there is no Exposure — which bare Reach transitions wake someone?](https://github.com/winniel123/verge-asm/issues/58) on evidence rather than cost: #21's list is attested for what is *never legitimately internet-facing*, and internally a Redis on 6379 is the **correct** configuration. Unlike most entries here this is **wrong** rather than deferrable: **it becomes legal only if somebody attests a list of ports never legitimately reachable from an operator's own network**
- **An `http-acceptance` facet** — which HTTP versions a listener accepts, the symmetric object to `tls-acceptance`. Ruled out in [Is everything our client offers an aperture input, or only the TLS candidate set?](https://github.com/winniel123/verge-asm/issues/54) on pricing rather than principle: a wholly new facet costs `revealed` plus one message with **no `Break`**, so there is no deadline and nothing to buy now. It is the honest home for a fact v1 deliberately does not record
- **Distinguishing h2c with prior knowledge from non-HTTP bytes in cleartext** — ruled out in #54: it needs a second probe sending an HTTP/2 preface at whatever is on the port, the class of risk against production #4's profile has not budgeted. The TLS-side case is bought instead by a wider ALPN offer (`h2, http/1.1`), free in Go, so what remains is cleartext only
- **A per-row evidence tier on `Signals`, or anywhere else in the interface** — refused in [Does the claim/attestation/determinacy standard generalise to the other nine signals?](https://github.com/winniel123/verge-asm/issues/33). Publishing the weak tier is confirmed and **widened to all three instruments' own documents**; what is out of scope is putting it **on a screen**, because **it is severity** arriving labelled as honesty, **it does not vary per subject**, **it names no act the operator can take**, and **its real consumer is the curator**. Home and trigger are named so nobody invents a badge later: it would live on #44's aperture statement on `Coverage`, and **it earns that home the moment the operator acquires a sanctioned way to disagree with a row**
- **SNMP on the sensitive list, and any `sensitive-port-reached-from-internet` coverage of `161/udp`** — removed in [Is `161/udp`'s Claim 3 boundary attested by an owner, or does the row come off?](https://github.com/winniel123/verge-asm/issues/66) on #21 §2.7's `111/tcp` ground — *we could not find anyone entitled to say that exposing it is never correct* — the two cases sharing a shape worth naming: **the row whose verdict everyone is surest of is the row with no owner behind it.** SNMP is among the most exposed management protocols on the internet and **v1 says nothing about it**. **Reopens on** a first-party sentence saying the protocol **assumes** its callers are inside a boundary, which re-admits it as a **new admission** — a version bump plus an aperture widening under ADR-0009's removal-plus-addition rule — never a reversal of this
- **Reading the CA's ARI renewal window per observed certificate** — ruled out for v1 in [Is `certificate-expiring`'s 30-day horizon attested by an owner, or does the number move?](https://github.com/winniel123/verge-asm/issues/67), and **not on cost**: it is genuinely available, and refused because the window is a **CA load-balancing instrument** in the spec's own words, buys **±1% of validity**, has **no answer for most of the estate**, and is a **per-certificate outbound request to every CA in the WebPKI**. **Reopens as a different rule**: *the CA recommended replacement on this date and it has not happened* reads a **published value**, making it an observation and dissolving gate 2 entirely
- **A `certificate-not-conforming-to-issuance-policy` signal** — deferred past v1 by [What is `certificate-weak-key-or-signature`'s table, and who owns a claim about the WebPKI?](https://github.com/winniel123/verge-asm/issues/68): the BR, Mozilla and Microsoft all restrict issuance more tightly than any owner calls weak, so *non-conforming* is a real and different fact needing its own table — one asserting about **a document** rather than the world, so it sits under #31's instrument and not #21's. **v1.1 can add it with no rework**
- **A coherence gate on shipped defaults** — *a restricting default attests only where the restriction describes a deployment somebody actually runs*. Refused in [An upstream example config a distributor ships as the default: documentation, or a shipped default?](https://github.com/winniel123/verge-asm/issues/69): *does anybody run this?* is a judgement about deployment reality with **no owner** — the shape §10.1 deleted — and it needs frequency evidence the note excludes by framing. **Reopens on:** a row passing §12 on the **owner's own** operative default where that same owner's documentation elsewhere calls the configuration unusable — attested incoherence rather than reasoned
- **A determinacy survey — proving no other service listens on a number.** Unbounded and unfalsifiable; §14.9 declined to claim it and [ADR-0048](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0048-a-convention-is-evidenced-by-placement-never-by-catalogue.md) rules a negative here is a **presumption not yet defeated**, never a proven absence — [#82](https://github.com/winniel123/verge-asm/issues/82). **Reopens on:** nothing; one document defeats, and no number of documents proves the negative
- **Re-founding §4.3's ten generic-port exclusions.** Grandfathered as a class and **priced at next individual use** — each owes its cited artefact the first time it is individually relied on, as `9090` now does — so filing a bulk re-founding ticket would contradict the pricing — [#82](https://github.com/winniel123/verge-asm/issues/82). **Reopens on:** an exclusion being relied on individually, which prices itself
- **Sweeping IPv6 address space** — ruled out in [Can an address-scope `Seed` be an IPv6 prefix, and what does the range cap mean if it can?](https://github.com/winniel123/verge-asm/issues/85), and **not on cost**: one `/64` — the architectural subnet floor, and the smallest prefix anyone is assigned — is ≈ 4.1 × 10¹¹ years on the daily tier at §6.3's 200 pkt/s ceiling, so this is a request **no instrument can serve** rather than an expensive one a knob could buy. The honest v1 statement is that **IPv6 addresses enter by resolution and are probed like any other, and IPv6 space is not swept**. **Reopens on** a *different instrument* and never a different number — a candidate list from routing data, PTR walking, or DNSSEC zone walking — each a **new source** with its own admission, consent and completeness properties, and each already fenced: PTR is out of the v1 qtype set ([#62](https://github.com/winniel123/verge-asm/issues/62)) and reverse-walking an owned range serves [#26](https://github.com/winniel123/verge-asm/issues/26)'s sub-1% population
- **Re-founding §4.6's Hadoop NameNode / YARN UI determinacy refusal** — the note's last exclusion with no cited artefact, from [Has ADR-0042's liveness test ever been run on esmagent and fmtp, and do 5601 and 8500 survive it?](https://github.com/winniel123/verge-asm/issues/87) §22.8. Grandfathered as a class alongside §4.3's ten generic ports and **priced at next individual use**, so filing a bulk re-founding ticket would contradict the pricing. **Reopens on:** the entry being individually relied on, which prices itself
- **Grading a footing tier on what an owner has committed to rather than on which words it used** — refused in [Is §20.8's tier criterion sufficient as well as necessary?](https://github.com/winniel123/verge-asm/issues/101), the **second** refusal after [#98](https://github.com/winniel123/verge-asm/issues/98)'s. The reading is not wrong about the owners — rsync really is saying what Redis is saying, and ZooKeeper's descriptive sentence really is a prohibition — and it is refused on **falsifiability**: *did the owner commit to this* cannot be checked by opening a document, and §2's whole architecture is that every cell can be. **The four limbs are each a retrieval**, and that is what they are for. Named here so no later pass re-proposes a conviction ranking as an improvement in honesty. **Reopens on:** nothing short of abandoning §2's retrievability requirement
- **The `wildcard-synthesis` facet** — the carrier for a name's measured wildcard synthesis, named and priced in [ADR-0062](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0062-a-wildcards-synthesis-is-a-fact-about-the-name-it-was-probed-under.md) and out of v1. **Deferred rather than refused, and now on price alone**: a wholly new facet costs `revealed` plus one message with **no `Break`**, so there is no deadline. **Its sixth part is now writable — [#108](https://github.com/winniel123/verge-asm/issues/108) / [ADR-0066](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0066-a-control-probe-is-generated-under-a-names-parent-and-that-population-is-aperture.md) declared the control-probe population, so the stated reopening condition is met.** Cost stated: **v1 holds a wildcard's content nowhere and a repoint is invisible**, every name beneath staying `Shadowed` across the move. **Two open items arrive with the discharge**: a probe site may be a name we do not hold, so it has **no carrier** (`ns.iana.org` is the measured instance) and inventing one is barred by ADR-0060 and ADR-0027 alike; and the **value space** ~~waits on [#111](https://github.com/winniel123/verge-asm/issues/111), since *a closed union of no-synthesis and synthesised RRset* is undrawable while **[measured]** two of four live wildcarded zones return a different synthesised RRset per control label~~ — **withdrawn by [#111](https://github.com/winniel123/verge-asm/issues/111) / [ADR-0068](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0068-a-wildcard-is-discriminated-only-where-its-synthesis-is-determinate.md), which settles it at **three** members *per component*: `NoSynthesis` │ `Determinate(RRset)` │ `Indeterminate`. The rotating case is not an undrawable value, it is a third value**. A message on top of it would be a `Signal` under ADR-0004 and never a facet-level channel, which [#64](https://github.com/winniel123/verge-asm/issues/64) already refused. **[#113](https://github.com/winniel123/verge-asm/issues/113) settles the last input the facet's measurement was missing** — the control-label construction — so nothing about `wildcard-synthesis` is now undecided except whether to build it, and it stays out on price alone. Its **[measured]** content also shifts: an address-parsing authority has no stable synthesis to hold, so the facet would carry `Indeterminate` for three of the measured zones rather than a value
- **The DNSSEC wildcard proof as a second discriminator** (RFC 4035 §3.1.3) — the only *sound* test that exists, and it would recover exactly the names [ADR-0068](https://github.com/winniel123/verge-asm/blob/main/docs/adr/0068-a-wildcard-is-discriminated-only-where-its-synthesis-is-determinate.md)'s indeterminate limb suppresses. Ruled out for v1 by [#111](https://github.com/winniel123/verge-asm/issues/111) on **availability, not cost**: **[measured]** 2026-08-14, **1 of 15** zones probed carries a DS, and that one online-signs its synthesised answers (`RRSIG cname 13 3 …`, `Labels` = 3 against a 3-label owner, no NSEC3) — so the machinery is missing exactly where content discrimination is also missing, and works only where no wildcard sits in front of it. It also changes the leaf's **query mode** (DO bit, RRSIG parsing, NSEC3), a parameter change of its own. **Reopens on** a signed-zone sample saying otherwise — the measurement is the whole ruling, so it is a **rerun**, not an argument. Ruled out here rather than ticketed because a ticket whose answer is *the instrument is absent from 14 of 15 zones* has nothing left to decide
- **A signal reading SMB's session-integrity requirement** (`smb-signing-not-required` as the ticket named it — it would not ship under that name, since ADR-0015 forbids naming a rule for a protocol) — ruled out for v1 in [#104](https://github.com/winniel123/verge-asm/issues/104) on the **aperture**, and on neither of the two grounds `insecure-listener-rules.md` §9.2 gave, both of which are withdrawn. The fact is **admissible and measured**: `SMB2_NEGOTIATE_SIGNING_REQUIRED` is mandatory, credential-free and still varies on current systems, and it is the only one of eight protocols expressing the fact server-originated, pre-credential and listener-scoped. What is missing is the observation — an SMB2 `NEGOTIATE` exchange and the `listener-negotiation` facet, both already out of scope with the wire prober. **Deferrable with no rework** at `revealed` plus one message and no `Break`. **Reopens** with the wire prober and only with it — which then owes a **third field** on `listener-negotiation`, since a value space is decided once and widening it later is a `Break`




















