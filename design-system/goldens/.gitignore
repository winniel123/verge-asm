# render-goldens intermediate HTML — regenerated by run.sh each run; the goldens
# are the PNGs under inventory/. Not committed (also keeps CodeQL from scanning it).
*.html

# Advisory pixel-diff images capture.mjs writes beside a golden on an over-threshold
# candidate miss — debug output, regenerated each run; never part of the committed goldens.
*.diff.png
