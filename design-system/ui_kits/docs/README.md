# Verge ASM — docs site UI kit

`index.html` — docs.verge-asm.org, "Installation" page: slim header (search, version select), paper sidebar with grouped nav (accent-soft active state), 760px prose column, ink code blocks with mono micro-label title bars, accent note callout, config-flag Table, and an "On this page" rail.

Prose is 14px/1.65; headings 34/20; everything else inherits the system tokens.
