# Package version

current: 3.16.2
exported: 2026-08-26

Convention: MAJOR.ROUND.PATCH — MAJOR is the package generation, ROUND increments per sync/Wayfinder round, PATCH per mid-round ruling or spec fix. The design workspace bumps this on every export; the repo copy always states what landed.

## Log
- 3.16.2 — 2026-08-26 · drift-audit rulings #28–#32 (SPEC-CHANGE.md, no template change): severity docs stale side (#1 stands); Run-now stays download-only + #17 delivery IS built (docs drop the stale AWAITING note) + format=pdf normative (#23c); 4 runnerless RIR proposers → the #241 catalogued-not-executing bucket; cadence preset times + cron = BUILD (spec normative); drift legend stands contingent on estate wiring (else trims next round)
- 3.16.1 — 2026-08-26 · DF-F3b per-job live output: job ids in Running now link to /runs/{run}?job={id}; rundetail loghead gains the .JobFilter chip (× clears); .Log arrives server-filtered; fixtures job hrefs; work-order addendum
- 3.16.0 — 2026-08-26 · dogfood features (WORK-ORDER-FEATURES-1, full Behavior sections — first under the 3.14.1 rule): DF-F1 paste-split scope declaration (.Refusals[] replaces .Refusal, onboarding tokenizer, per-token callouts), DF-F2 multi zone upload (one dated act per file, .ZoneErrors[] replaces .ZoneError), DF-F3 Running-now/history rows link to RunDetail (.Active ID+Href, .History Href; .Refresh=5 live tail — rundetail.tmpl unchanged), DF-F4 stop-dispatch + terminate (PRG dialogs, POST /scans/stop|terminate, stopped·partial/terminated semantics, admin-only); fixtures active dispatch → 1408 + running run; 2-3 new states
- 3.15.0 — 2026-08-26 · dogfood round 1 (operator notes + 8 screenshots): scope.tmpl (chip ×-form no longer inherits the field-input flex — the trailing-space bug; zone rows one-line with ellipsis + right badge; zone drop uses requestSubmit so scroll preservation fires), reports.tmpl (barchart clips — ≤31-bar contract), inbox.tmpl (equal-height empty cards), settings.tmpl (instance Fleet·Vantages empty state, .Vantages nullable), inventory.tmpl (fixed table layout — no column shift on expand; animated records reveal; muted none for recordless facets). Behavior: no change. WORK-ORDER-DOGFOOD-R1.md carries 6 repo-side flags (toast spam per-dispatch flash, seed-removal flash, health wiring, scantrigger width, scroll-JS verify, scan pill via batch 8)
- 3.14.1 — 2026-08-26 · Behavior spec codified: WORKFLOW.md gains the required `## Behavior` WORK-ORDER section for new features (entry points, state transitions, data contract per hole, actions & side effects, edge cases; gaps = collision, never improvisation); consume-design-package SKILL.md gains the matching "Implementing a new feature" procedure. No template/css/fixture/golden changes — G1 scope untouched
- 3.14.0 — 2026-08-26 · batch 8 (screen 22, solo, LAST): shell.tmpl — head/chrome/foot replace templates_shell.go and pageCSS wholesale (#27a; design tokens now serve every page; the .DesignTokens gate and shell resets retire per P4.4); spec TopNav (pulse wordmark, nullable OrgSwitcher, pill nav, scan status, bell popover, account menu with sign-out), palette keeps the #315 scaffold contract under the spec panel (#27c), spec ToastStack over the PRG flash store (#27d), spec footer + byte-kept scroll-preservation JS (#27e); #27f one-time FULL-PAGE golden regen for all landed screens; fixtures + 7 shell states; WORK-ORDER-22-BATCH8.md; Settings marked LANDED
- 3.13.0 — 2026-08-26 · batch 7 (screen 21, solo): settings.tmpl — all sections under the spec sidebar (#26a; tabs retire): scans queue read + relocated cold tier (Recurring-intent card + calendar DEFERRED, no config store — ADR-0110), vantage cards + prober lifecycle with pinned-key CodeBlock + egress callout (preferred-vantage combobox DEFERRED), OIDC single sign-on stands (#26e — the SAML mock content retires per ADR-0113), spec team table + PRG dialogs + invite-link flow, sessions + typed-confirm revoke-all, honest audit empty, three-tier sources with PRG consent dialog, locked sensitive tier + frequency table, instance health, channels with store-vocabulary classes (#26f), PRG integrations catalogue + drawer (#26j), operational messages record, delivery outcomes + retention dials (#26h — error/skeleton client states drop); reconciliation #26; fixtures + 19 states; WORK-ORDER-21-BATCH7.md; SearchResults/Onboarding/FirstRun marked LANDED
- 3.12.0 — 2026-08-26 · batch 6 (screens 19–20): search.tmpl (spec match highlighting via segment holes, asset rows regain severity, mono input sans icon), onboarding.tmpl (spec panel + step rail over the PRG post-back flow, spec TagInput/RadioCards/cadence listbox, JS validity gating with .StepValid floor, stateful GET step URLs, no cancel — first-run has nowhere to cancel to), firstrun.tmpl (bare define replacing templates_firstrun.go; action = link or form post, step 4 gated with title); reconciliation #25; fixtures + states; WORK-ORDER-19-20-BATCH6.md; Reports/ReportArtifact/Inbox marked LANDED
- 3.11.1 — 2026-08-25 · SPEC-CHANGE #24 (ruled): inbox detail carries NO prose body — the .Body hole retires permanently (supersedes the #23i .Body item; the sample paragraph predates the census store, the ADR-0064 valence-free discipline stands, no prose producer gets built); the detail form = census + delivery receipts. inbox.tmpl + fixtures updated. Scope: inbox-only delta — reports.tmpl/reportartifact.tmpl bytes identical to v3.11.0, so the open 16+17 PR stands; screen 18 pins v3.11.1
- 3.11.0 — 2026-08-25 · batch 5 (screens 16–18): reports.tmpl (spec range picker + SplitButton export incl. PDF, KPI cards with spark/bar motion, hover readout on the trend chart, client-side schedule sort, spec kebab row menus, schedulewizard as spec panel with listboxes + JS validity gating on the PRG post-back flow), reportartifact.tmpl (design-owned "artifactdoc" define adopted by internal/message.RenderArtifact — on-screen/PDF/email render one markup; Edit schedule links the now-real edit route), inbox.tmpl (spec list + detail, .Body prose joins, design-owned census + delivery receipts incl. the undelivered treatment); reconciliation #23 (MTTW adopts the withdrawal ontology, names · services vocabulary); fixtures + states for all three; WORK-ORDER-16-18-BATCH5.md; AssetDetail/SubjectDetail/Graph marked LANDED
- 3.10.0 — 2026-08-25 · batch 4 (screens 13–15): asset.tmpl (spec CertificateCard from the built parsed-cert read, census Service column joins stored identity, signals-here deep links, collapsible drift-trail group), subjectdetail.tmpl (spec citation rail, sunken facet KVs, tooltip span bounds, spec withdrawn banner + mark, spec menus), graph.tmpl (domain/subdomain split, two-radius halos, spec listbox filter, selection ring, spec node drawer; pan/zoom engine kept); reconciliation #22; fixtures + states for all three; WORK-ORDER-13-15-BATCH4.md; Scope/Dashboard/Signals marked LANDED
- 3.9.0 — 2026-08-25 · batch 3 (screens 10–12): scope.tmpl (spec two-column layout, tag-field seeds with shape-inferred kind, RefusalCallout, per-scope custody + census, FileDrop zone re-supply with apex inference, JS name tree, proposals org-search + confirm-one/decline-many, exclusion editor + preview receipt), dashboard.tmpl (spec stat band/banner/progress, denominator coverage meters, drawer-deep-linked recent signals), signals.tmpl (PRG skeleton kept; spec tabs/listbox/table/pagination, row kebab + context menus, full drawer with tags/desc/rule/diff/annotation/history, typed-confirm descope); reconciliation #21; cold tier + probers relocate to /settings; fixtures + states for all three; WORK-ORDER-10-12-BATCH3.md; Exposure/Drift/RunDetail marked LANDED
- 3.8.0 — 2026-08-25 · batch 2 (screens 7–9): exposure.tmpl (withheld first-class, delta chip, both-legs table), drift.tmpl (spec range picker with custom ISO pair, client-side kind toggles, collapsible batch groups, framed diff cards), rundetail.tmpl (Outcome = transitions + new signals per 2026-08-24 binding ruling, colored log levels, run-missing retired to error.tmpl); reconciliation #20; fixtures + states for all three; WORK-ORDER-7-9-BATCH2.md; SignIn/Setup/Coverage marked LANDED
- 3.7.0 — 2026-08-25 · batch 1 (screens 4–6): signin.tmpl (11 auth states, segmented code input, SSO not-configured state), setup.tmpl, coverage.tmpl (denominator meters for address scopes, relative-time messages); reconciliation #19; SignIn/Setup specs realigned to username identity; fixtures + states for all three; WORK-ORDER-4-6-BATCH1.md; Profile marked LANDED
- 3.6.0 — 2026-08-25 · screen 3 (Profile): profile.tmpl (PRG holes kept; spec cards/dialogs/badges/tables), reconciliation #18 (plain revoke confirm, TOTP-off state, toast mapping), profile fixtures + six goldens states, WORK-ORDER-3-PROFILE.md; ErrorPage marked LANDED
- 3.5.1 — 2026-08-25 · batching rule expanded: parallel-session mechanics (one screen/branch each, single package version per batch, append-only shared files, serial merges with G1+G2 re-runs)
- 3.5.0 — 2026-08-25 · screen 2 (ErrorPage): error.tmpl (six kinds, spec copy verbatim, hover copy control, chrome-when-signed-in rule), fixture accounts + deterministic incident id, six capture states, WORK-ORDER-2-ERROR.md; Inventory marked LANDED (operator confirmed)
- 3.4.1 — 2026-08-24 · claude/skills/consume-design-package/SKILL.md added — the repo-side skill encoding the v4 consuming contract (land a version, convert a screen, wire holes, gates, escalation)
- 3.4.0 — 2026-08-24 · P4.0 pilot artifacts: templates/inventory.tmpl (design-owned, self-contained, exact component values; carries recordrows + subject-missing), fixtures/fixtures.json (view contract + clock), verify/{states,config}.json, verify/PILOT-P4.0.md work order
- 3.3.2 — 2026-08-24 · WAYFINDER-MAP.md: per-screen conversion order (22 stops, Inventory pilot, shell last with crop-to-main rule), the 4-step loop with operator visual sign-off gates, batching rule
- 3.3.1 — 2026-08-24 · determinism section: one canonical pinned Linux container renders BOTH goldens and repo captures (goldens regenerated in-container from templates+fixtures, never workspace screenshots); packaged fonts; Windows/macOS runs advisory
- 3.3.0 — 2026-08-24 · WORKFLOW v4 ruled and charted: design-authored .tmpl view layer, fixture contract, hard CI pixel gate (G1 verbatim + G2 pixelmatch 3%), P4 migration tree (P4.0 Inventory pilot first); CLAUDE.md block v2
- 3.2.4 — 2026-08-24 · collision #13 ruled (U6): Inventory re-specced onto the shipped open-span read — dead controls retired, client-side kind/gaps/filter scope added; shot 03 recaptured
- 3.2.3 — 2026-08-24 · collision #17 ruled (P0.6c schedule→channel binding); wizard Delivery step + list Delivery column specced (shot 15); docs artifact copy fixed
- 3.2.2 — 2026-08-24 · collision #16 ruled (P0.6b notify-with-link delivery; ADR-0039 stands)
- 3.2.1 — 2026-08-24 · collision #15 ruled (P0.6 dispatch + delivery backend); SPEC-CHANGE log at 15 entries
- 3.2.0 — 2026-08-24 · round-2 chart: verified Landed ledger, rulings #7/#11/#13/#14, Profile spec catch-up (account model + SSO section, shot 14)
- 3.1.0 — 2026-08-24 · parity ruling + PARITY-CHART; SPEC-CHANGE escalation protocol; Export CSV specced
- 3.0.0 — 2026-08-22 · v3 full package: 26 screens, ~112 components, WORK-CHART
