Card-style exclusive choice when options deserve descriptions (scan profiles). Plain Radio stays the compact form.
```jsx
<RadioCards value={p} onChange={setP} options={[{value:"standard",title:"Standard",description:"Top 1,000 TCP",meta:"default"}]} />
```
