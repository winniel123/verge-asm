Action button — labels are sentence-case imperative verbs (Add seed, Run scan, Export CSV).
```jsx
<Button icon={<Icon name="play" size={14} />}>Run scan</Button>
<Button variant="secondary">Export CSV</Button>
```
Variants: primary | secondary | ghost | danger. Sizes sm/md/lg. One primary per view.
