Code on the console ground (`--surface-console`, dark in both themes, never the theme-flipping `--surface-inverted`) with hover copy control. Docs + empty states.
```jsx
<CodeBlock title="shell">docker run -p 8443:8443 verge/verge</CodeBlock>
```
