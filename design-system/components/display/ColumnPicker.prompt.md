Popover of checkboxes controlling table column visibility; lock identity columns.
```jsx
<ColumnPicker columns={[{key:"asset",label:"Asset",locked:true},{key:"ip",label:"IP"}]} visible={vis} onChange={setVis} />
```
