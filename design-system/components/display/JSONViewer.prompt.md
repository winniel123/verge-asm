Collapsible JSON tree (delivery payloads, API examples). Type colors are value semantics, never severity. CodeBlock stays the form for flat snippets.
```jsx
<JSONViewer data={payload} defaultDepth={2} />
```
