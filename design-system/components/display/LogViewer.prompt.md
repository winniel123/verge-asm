Streaming batch output on the console ground — the panel the system reserves for logs. The ground is `--console-surface`, dark in both themes, never the theme-flipping `--surface-inverted`. Auto-follows while live.
```jsx
<LogViewer title="batch 2026-08-22T14:00Z" live lines={lines} />
```
