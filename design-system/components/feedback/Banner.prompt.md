Persistent inline alert — page/section level; stays until resolved (Toast is the transient one). Copy = fact + next action.
```jsx
<Banner tone="warn" title="Probe unreachable" action={<Button size="sm" variant="secondary">Retry</Button>}>ap-south-1 missed 2 checks. Scans continue from other probes.</Banner>
```
