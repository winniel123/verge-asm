Failure state = fact + retry (EmptyState's sibling; same layout, danger iconography).
```jsx
<ErrorState message="Batch failed to start." detail="Vantage ap-south-1 refused the run. Retry or pick another vantage." onRetry={retry} />
```
