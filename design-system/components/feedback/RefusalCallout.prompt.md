Declaration refusal — names the refused input and the reachable set, never auto-corrects.
```jsx
<RefusalCallout input="203.0.113.0/20" reason="Spans 4,096 addresses — the cap is 1,024." reachable="203.0.113.0/22" />
```
