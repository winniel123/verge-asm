Lucide icon wrapper — the system's only glyph API; inherits currentColor.
```jsx
<Icon name="radar" size={16} />
```
Requires the Lucide UMD script tag on the page. Vocabulary: radar scans, globe domains, server services, shield-alert signals, network graph, file-text reports, git-branch drift.
