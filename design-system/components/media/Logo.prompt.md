Brand mark — the pulse glyph (signal dot in two watch rings) + wordmark. Accent azure only, never severity colors; rings never animate in chrome. Placeholder-quality mark pending professional brand work.
```jsx
<Logo size={20} />            // glyph + verge ASM
<Logo withWordmark={false} tile />  // favicon tile
<Logo inverted />             // on inverted ink
```
