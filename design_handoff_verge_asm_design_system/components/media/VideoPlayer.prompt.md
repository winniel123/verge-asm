Custom-chrome video: tokened controls on a gradient scrim, mono timecodes, accent progress, idle-hide while playing. Volume drag-slider next to mute. Keyboard: space/k, m, f, left/right seek 5s, up/down volume. Fullscreen falls back to a viewport fill where the API is blocked.
```jsx
<VideoPlayer src={mp4Url} label="Scan walkthrough (2:04)" />
```
