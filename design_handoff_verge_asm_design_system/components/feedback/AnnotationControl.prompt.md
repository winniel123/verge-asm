The only operator dial on a signal — accepted risk + reason prose. Never a status dropdown, never an expiry, never an author (ADR-0073). Replaces mute/resolve/triage patterns.
```jsx
<AnnotationControl annotation={ann} onAnnotate={(reason)=>save(reason)} onRemove={clear} />
```
