Hover peek for cross-references (asset names → mini preview) — a read-only glance, never actions. Tooltip stays the one-liner form.
```jsx
<HoverCard content={<AssetPeek asset={r}/>}><span>{r.asset}</span></HoverCard>
```
