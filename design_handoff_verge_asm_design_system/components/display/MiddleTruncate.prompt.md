Middle-ellipsis for long technical names — the tail (TLD, port) stays visible. Needs a min-width-0 flex/grid cell to shrink in.
```jsx
<MiddleTruncate value="build-artifacts-cache-euw.acmecorp.io" tail={12} />
```
