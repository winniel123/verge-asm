Coverage read — "did we look, how completely". counted/total renders a bar; omit total for the striped census state (a census never claims a percentage).
```jsx
<CoverageMeter label="203.0.113.0/24" counted={212} total={256} unit="addresses" />
<CoverageMeter label="acmecorp.io names" counted={1284} unit="names" />
```
