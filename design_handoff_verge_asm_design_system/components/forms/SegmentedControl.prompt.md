2-4 way exclusive toggle (table density, graph facets). Short labels; 5+ options belong in Select.
```jsx
<SegmentedControl options={["comfortable","compact"]} value={d} onChange={setD} label="Row density" />
```
