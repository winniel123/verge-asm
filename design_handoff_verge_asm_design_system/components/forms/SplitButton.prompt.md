Split button — primary act + alternates menu. Canonical use: Export CSV ▾ JSON / PDF.
```jsx
<SplitButton icon={<Icon name="download" size={14}/>} onClick={csv} items={[{label:"Export JSON"},{label:"Export PDF"}]}>Export CSV</SplitButton>
```
