Multi-line Input sibling (label/hint/error/mono, optional autoGrow). Use for annotation reasons and any prose field.
```jsx
<Textarea label="Reason" rows={3} value={v} onChange={(e)=>setV(e.target.value)} />
```
