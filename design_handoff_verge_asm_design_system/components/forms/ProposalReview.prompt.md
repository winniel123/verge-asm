Registry proposals — per-row Confirm (never bulk), checkbox + one button for bulk decline. The asymmetry is the design.
```jsx
<ProposalReview proposals={[{id:"1",value:"acme-corp.net",kind:"name",source:"registrar match"}]} onConfirm={add} onDecline={drop} />
```
