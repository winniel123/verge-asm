Sectioned settings left-nav (micro-label sections, accent pill active).
```jsx
<SettingsNav active="channels" sections={[{label:"Delivery",items:[{id:"channels",label:"Channels",icon:"send"}]}]} onNavigate={go} />
```
