# Agent worktrees — scratch clones, never committed
.claude/worktrees/

# Go build output
/bin/

# Local compose secrets
.env

# Local troubleshooting artifacts — screenshots, dumps, scratch
temp/

# built binaries (go build ./cmd/web etc.)
/web
/worker
/prober

# Agent memory notes — session scratch, never committed
memory/

# commentlint residue manifest — sweep scratch, never committed (SPEC §3.8)
.commentlint/
